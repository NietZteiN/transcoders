### Target Date: 2026-08-04 (E1 path live: converter + SAE loader + real-model smoke)
- **Hypotheses / what we're testing:** None — infrastructure, but with one measured quantity: does the base-trained Llama Scope SAE transfer to Instruct activations well enough to proceed? (Exploratory; no pre-registered threshold.)
- **Setup:** Host `csr-94608`, env `transcoders-mi` (`source scripts/env.sh`), seed 20260724. GPU 1 (auto-picked; GPU 0 was busy with another user's job — the idle-check worked). New code: [`../../src/convert_stimuli.py`](../../src/convert_stimuli.py) (stimuli → Snippet JSONL, cross-tier span derivation), [`../../src/dictionaries.py`](../../src/dictionaries.py) (SAELens/LXR + Qwen-Scope loaders; `apply_dictionary` de-stubbed). Commands: `python -m src.convert_stimuli --validate`; `python -m src.extract_activations --config configs/experiments/e1_semantic_capture.yaml --gpu auto --limit 10`; post-hoc L16 SAE reconstruction on the captured identifier-token activations.
- **Results:**
  - **HF cache discovery:** Llama-3.1-8B-Instruct fully cached (15 GB — gated license evidently already accepted), all 8 panel models cached, and nearly the whole dictionary registry pre-fetched. Only `gemma-2-2b` / `Llama-3.2-1B` absent → E3 PoC re-planned onto cached Qwen3-0.6B + `mwhanna` PLTs. NLA source identified: `kitft/nla-qwen2.5-7b-L20-{ar,av}`.
  - **Alignment-parquet divergence (important negative):** the `L1b_mapping_*` parquets are from a *different generation round* than the study stimuli (A: `fibfib→smoothArea`; parquet: `fibfib→rns`; ~0/10 js L1b rows match). Converter therefore derives mappings **cross-tier from the stimuli** (parallel identifier-token walk).
  - **Converter output:** 100 + 250 rows → `data/stimuli/dataset_{a,b}/*.jsonl`. Span classes (A): orig 713, adversarial 824, l1_neutral 416, fn_adversarial 13, fn_orig 52. **All 20 Dataset-A L1b rows positionally paired** (decoy↔true `rename_map` recovered; flagship JavaScript/63 = `{fibfib→smoothArea, n→_lastNSecs}` ✓); fallbacks only on L1 (10) / L3 (11). Dispatcher var detected on 349/350 L2·L3 rows (Python `while <var>` + JS object-dispatch table). **Span→token resolution 1.0000** on both Llama-3.1-8B-Instruct and Qwen3-0.6B tokenizers (8,034 spans). Report: `data/stimuli/conversion_report.json`.
  - **Dictionary loader:** Llama Scope LXR-8x loads via stock SAELens (`llama_scope_lxr_8x/l16r_8x`, identity+revision recorded); Qwen-Scope `.pt` loader implemented. Random-input shape test green.
  - **Real-model E1 smoke (GPU 1, bf16):** 10 Dataset-A L0/L1b items through Llama-3.1-8B-Instruct; span resolution 1.000; `resid_post_L{12,16,20}` captured at identifier tokens (321 vectors). One real bug caught: snippet ids contain `/` → filename subdirectory crash; fixed (`fs_key` sanitization, logical key kept in manifest).
  - **First transfer measurement — L16 base-trained SAE on Instruct identifier-token activations: FVU = 0.506, cosine = 0.773, mean L0 = 28** (k target 50).
- **What worked / hypothesis verdict:** Converter, loaders, and the full E1 capture path work end-to-end on the real model + real stimuli. SAE transfer: **INCONCLUSIVE** — L0=28 and cosine 0.77 look healthy, but FVU 0.5 is well below Llama Scope's base-data quality. Two candidate explanations: (a) genuine base→Instruct transfer gap (the documented confound), (b) **norm-convention mismatch** — Llama Scope trained with dataset-wise activation normalization and expects TransformerLens-hooked activations; ours are raw HF `hidden_states`. Cosine≫FVU is what a scale mismatch looks like.
- **Observations:** GPU 0 occupied mid-session by another user — `pick_free_gpus` excluded it correctly (shared-box discipline held). The `/`-in-id bug is the second defect only reachable with real data (after the BOS/token-index one) — toy smokes keep proving insufficient exactly once per data boundary.
- **New questions / new hypotheses:** **Q-norm (blocking E1 analysis):** does rescaling raw HF activations by Llama Scope's `norm_scaling_factor` (or extracting via TransformerLens hooks) close the FVU gap? CONFIRM scale-mismatch if FVU drops substantially (→ use corrected pipeline); REFUTE if FVU stays ~0.5 (→ it's real transfer loss; Goodfire l19 Instruct-trained SAE becomes the E1 primary and Llama Scope the robustness check).
- **Next Steps:** (1) resolve Q-norm (compare TL-hooked vs raw-HF activations + scaling factor on one snippet); (2) then the full Dataset-A E1 sweep (all 100 rows, layers 12/16/20) + auto-interp labeling of decoy/true feature sets; (3) NLA replication check (`nla/src/replicate_example.py`) when convenient — gear verified downloaded + complete today, wired into charter §3.
