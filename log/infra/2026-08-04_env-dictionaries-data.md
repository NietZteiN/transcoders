### Target Date: 2026-08-04 (Env finalized · dictionaries pinned · stimuli/behavioral located)
- **Hypotheses / what we're testing:** None — infrastructure. Goal: clear the three Phase-0 blockers left by [`2026-08-03_pipeline-scaffold-smoke.md`](2026-08-03_pipeline-scaffold-smoke.md): conda env, dictionary repo ids, stimuli/behavioral data.
- **Setup:** Host `csr-94608`, no GPU. Env build: `conda env create -p /data/jvl210002/conda_envs/transcoders-mi -f environment.yml` (background, ~5.5 min, log `data/env_create.log`), then `pip install git+circuit-tracer@8f1e243`. Hunts: a 6-agent workflow — 4 dictionary hunters (protocol: repo counts as found ONLY if verified via `HfApi.model_info` with sha captured) + 2 read-only filesystem hunters for stimuli/behavioral tables. Workflow was interrupted by a session exit and resumed from cache (`resumeFromRunId`).
- **Results:**
  - **Env `transcoders-mi` READY:** torch 2.13.0+cu130 (CUDA OK, 4 GPUs), sae-lens 6.47.0, transformer-lens 3.2.1, nnsight 0.6.1, circuit-tracer 0.5.2. Two gotchas: (a) CXXABI import failure → `LD_LIBRARY_PATH=$ENV/lib`, wrapped in [`../../scripts/env.sh`](../../scripts/env.sh); (b) **circuit-tracer downgraded** transformers 5.14→4.57.3, transformer-lens 3.6→3.2.1, nnsight 0.7→0.6.1 — `pip check` clean, all imports co-verified, smoke green in-env; install-order documented in [`../../environment.yml`](../../environment.yml) (exact pins) + `environment.lock.txt` (full 162-line freeze). Smoke in-env: 4 items, mean span-resolution 1.000.
  - **Dictionaries: all 14 registry entries pinned** with verified shas in [`../../configs/dictionaries.yaml`](../../configs/dictionaries.yaml). Highlights: Llama Scope = 8 repos under `OpenMOSS-Team` (org renamed from fnlp), LXR loads with stock SAELens, LXTC (transcoders, all 32 layers) needs OpenMOSS lm_sae tooling; **Qwen-Scope turned out to be official `Qwen/SAE-Res-*` repos** (plain torch `.pt` dicts, not SAELens); BluelightAI CLTs are Base-trained while **`mwhanna/qwen3-*-transcoders-lowl0` match the post-trained panel checkpoints** (registered as preferred); Goodfire l19 is the only Instruct-trained Llama dictionary (transfer-validation anchor); Gemma Scope res/tc + the `mntss` circuit-tracer shim (weights stream from the google repo — pin both shas per run).
  - **Models: all 12 hf_ids verified (zero 404s), revisions pinned** in [`../../configs/models.yaml`](../../configs/models.yaml). ⚠️ 3 gated: Llama-3.1-8B-Instruct, gemma-2-2b, Llama-3.2-1B — metadata reads fine, weight download needs license acceptance on the HF_HOME account.
  - **Stimuli + behavioral tables LOCATED and symlinked** into `data/` (10 links, all resolve; provenance in [`../../data/DATA_SOURCES.md`](../../data/DATA_SOURCES.md)). Dataset A = `full_human_experiment_v2.json` (100 rows, 20×5, `fibfib`→`smoothArea` confirmed present); Dataset B = `tasks_unified_50.json` (250 rows, 50×5); identifier alignment = `*_test_with_L1b.parquet` (`L1b_mapping_var` orig→adversarial name maps — spans computable by string search); Paper 2 joins = `trials.parquet` (31,711 rows; `is_core==1` → 29,546) + `adv.parquet` (ISF, 71 snippets) + `tasks_cf.csv` (dispatcher complexity); Paper 3 = graded human CSV (n=73 study) + `model_results.xlsx` (1,584 rows).
- **What worked / hypothesis verdict:** N/A (infra). The verify-or-it-didn't-happen protocol paid off: every repo id in the registry carries a sha that `model_info` actually returned; zero guessed ids.
- **Observations:** (1) **SFR embeddings are NOT on this host** (`MISSING_DATA.md` in the dual-system repo) — semantic-displacement measures exist only as proxies in `adv.parquet`; E1's SCS↔displacement correlation must use those proxies or regenerate embeddings. (2) **HCI is a derived quantity** (`correct==0 & confidence_z>0`, z within model×prompt_condition; reference: `stats/R/06_rq45_adversarial.R`) — the E1 join must reproduce it, not look it up. (3) Canonical stimuli hub is `model_understanding/base/data/corpus/`; the `dataset/` tree and `* copy` dirs are duplicates.
- **New questions / new hypotheses:** None scientific. Infra: is `facebook/crv-8b-instruct-transcoders` (unverified lead; Instruct-trained PLTs for Llama-3.1-8B) real? If so it de-confounds E3-on-8B.
- **Next Steps:** (1) **stimuli converter**: source JSON → Snippet JSONL with `identifier_spans` from `L1b_mapping_var` + span-resolution report; (2) implement `apply_dictionary` (SAELens/LXR path first); (3) accept the 3 gated licenses; (4) Gemma-2-2B or Llama-3.2-1B real-model smoke; then the E1 Llama-3.1-8B extraction sweep.
