### Target Date: 2026-08-03 (Pipeline scaffold + smoke + adversarial review)
- **Hypotheses / what we're testing:** None — infrastructure. Goal: stand up the Phase-0 pipeline scaffold (`configs/`, `src/`, `data/`, `scripts/`, `environment.yml`) with the charter's seed/GPU/provenance discipline built in, and validate it end-to-end at micro-scale before any real run.
- **Setup:** Host `csr-94608`, no GPU used (smoke is CPU-only by design). Interpreter for checks: base miniconda python 3.13 (torch 2.9.1+cu128, transformers 4.57.1) — **interim only**; the project env `transcoders-mi` is specified in [`../../environment.yml`](../../environment.yml) but not yet created. Seed 20260724 throughout. Commands: `python3 -m py_compile src/*.py`; torch-free unit checks (config merge, layer validation, span helpers, provenance, seed determinism); `bash scripts/smoke.sh` (tiny-gpt2, 4 toy snippets incl. the fibfib/smoothArea L0/L1b pair); a CWD-independence run launched from `/`. Review: a 34-agent adversarial workflow (3 lenses — python correctness, config↔code consistency, charter compliance — each finding verified by an independent refuter).
- **Results:**
  - Scaffold built: `configs/` (compute/models/dictionaries/data + `experiments/_base.yaml`, `e1_semantic_capture.yaml`), `src/` (7 modules), `scripts/smoke.sh`, `data/` landing zones, `environment.yml`.
  - Initial smoke PASSED (2 transformers deprecations found+fixed: `torch_dtype`→`dtype`; `output_hidden_states` moved to the forward call).
  - **Review: 31 raw findings → 28 confirmed, 3 refuted** (~11 root causes). Highs: (1) static token indices can't survive tokenizer differences (Llama BOS off-by-one would have silently corrupted E1's Semantic-Capture Score — smoke never exercised the identifiers path); (2) all paths CWD-relative (runs from elsewhere would write outside `data/` and hash zero scripts); (3) `hidden_states[l]` vs Scope-style `resid_post` layer numbering ambiguous; (4) `snippet__tier` keys collide across languages + same-second run dirs merge.
  - All confirmed findings fixed: **character-span schema** (`identifier_spans`/`dispatcher_spans`) resolved to tokens per model via fast-tokenizer offsets with decode-overlap verification and a hard `min_resolution_rate: 0.95` gate; `PROJECT_ROOT` anchoring everywhere; explicit `resid_post_L{l} = hidden_states[l+1]` convention (out-of-range layers raise); `snippet__language__tier` keys + duplicate guard + pid-suffixed `exist_ok=False` run dirs; provisional manifest written at run start + incremental `manifest.jsonl` (crash still leaves provenance); resolved HF revision recorded; dead knobs wired (`dtype`, idle thresholds from `compute.yaml`) or removed; safetensors fallback removed (raises); empty-dataset and no-spans conditions raise; Qwen-Scope↔0.6B pairing corrected to Qwen3-1.7B; `smoke.sh` empty-glob abort fixed.
  - Post-fix validation PASSED: byte-compile; unit checks; smoke now exercising the **identifier-span path** — 4 items, **mean span-resolution 1.000**, keys `toy_fibfib__javascript__L0` etc., `resid_post_L{0,1}` tensors; CWD-independence run from `/` wrote correctly under `PROJECT_ROOT/data/activations/`.
- **What worked / hypothesis verdict:** N/A (infra). The §4 smoke-first discipline earned its keep twice: the deprecation pair, and the review catching a flagship-corrupting alignment bug *before* any GPU hour was spent.
- **Observations:** The worst finding (static token indices) was invisible to the passing smoke because smoke forced `token_positions='last'` — the fix includes making smoke exercise the same code path as the real experiment (toy snippets now carry real spans). Lesson consistent with the charter's silent-failure doctrine: a smoke test that skips the risky branch is a false comfort.
- **New questions / new hypotheses:** None scientific. Open infra: confirm + pin the dictionary repo ids (`configs/dictionaries.yaml` all `null`); create the conda env and freeze pins; import stimuli/behavioral tables (the HT1/HT3/HT6 join).
- **Next Steps:** (1) create `transcoders-mi` env from `environment.yml` + freeze; (2) locate + pin Llama Scope / Qwen-Scope / Gemma Scope repo ids and model revisions; (3) import Dataset A + behavioral tables into `data/`; (4) implement `apply_dictionary` (SAELens) once ids are pinned; (5) Gemma-2-2B/Llama-3.2-1B real-model smoke before the Llama-3.1-8B E1 run.
