# Experiment log — master index

*Last updated: 2026-09-07*

The continuous research ledger for the `transcoders/` sub-project (protocol in [`../CLAUDE.md`](../CLAUDE.md) §6). Entries are organized **by thread and by date**: `log/<thread>/YYYY-MM-DD_<slug>.md`. Copy new entries from [`TEMPLATE.md`](TEMPLATE.md). **Append-only** — never edit a past entry; corrections go in a new dated entry that references the old one.

On every new entry, update **both** tables below and the thread's own `README.md`.

## By thread

| Thread | Status | Entries | Purpose |
|--------|--------|---------|---------|
| [`setup/`](setup/) | done (ongoing) | 3 | Foundational-paper registration, goal-artifact organization, charter + scaffold, hypotheses/task checklist. |
| [`infra/`](infra/) | active | 3 | Pipeline code, conda env, configs, seed/GPU/provenance discipline. **E1 path live end-to-end 2026-08-04 (converter + SAE loader + real-model smoke); Q-norm open.** |
| [`nla-harness/`](nla-harness/) | active | 118 | **2026-09-07 — H-W31 RESOLVED (`W31-CONCENTRATED` / `W31-SAME-CIRCUIT`, job 382366): 8 of 255 components carry 60 % of the transported state, led by the global-layer head `L41H4`, and the NLA's version routes where the raw state does (ρ 0.838); Phase B (multi-layer NLA on Gemma-3-4B-it) scoped in `docs/nla_multilayer_scoping.md`.** 2026-09-06/07 — `W16-MEANING-CARRIES-IT`: on the repaired 471-span / 60-item anchoring the true-name vs nonsense-name contrast is **+21.05** [+17.80, +24.28] in both languages (meaning ≈ 51 %, decoy removal ≈ 49 %, structure ≈ 1 %); `arm_guard` added after two zero-position bugs; the `?unpaired` sentinel traced to the stimulus pipeline (H-W19). Instrument-2 NLA replication + faithfulness + belief-steering experiments. **2026-09-04/05 — Experiment W asked the steering question nobody had asked (every banked "NLA" arm fed the AR a *template string*, not a verbalized read, so `V1 ≡ V3` with CI [0,0] was close to guaranteed by construction) — and it inverted the programme's standing conclusion. The NLA's own read→edit→reconstruct→write-back protocol does NOT steer (+19.47 nats, at the banked random benchmark, below prompting). But the same operator fed the *clean* state reaches **+45.71 = 40 % of a full prompt swap and beats prompting** — the first arm in this programme ever to do so — and the NLA round trip preserves **98.3 %** of that causal power [0.967, 0.999]. **So the channel was never the bottleneck; the language edit is.** The null battery makes it **item-specific** (foreign item's clean span keeps only 33 %, matched wins 49/49) but **NOT span-specific** (a sibling span keeps **87.4 %**) — while stage 0 measured the round trip as sharply span-specific *representationally* (+0.6807 vs +0.0525). **Representation separates spans; causation only separates items.** And restoring clean identifier representations 98 % faithfully still does **not** restore correctness (0.714 → 0.694), so the trap's damage is not confined to identifier positions.** **Phase 0 DECIDED 2026-08-28: P0.1 HARD · P0.2 NOT CHANNEL-LIMITED · P0.3 SITE LIVE → licensing row 4 → Phase 1b (readout paper with a bounded causal negative).** **P0.4 → NOT DEPTH-LIMITED (2026-08-28)**, the third independent leg under row 4. **Experiment L then retired the fix: the token inflation does no independent work (ρ_partial −0.159), and T ends on this corpus. Its companion claim that renaming dose doesn't predict cost was **withdrawn the same day**: that −0.166 was an artifact of a per-token normalisation (ρ(G, 1/reply-length) = +0.889), and on total cost the dose effect is **+0.378 [+0.104, +0.606]** — inside its original pre-registration. What survives as the real predictor is **decoy/true term overlap** (−0.352 partial, −0.382 on the total): decoys sharing terms with the truth cost less.** **The patching map (T) is not executable on this corpus: 0/60 L0/L1b pairs admit a 1:1 token map, because adversarial renaming inflates prompts by a median of 27 tokens — which also flags an uncontrolled token-count confound in every length-sensitive L0-vs-L1b measure across the study.** **R2 (2026-09-03) closes the steering question on this site: with the corrupt term dropped and damage vetoed, the only two arms that move the model toward its own correct reply are a prompt change (23 % of a prompt swap) and a random vector (18 %) — every belief-shaped write at the banked site is *negative* and worsens with magnitude, so the nulls were never a matter of too-weak writes.** **2026-09-03 (R/S/T): the continuous readout built to escape the 6-item flippable ceiling is itself confounded — ΔM is monotone in damage, so the clean half moves the *wrong way* in 21 of 28 arms and matched random overlaps the one 'supporting' belief arm; an optimiser given the site free rein reached held-out ΔM +20.8 by driving greedy accuracy 0.767 → 0.000 (L0 specificity −48.8), rescuing 1 flippable item across 5 splits. Both headline verdicts (`R-1`, `S-LIVE`) are rule artifacts and are recorded as such rather than retro-edited; the patching map refused at its own smoke gate (0.784 < 0.80) before spending a GPU-hour. Prompting still beats every vector on the non-damage half.** **2026-09-03: NLA steering replicates its refutation on a permitted host (V1 ties V3 exactly, McNemar p=1.00 at three α), coverage is not the limitation (1 vs 44 tokens, churn 0/60), and no arm in any experiment ever recovered more than 2 of 60 items** — too weak and the writes do nothing, strong enough and they only destroy. The site controls wording, not correctness. Earlier the same day, the mechanical challenge to the write-side nulls returned UNINFORMATIVE — single-layer steering does not move the task on Gemma, so there was nothing for the multi-layer fix to rescue. The channel is demonstrably live (random directions damage −0.15; every steered reply differs) but no belief-shaped edit, up to exact clean-state replacement at all 33 layers, moves correctness beyond the reproducibility floor. Formerly stated as (H-C0/H-C1/H-C2) — the one-block hook leaves the K/V at all layers below it un-edited at the steered position (measured max |Δ| = **0.000**), so a banked intervention reached later tokens through the **top 7 of 28 layers only** — the bottom 21, 75% of the depth, still carry the un-edited decoy. (The stronger phrasing 'never changed what the model read' was filed and is **withdrawn**; it is false and contradicts itself.) Running on Gemma-3-12B after two Qwen jobs were cancelled under the model constraint. P0.3-ext pre-registered but blocked on `adversarial_rename`. Earlier: G0 PASS · confirmatory family CLOSED (HT12/HT13/HT14) · B1 ✗ · B4 ✗ across a 16× α range · B5 null · E5b flipped B0's sign at a second seed. N1–N3 (HT9–HT11) parked. |

<!-- Planned threads (create the folder + seed its README when the first entry lands):
     sae-features (E1/E6), steering (E2), attribution-graphs (E3), state-binding (E4), triangulation (E7) -->

## Timeline

| Date | Thread | Entry | Headline |
|------|--------|-------|----------|
| 2026-09-07 | `nla-harness` | [`2026-09-07_head-mediation-results`](nla-harness/2026-09-07_head-mediation-results.md) | **`W31-CONCENTRATED` + `W31-SAME-CIRCUIT` — 8 of 255 components carry 60 % of the transported state, and the NLA's version routes exactly where the raw one does.** Job 382366 (h200, **2:27:24**, COMPLETED, 0 errors), 471 repaired spans / 60 items / 1,459 written prompt positions, **93,060 sweep + 5,340 joint forwards at 68 ms**. **Identity gate passed exactly** — `SELF` max **0.0000** over 960 assertions, `ALL` **0.0000**, `ko_gap` **0.0000** (bit-identical, not merely inside the 0.05 tolerance); anchor `P_patch` **+41.52** [+36.95, +46.25] reproduces H-W23's repaired **+41.52** from a different script. **H-W31a:** top-k chosen on the opposite crc32 half recovers **0.80 / 0.83** of dG_S at k=16 against a ≥ 0.50 bar and **+30.92 / +34.06** over the random-16 null (0.75 / 0.82 of the effect) against a ≥ 0.30 bar — 50 % is first crossed at **k = 8, 3.1 % of the components**, and a **single head, `L41H4`, carries 18 %**. Uniform-random stays ≈ 0 while the **layer-matched** null reaches +13.95 at k=32, so depth alone buys a third of the localisation — the null that mattered. **H-W31b:** 14 heads + 2 MLPs in the top-16; best head `L41H4` (+7.28/+7.86), best MLP **`L34M`** (+4.81/+5.39) one layer above the write, which the prereg did not anticipate. Summed necessity concentrates on the **global layers 41 (+4.78) and 47 (+5.74)**, the ones that can see a span 1,024+ tokens back. **H-W31c → SAME-CIRCUIT** (Spearman **0.838**, Jaccard top-16 **0.78**): 98.3 % causal fidelity is faithful in *where* it acts too — closing the last place that number could have hidden a different route. **But the residue is systematic**: C3pure sits below P_patch with CI-clearing margins at precisely the load-bearing components (`L46H1` −1.02, `L34M` −0.58, `L41H4` −0.58, `L45H3` −0.47, `L47H2` −0.46) — the ~1.7 % loss is not spread evenly, it is a consistent shortfall at the heads that carry the most. **H-W31d ✓ primary / ✗ secondary:** global-minus-local necessity **+0.291** [+0.230, +0.355] clears, but the predicted *widening* on long items fails — **+0.270 long vs +0.296 short** (n = 11). **H-W31e:** ρ(read, nec) 0.49/0.43 with **13 of the top-16** having a read CI excluding zero — the carrying heads attend to the spans directly. Also measured: single-component sufficiencies sum to only **0.62–0.68** of dG_S and summed necessity is **negative at nine of fifteen layers**, so the effect is a concentrated positive net of a diffuse drag. Raises H-W35 (specificity: do the top-8 heads matter with no write at all?), H-W36 (the residue predicts anisotropic AR error — no GPU), H-W37 (is `L34M` transport or re-normalisation?). Phase B's layer choice is now informed: a second NLA layer belongs **just above 41**, not spaced by depth. |
| 2026-09-07 | `nla-harness` | [`2026-09-07_pairing-recovery-results`](nla-harness/2026-09-07_pairing-recovery-results.md) | **H-W28 ✓ — the identifier pairing recovers 3.05× at precision 1.000, and routing L3 through L2 is what makes JavaScript L3 recoverable at all.** CPU, ~40 s, no GPU. Re-derived the original↔renamed mapping for all 350 stimulus rows with `repair_pairs.recover()` at its H-W22 defaults, anchoring L1/L1b on **L0** and — following this morning's [`2026-09-07_l3-is-l1-not-l1b.md`](2026-09-07_l3-is-l1-not-l1b.md) — **L3 on L2**, which it differs from by a rename only. **Corpus totals 539 pipeline pairs → 1,642 usable (3.05×), 1,103 recovered pairs written**, precision **1.000 in every cell that could be scored (539/539 comparable pairs agree)**. 10 of 12 cells licensed; the frozen gate (precision ≥ 0.95 on ≥ 20 comparable pairs) **refused both L1·javascript cells** for lack of ground truth, not for being wrong — dataset A's 5 comparable pairs all agree but 5 < 20, and dataset B records none at all; their 181 recovered pairs are computed, reported and **not written**. **H-W28b is the decisive part:** on Python L3 the L2 anchor is equal-or-better (1.000 vs 1.000; **1.000 vs 0.968**), but on **JavaScript L3 the L0 anchor recovers no checkable pair whatsoever** while L2 gives 24 at 1.000 and unlocks **401 pairs** — the worst cells in the corpus (100 % and 87.9 % sentinel) become the best-served. This is the morning's charter correction paying for itself as an engineering fact within hours; H-W22 had left L3 out precisely because the L0 route scored 0.833 on JavaScript. Output is a **sibling file** (`data/stimuli/pairing_recovered.jsonl`) carrying a per-key `source ∈ {pipeline, recovered}` — the stimulus files are never edited in place — so **Instrument 1 now has a validated mapping to join against** for every tier except L1·javascript. Bound quoted with the result: validation is only possible where the pipeline succeeded, so 1.000 is an **upper bound** over the slots actually filled; the number to trust is *1.000 on 539 checkable pairs*, not on 1,103 recovered ones. Raises H-W34 (L1·javascript is unrepairable from inside this corpus — only the obfuscation generator, which did not travel to this host, could settle it). |
| 2026-09-07 | `nla-harness` | [`2026-09-07_pairing-recovery-prereg`](nla-harness/2026-09-07_pairing-recovery-prereg.md) | **PRE-REGISTRATION H-W28: re-derive the identifier pairing for the whole stimulus set, and route L3 through L2.** Frozen before running. Current damage measured first: **749/1,288 = 58.2 %** of `rename_map` keys are `?unpaired` sentinels, worst at A·L3·javascript **92/92**, B·L1·javascript **123/123**, B·L3·javascript **175/199**. The one design decision follows from [`2026-09-07_l3-is-l1-not-l1b.md`](2026-09-07_l3-is-l1-not-l1b.md): `recover()` bridges a rename well and a restructuring badly (H-W22 measured 1.000 on L1/L1b but **0.833 on JavaScript L3** anchored on L0), and since L3 = L1 ∘ L2 it differs from **L2 by a rename only** — so L1/L1b anchor on L0 and **L3 anchors on L2**, with both routes scored so the choice is measured rather than asserted. Gate: a (dataset, tier, language) cell is written only at **precision ≥ 0.95 on ≥ 20 comparable pairs** against the pairs the pipeline did record; an unscoreable cell may borrow the same tier+language verdict from the other dataset, marked distinctly. Output is a **sibling file**, never an in-place edit of the shared stimuli. Rejected: re-implementing positional matching (H-W22 showed it recovers nothing — it is what the generator already tried); regenerating the tiers (the pipeline did not travel to this host). Bound stated in advance: validation is only possible where the pipeline succeeded, and those are plausibly the easy cases, so any precision measured is an **upper bound** on the slots actually filled. |
| 2026-09-07 | `nla-harness` | [`2026-09-07_l3-is-l1-not-l1b`](nla-harness/2026-09-07_l3-is-l1-not-l1b.md) | **L3 stacks the NONSENSE rename, not the adversarial one — the charter's "decisive test" does not exist in this corpus.** Measured over all 70 items carrying every tier (`data/stimuli/dataset_{a,b}_source.json`, CPU, read-only): identifiers **L1b** introduces appear in L3 with mean fraction **0.0000** on **0/70** items; identifiers **L1** introduces appear with fraction **1.0000** on **70/70**. **L3 = L1 ∘ L2**, not L1b ∘ L2. The flagship is legible by eye — L0 `fibfib(n)` → L1b `smoothArea(_lastNSecs)` → **L3 `fibfib(a)`** with a dispatcher table; the decoy never appears. Corroborated by `convert_stimuli.py:121`, which derives L3's rename map by pairing `("L2","L3")` and never `("L1b","L3")`. **Three consequences.** (1) The test named in `CLAUDE.md` §3 — "L3 stacked, the decisive test where the two routes compete for one budget" — **has no stimulus in this corpus**, because the adversarial atom route is not in L3; HT3's "breaks at L3" must be re-read before E3 is designed. (2) **1,327 L3 spans are mislabelled `adversarial`**: the converter's shape heuristic catches Python's `var_68f8` but not JavaScript's `a`/`b`/`uepoi`, so they fall through to the decoy class on code containing no decoy (Dataset A js 436, Dataset B js 891). (3) It **excludes** the two-routes-compete answer to the standing puzzle of why L3 (+0.0418) is weaker than L2 (+0.1253); the measured explanations stand — inflated code size raising L3's own baseline to +0.7893, and repetition becoming the binding predictor (+0.2531, driving Llama L3 to −0.0845). It also explains why L1 and L3 fail the identifier pairing together on JavaScript (97.3 % / 91.8 %) while L1b is perfect there (0.0 %): L1 and L3 share the mangling that breaks equal-length positional matching. Raises H-W32 (build a true L1b ∘ L2 tier) and H-W33 (classify L3 spans by what the tier did, not by name shape). |
| 2026-09-07 | `nla-harness` | [`2026-09-07_head-mediation-prereg`](nla-harness/2026-09-07_head-mediation-prereg.md) | **PRE-REGISTRATION H-W31: which downstream heads and MLPs carry the NLA-transported clean state?** Activation patching between an unsteered L1b run (U) and the same run with `C3pure` / `P_patch` written at L32 (S), over **240 heads + 15 MLPs of layers 33–47**: `suf_c = dG(U, c←S)`, `nec_c = dG(S) − dG(S, c←U)`, plus a read-side knockout (head blind to the span keys, against a mask-only reference). Joint greedy top-k (k ∈ 1…32) **selected on the opposite crc32 half** vs uniform random-k (3 seeded draws), layer-matched random-k, and ALL. Gate H-W31-ID: every `SELF_c` within 0.05 nats, ALL within 5 % of dG(S), every `ko_gap[L]` within 0.05 → else exit 3 `W31-HARNESS-FAULT`. H-W31a CONCENTRATED (TOP_16 ≥ 50 % of dG(S) and ≥ 30 pts over RAND_16) / DISTRIBUTED (no k ≤ 32 reaches 50 %) / INTERMEDIATE; H-W31c SAME-CIRCUIT (ρ ≥ 0.80, Jaccard ≥ 0.50) / DIFFERENT (ρ < 0.50); H-W31d global heads 35/41/47 carry more `nec` (17/60 items have T ≥ 1024). Vectors on the repaired 471-span / 60-item anchoring, so **H-W25's fidelity ratio falls out as a secondary**. ≈ 98 k forwards, 2–3.5 h on one H200; config `nla/configs/nla_heads.yaml`. Stated priors: INTERMEDIATE, SAME-CIRCUIT. **Submitted as job 382366.** |
| 2026-09-07 | `nla-harness` | [`2026-09-07_repaired-rerun-results`](nla-harness/2026-09-07_repaired-rerun-results.md) | **`W16-MEANING-CARRIES-IT` — H-W23: the meaning effect replicates at 3.6× the span set and holds in JavaScript; the Python-only bound is lifted.** Job 380671 (h200, 55:20, 0 errors). Coverage **471/476** spans anchored for both L0 and L1 (was 375/131 with 88 unanchored); items 49 → **60**. SELF −0.0175 PASS. `T_L0_all` **+41.52** [+36.84, +46.21], `T_L2_all` +41.06, `T_L1_sub` **+20.47** [+17.31, +23.69]. H-W23a gap **+21.05** [+17.80, +24.28], clearing on **58/60** (H-W17a had +21.82 on 20 Python items). H-W23b: Python n=30 **+17.83** [+13.71, +22.33] ✓, JavaScript n=30 **+24.28** [+19.57, +28.84] ✓. H-W23c structure **+0.46** [+0.07, +0.88], replicating +0.66. Decomposition on 60 items: **meaning ≈ 51 %, decoy removal ≈ 49 %, structure ≈ 1 %**. Raises H-W25 (fidelity + null battery on the repaired set) and H-W26 (JS gap > Python, CIs overlap). |
| 2026-09-07 | `nla-harness` | [`2026-09-07_rename-map-audit`](nla-harness/2026-09-07_rename-map-audit.md) | **Audit of the `rename_map` defect's blast radius.** `src/convert_stimuli.py:derive_rename_map()` zips equal-length identifier lists positionally and emits `?unpaired{i}` keys with `pairing_ok=False` otherwise — JS member-access rewrites (`lst.length` → `c['length']`) and L3 stacking break the length assumption (97.3 % JS L1, 91.8 % JS L3, 0.0 % JS L1b). `meta.pairing_ok` exists on every row (dataset_a: 39 True / 21 False / 40 None) and is honoured by `task_bank.py` and `overnight_capture.py`; **one banked arm does not honour it**: `trace_llr.py` V2 substituted the literal `?unpaired0` on **11 of 60 items** (e.g. `_node_to_msg → ?unpaired0`) — V2 is secondary and the negative verdict's direction stands, but its clean denominator is 49/60; guard added. W-family scripts survived by accident (the sentinel fails `term_spans`). Raises H-W28 (offline `rename_map` re-derivation as a sibling field); blast radius to Instrument 1's attention measures unresolved. |
| 2026-09-07 | `nla-harness` | [`2026-09-07_ladder-position-confound`](nla-harness/2026-09-07_ladder-position-confound.md) | **The `arm_guard` refused a contrast and was right: H-W15's residual is a position-count artifact — the verdict survives, its caveat does not.** Job 380557 died at 8:29 on `ArmNotWritten: R_2-P_2: no item where both arms wrote at matching positions`. Forward vs reverse ladders write different token counts (k=1 3.24 vs 3.55, equal on 27/49; k=2 6.12 vs 6.63, 16/49; k=4 12.22 vs 13.37, 8/49). At the dose curve's ~1.86 nats/position the extra positions predict **+0.58 / +0.95 / +2.12** against the observed +1.10 / +0.98 / +2.40 — **53 % / 97 % / 88 % explained**; residual-vs-position Spearman +0.144 / +0.279 / +0.283. Fix: drop the ladder (`--no-ladder`) rather than weaken `paired()`; job 380566 re-runs without it. Tenth defect in the family, **the first caught by an automated invariant in a banked result**. Raises H-W24 (verify position-count equality on `T_L0_all − T_L2_all` and `T_L0_sub − T_L1_sub`). |
| 2026-09-07 | `nla-harness` | [`2026-09-07_flippable-census-results`](nla-harness/2026-09-07_flippable-census-results.md) | **Flippable census — Llama clears the letter of the power gate and fails its spirit; Gemma was already the better trap host.** Job 381294 (25:10, 60/60, two greedy generations per item, no steering). llama8b: L0 0.583 → L1b 0.533 (−5.0 pts), →wrong **9**, →right **6**, net 3, churn 15; gemma12b: 0.733 → 0.633 (−10.0 pts), →wrong 6, →right **0**, net 6. `clears_power_gate: true` (9 vs threshold 9.0) but the pass is not real: Llama's 6 *reverse* flips sit inside the 0.85–0.90 greedy-reproducibility floor's predicted 6–9 disagreeing items. Rule stands as fired (fourth time a threshold measured the wrong thing). The accuracy question is unavailable on either host — the corpus is 60 items and cannot grow. Retrospectively justifies `G_sum` as the readout. Raises H-W29 (net penalty with a reverse-flip check as the power measure) and H-W30 (stimuli built to have a denominator). |
| 2026-09-06 | `nla-harness` | [`2026-09-06_unpaired-sentinel-defect`](nla-harness/2026-09-06_unpaired-sentinel-defect.md) | **H-W19 ✓ resolved — the `?unpaired` sentinel is a stimulus-pipeline pairing failure, not a property of the obfuscation, and it holes identifier correspondence tier- and language-dependently.** JS L1 items DO carry a `rename_map` (30/30, as 40/40 Python) and renaming thoroughness is identical across languages (0.521 vs 0.522 of L0 identifiers absent from L1). Sentinel share of keys: L1 **48.7 % Py / 97.3 % JS**; L1b 41.7 % / 0.0 %; L3 45.4 % / 91.8 %. Two corrections: the 2026-09-04 fidelity prereg's "renaming *introduced* identifiers" inference is wrong (the 88/476 spans do have a clean counterpart; H-W7's operational exclusion stands), and the H-W17 subset is "items whose L1 pairing metadata survived", not "thorough renaming". Any consumer of `rename_map` inherits a 42–97 % hole — Instrument 1 included. Raises H-W22 (re-align the 88 spans offline). |
| 2026-09-06 | `nla-harness` | [`2026-09-06_repaired-rerun-prereg`](nla-harness/2026-09-06_repaired-rerun-prereg.md) | **PRE-REGISTRATION H-W23: H-W17 re-run on repaired pairings — the first test of the meaning effect in JavaScript.** `repair_pairs.recover()` (commit `2edf5ca`) validated at precision 1.000 / recall 1.000 on L1 and L1b in both languages; coverage on the 476 spans **L0 375 → 471, L1 131 → 471** (JavaScript 7 → 240). Recorded `rename_map` keeps precedence; recovery is consulted only where a `?unpaired` sentinel exists. H-W23a `T_L0 − T_L1 ≥ +12.11` on 471 spans; H-W23b the same split by language; H-W23c `T_L0 − T_L2` (expected ≈ 0; a large value would impugn the expanded set). `arm_guard` wired in, SELF exit 3, bootstrap 10,000, seed 20260724. No expectation recorded for H-W23b — the author had been wrong twice in the direction of the prior. |
| 2026-09-06 | `nla-harness` | [`2026-09-06_arm-guard-and-audit`](nla-harness/2026-09-06_arm-guard-and-audit.md) | **H-W20 — audit of every banked W arm, and a guard that refuses to average an arm that wrote nothing** (no GPU, commit `a90ccfc`). Five of six banked runs (377399, 377835, 378019, 379075, 379819) have no zero-position items; only meaning 379908 is affected: `n_positions_sub = 0` on **29/49** and `T_L2_all` on 49/49 rows at exactly 0.0. `nla/src/arm_guard.py` adds two detectors — `n_pos_<arm> == 0`, and `dG` **exactly 0.0** for banked rows without the field (detector 1 alone reproduced the bad verdict) — with per-arm `allow_exact_zero` (SELF scored exactly 0 on 21/49) and `paired()` refusing mismatched position counts. 379908 now scores `W16-ARM-ABSENT`; 379819 re-scores unchanged (+0.66 structure, +8.70). Does NOT fix defect #7 (the `tier_anchor` fallback is semantic). Nine defects in the family. Raises H-W21 (four other scorers still average raw columns). |
| 2026-09-06 | `nla-harness` | [`2026-09-06_hw17-results-and-two-bugs`](nla-harness/2026-09-06_hw17-results-and-two-bugs.md) | **H-W17a ✓ — meaning carries 64 % of the item component on Python; plus two bugs, one of which invented a verdict contradicting a result already in hand.** Job 379908 (45:23, 0 errors). On the 20 written items: `T_L0_ren` **+34.19** [+28.44, +40.07] vs `T_L1_ren` **+12.37** [+9.36, +15.41], gap **+21.82** [+15.52, +28.26], positive on 19/20; nonsense recovers 0.362 of truth. **Bug #8:** `--strict-anchor` also stripped L2's only anchoring path, so `T_L2_all` wrote at zero positions (dG +0.00, CI [+0.00, +0.00]) and the scorer emitted `W16-STRUCTURE-CARRIES-IT` from 45.71 − 0.00 — void, contradicting 379819's 98.6 % L2 recovery. **Bug #9:** 29 structural zeros averaged into both sub-arms reported +13.95 and a failing +8.91 gap instead of +34.19 / +21.82 — corrected without GPU. Three of the last four verdict lines were artifacts. Raises H-W20. |
| 2026-09-06 | `nla-harness` | [`2026-09-06_hw17-language-confound`](nla-harness/2026-09-06_hw17-language-confound.md) | **Filed BEFORE the result: H-W17's subset is a language split, and the frozen rule underweights it.** H-W17b on CPU, anchored (n=20) vs others (n=29), Mann–Whitney + BH-FDR: `G_sum` unit 115.13 vs 109.12 (q 0.540), L0 accuracy 0.800 vs 0.759 (q 0.746), prompt tokens 224.3 vs 265.1 (q 0.157), spans/item 7.60 vs 7.69 (q 0.746), **is-JavaScript 0.05 vs 1.00 (q 0.000)**. One of five at q < 0.05, so the "≥ 2 of 5" rule does not flag `corpus-bounded` — but the one difference is a near-perfect partition (19/20 Python vs 29/29 JavaScript). H-W17a must be cited as "on 20 Python items"; the nonsense-vs-truth question is unaskable in JS on this anchoring. Rule stands as frozen (sixth threshold to measure the wrong thing). Raises H-W19. |
| 2026-09-06 | `nla-harness` | [`2026-09-06_meaning-clean-prereg`](nla-harness/2026-09-06_meaning-clean-prereg.md) | **PRE-REGISTRATION H-W17: the clean nonsense-vs-truth contrast, and a correction to what it was claimed to fix.** All 131 `rename_map`-anchored spans sit in the **same 20 items** (6.55 each; the other 29 have zero), so a span-level restriction does not escape selection bias — the confinement is a stimulus property. H-W17a `T_L0_ren − T_L1_ren ≥ +12.11` over the 131 spans (`--strict-anchor`); H-W17b the 20-vs-29 comparison on five frozen properties, ≥ 2 of 5 at q < 0.05 → `corpus-bounded`. Expectation recorded: the post-hoc +21.48 (ratio 0.584) should move up once the 14 % diluting spans are gone — the opposite of the H-W16b prior. n = 20, the smallest denominator in any W result. |
| 2026-09-06 | `nla-harness` | [`2026-09-06_tier-source-results`](nla-harness/2026-09-06_tier-source-results.md) | **H-W15 ✓ quantity not position · H-W16a ✓ structure is worth ~nothing · H-W16b's verdict is an artifact of the harness's own fallback, and it flattered the stated prediction.** Job 379819 (45:24, 49/49, SELF −0.0096 PASS). `T_L0_all` **+45.71** (fourth reproduction), `T_L2_all` **+45.05** — L2 recovers **98.6 %** (gap +0.66 [+0.26, +1.11]); `T_L0_sub` +30.90 vs `T_L1_sub` +22.20, gap **+8.70** [+5.14, +12.69], does not clear. H-W15: reverse ladder R − P = +1.10 / +0.98 / +2.40, all CIs containing 0. **Defect #7:** `tier_anchor()` fell back to the true term, adding spans that cannot express the contrast (14 % null in the 20 anchored items, 100 % null in the other 29); restricted to the 20 items the gap is **+21.48** (ratio 0.584) — post-hoc, so `W16-NEITHER-REMOVAL-SUFFICES` is recorded as fired but **not evidence about meaning**. Raises H-W17 (clean re-run) and H-W18 (is the L2/L3 route invisible at identifier spans?). |
| 2026-09-06 | `nla-harness` | [`2026-09-06_tier-source-prereg`](nla-harness/2026-09-06_tier-source-prereg.md) | **PRE-REGISTRATION H-W15 / H-W16: what the 56 % item component IS.** Cross-tier patching from L1 (nonsense names, original flow) and L2 (true names, flattened) at the same L1b spans. Coverage measured first: L2 **375/375**; L1 **190/375 (50.7 %)** — 131 via `meta.rename_map` + 59 unrenamed — so L1 runs on a 190-position subset with a matched `T_L0_sub`. H-W16a `T_L0_all − T_L2_all ≥ +12.11` → structure matters; H-W16b `T_L0_sub − T_L1_sub < +12.11` → nonsense repairs as well as truth; H-W15 reverse ladder `R_k` vs forward `P_k`, all |R − P| < +12.11 with CIs containing 0 → quantity, not position. SELF re-asserted with exit 3. Expectation: H-W16b fails its bar (only 9.1 % of the effect attaches to "clean"). |
| 2026-09-06 | `nla-harness` | [`2026-09-06_dose-and-tier-results`](nla-harness/2026-09-06_dose-and-tier-results.md) | **`W13-ACCUMULATIVE-TIER-GENERIC` — the effect accumulates across spans and is mostly item identity; the harness passes an exact identity check.** Job 379075 (44:28, 49/49). **SELF −0.0096 nats** (exactly 0.000 on 21/49) → PASS. `P_1` **+10.29**, `P_2` +17.95, `P_4` +28.19, `P_all` **+45.71** (third reproduction); `F_1` +1.81, `F_all` +14.27, `F_L1b_all` +10.10. `P_1/P_all` = **0.225** [0.194, 0.257] vs the 0.50 bar → H-W13a ✗ not redundant, H-W13b ✓ accumulative with diminishing returns (per-span yield 10.29 → 5.98). `F_all − F_L1b_all` = **+4.16** [+1.37, +7.30] → H-W14 ✗ the tier is mostly generic. Additive decomposition of +45.71: generic 22.1 %, clean 9.1 %, **this item 56.2 %**, correct span 12.6 %. Cluster: 44 min compute after ~19 h queue (median wait 110 min vs 8 min run). Raises H-W15, H-W16. |
| 2026-09-05 | `nla-harness` | [`2026-09-05_dose-and-tier-prereg`](nla-harness/2026-09-05_dose-and-tier-prereg.md) | **PRE-REGISTRATION H-W13 / H-W14: dose curve over patched spans, tier contrast, and a SELF identity check.** Separates *redundant* (`P_1 ≥ 0.50 × P_all`) from *accumulative* (`P_1 < 0.50 × P_all`, monotone over k ∈ {1, 2, 4, all}) with matched foreign controls `F_k`, so `P_k − F_k` isolates content; H-W14 `F_all − F_L1b_all ≥ +12.11`. Adds the **SELF identity assertion** (`|ΔG_SELF| ≤ 1.0` nat; violation refuses the run). 375 spans / 49 items, direction-only `PositionReplacer`, seeds from `crc32(snippet_id#k)`, bootstrap 10,000, seed 20260724; generations only for noop, `P_1`, `F_L1b_all`. |
| 2026-09-05 | `nla-harness` | [`2026-09-05_null-battery-results`](nla-harness/2026-09-05_null-battery-results.md) | **`W9-ITEM-SPECIFIC`: the clean-state effect is item-specific but NOT span-specific — and that split is the finding.** 49 items / 375 spans / 0 errors / 1:06:52. `P_patch` **+45.71** [+40.89, +50.78]; a **foreign item's** clean span **+15.18** (gap **+30.53** [+25.75, +35.79], winning on **49 of 49 items**), which **discharges H-W7's outstanding caveat** — the causal-fidelity result may now be called item-specific. `N_random` **−365.59**, accuracy 0.714 → **0.306**, parse 0.857 → 0.490, positive on just 5/49 and as low as −1,847: the family's only veto trip, and proof that **the operator alone destroys, so every positive effect here comes from content**. **But H-W12 fails at the frozen bar** — a **sibling span of the same item** retains **87.4 %** of the effect (gap **+5.77** [+3.37, +8.43] against +12.11, winning on only 36/49), and all 49 items had more than one locatable span so it is not a denominator artifact. **Stage 0 measured the same round trip as sharply span-specific *representationally* — +0.6807 matched against +0.0525 for a sibling span — and causally that specificity nearly vanishes.** The residual at each identifier position encodes *which identifier it is*, while the causal work those positions do for the answer is carried by information **shared across the item's spans**: the patch is not restoring "what this identifier means" but something item-level, plausibly *"this is the un-obfuscated program"*, present at every span and largely interchangeable between them. Two instruments, same positions, opposite granularity — E7's premise arriving inside a single instrument, and diagnostic rather than a problem. **Corrects stage 1's own framing:** W1 "matching random +19.60" was compared against an **additive** arm at α = 0.149, not a like-for-like replacement one, which is −365.59. **Incidental but banked:** `P_patch` returns **+45.71** here and **+45.71** in H-W7 — different job, different node — so teacher-forced `G_sum` is reproducible to the reported precision, unlike the 0.85–0.90 greedy-generation floor this thread has documented since 2026-08-29. |
| 2026-09-05 | `nla-harness` | [`2026-09-05_nla-fidelity-results`](nla-harness/2026-09-05_nla-fidelity-results.md) | **NLA causal fidelity = 0.983 [0.967, 0.999], and the first intervention in this programme to beat prompting.** `C3pure` — the clean activation described in English and reconstructed — **+44.94** [+40.25, +49.90] = **40.3 %** of the 49-item prompt swap, against `P_patch` **+45.71** written directly: **turning an activation into English and back costs ~1.7 % of its causal effect** (0.77 of 45.71 nats, CI just excluding zero). C3pure's CI lower bound **+40.25** clears prompting (+27.66) outright, which no steering arm since Paper 2 has managed. `C1r` **+9.30** — the same generator and operator fed a description of the *obfuscated* state — shows the effect is **content, not operator**. The released pair is scored on `MSE = 2(1−cos)`, i.e. **reconstruction** fidelity; **causal** fidelity is a different question (a direction can return at cos 0.9 and do something else to behaviour) and is not reported anywhere in the NLA literature. **Three caveats filed as prominently as the result:** (1) 98.3 % causal fidelity and stage 0's raw cosine **0.9877** are **the same fact viewed twice** — `PositionReplacer` writes `‖h‖·unit(v)`, so the arms differ by ~9° and near-lossless transfer is what that angle predicts; never to be presented as independent confirmations; (2) the foreign-clean null was still missing at the time of writing, so "item-specific" outran the evidence (**since discharged**); (3) **n = 49 on the easier subset** — 11 items have no locatable span, baseline accuracy **0.714** vs 0.633, swap unit **+111.57** vs +121.13 — nothing directly comparable to stage 1's 60-item numbers. **The dissociation is the paper-relevant part:** a **41 %-of-swap** likelihood shift toward the clean reply moves accuracy 0.714 → **0.694**. Restoring the clean identifier representations, 98 % faithfully, at every locatable span, **does not restore correctness** — partly the 3-of-49 flippable ceiling, but the direction says the adversarial trap's damage is **not confined to identifier positions**, consistent with L3's `decoy_overlap` result and with Papers 2–3 treating L1b as semantic displacement. |
| 2026-09-05 | `nla-harness` | [`2026-09-05_null-battery-prereg`](nla-harness/2026-09-05_null-battery-prereg.md) | **PRE-REGISTRATION: three nulls in one job, because H-W7's decisive control was missing.** H-W9 (a foreign item's clean span), H-W12 (a sibling span of the same item), H-W6 (a random vector) — each as `P_patch − null ≥ +12.11` nats with a CI excluding 0, all at the same 375 locatable positions, all **direction-only** so every arm delivers the same magnitude and they differ in **direction alone**. Two design points that were load-bearing: the clean-state pool is built in a **first pass over every item**, so the foreign arm can draw from items processed later as well as earlier — drawing only from already-seen items would make it depend on corpus order, a silent and seed-invisible dependence; and the **operator null is evaluated first**, because if content does not matter at all then item- and span-specificity are not askable questions. **Recorded in advance that an H-W9 failure was a live possibility** — residual streams are anisotropic, so a foreign clean activation is not obviously a different direction from the matched one — **and that it would be the correct and publishable result**, saying the identifier positions accept a clean-code-shaped direction and the trap's damage is at that granularity. |
| 2026-09-05 | `nla-harness` | [`2026-09-05_hw4-not-cheap`](nla-harness/2026-09-05_hw4-not-cheap.md) | **Correction: H-W4 is a GPU re-extraction, not the cheap re-analysis it was filed as.** Stage 0 raised it as "re-express the banked `rt_cos` corpus as centred cosine — **no GPU where the vectors were persisted**", and the qualifier was carrying the entire claim. Checked: **not one** banked read corpus (`n13/read_instability`, `n13/act_norm`, `n10b/benign_reads_{neutral,security}`, `n11/{coupling,deception}_reads`) stores an activation vector. `rec` can be recomputed from the banked read text with the AR alone, but `h` must be **re-extracted from the subject model for ~14,600 rows**, so H-W4 must be scheduled and justified as a GPU job. **The general pattern is the part worth keeping:** this project systematically **persists derived scalars and discards the vectors they were derived from** — B5 ran with no per-case rows at all and its six cells can never be re-analysed from disk; W stage 0 shipped dropping `h`/`rec` on the explicit claim that "every downstream question is answered by the cosines", false within the same day and fixed only because the anisotropy question arrived before the vectors were gone; and now the read corpora. The cost is always invisible at the time and always identical: **a metric-definition question that should cost minutes costs GPU-hours** — and after 2026-09-03, when four wrong headlines in one day all came from measurement definitions, that is precisely the kind of question that arrives *after* the run. `/work` has 105 TB free; the storage argument does not survive contact with the arithmetic. |
| 2026-09-04 | `nla-harness` | [`2026-09-04_nla-fidelity-prereg`](nla-harness/2026-09-04_nla-fidelity-prereg.md) | **PRE-REGISTRATION: how much of an activation's CAUSAL power survives the round trip through language?** `C3pure / P_patch`, with **both ceilings direction-only** — `PositionReplacer` writes `‖h‖·unit(v)` and the AR is trained on `2(1−cos)` so its output norm carries no information; a full-magnitude patch would beat it on a dimension the AR cannot even represent, and the ratio would measure the handicap rather than the fidelity. Ratio CI resamples **items** and recomputes **both** means on the same resample, rather than dividing two independently bootstrapped means, which would discard the pairing. **Also diagnoses stage 1's 21 % C3 fallback as stimulus-level, not a code defect:** of 476 annotated L1b identifier spans, **375 (78.8 %) are locatable**, **88 (18.5 %) map to `?unpairedN` sentinels — adversarial renaming did not only *rename* identifiers, it *introduced* them, so there is no clean state to patch** — and 13 (2.7 %) are language builtins or one-character loop variables that were never renamed. **This reaches beyond this experiment: any measure that assumes a decoy↔true correspondence at annotated spans, E1's semantic-capture diff included, is undefined on roughly a fifth of them.** Same class of stimulus-level obstacle as T's alignment failure, and found the same way — by asking why a denominator shrank instead of accepting the number it produced. |
| 2026-09-04 | `nla-harness` | [`2026-09-04_writeback-results`](nla-harness/2026-09-04_writeback-results.md) | **`W-GENERIC-PERTURBATION`: the NLA's own protocol matches a random vector — and its channel's ceiling is twice what the edit reaches.** 60/60 items, 1,480 positions across 476 spans, **198 editable (41.6 %)**, 0 errors, 1:43:09. W1 **+19.47** [+16.51, +22.51] against the banked random vector **at these very positions, +19.60** — the protocol lands on the generic benchmark to within **0.13 nats**, and below prompting (+27.53), which nothing in this programme has ever beaten. **C1 = +8.84 is below support**, so W1 is not round-trip artifact and the family is interpretable. **A real content-specific component exists and is sub-threshold:** W1 beats the **foreign** edit — same operator, same positions, same edit shape, wrong content — by **+8.79** [+6.35, +11.41] on 47/60, and the W1−C1 gap **scales with the number of editable spans** (ρ = **+0.402**). **The headline is the arm nobody was watching: C3 = +37.91 nats, 31.3 % of a full prompt swap, nearly double the edited protocol and above prompting** — and **understated**, since 101 of 476 spans fell back to the unedited vector. Every prior result in this programme was consistent with "this channel cannot deliver anything", and Phase 0 row 4 and R2 both leaned on that: **it can.** The failure is now located precisely — not the site, not the operator, not delivery, but **the edited text**. **H-W2′ VOID (defect #6):** all 60 cosines null, ‖Δh‖ **exactly 0**, because a forward hook on layer 32's output cannot change layer 32's output at any other position in the same forward pass; the write propagates only to layers ≥ 33. The 09-04 correction replaced a **trivially-1** statistic with a **trivially-0** one — same defect class, opposite direction, same hypothesis — because I reasoned about which *position* to read and never about which *layer*. |
| 2026-09-04 | `nla-harness` | [`2026-09-04_support-floor-too-low`](nla-harness/2026-09-04_support-floor-too-low.md) | **Filed MID-RUN at 11 of 60 items, before any control mean or H-W2′ statistic existed: the frozen +12.11 support threshold sits BELOW the banked random-noise benchmark.** Re-expressing the banked R2 arms exactly in `G_sum` units — same items, same clean traces, per-item differences of log-prob sums, no approximation and no new compute — gives the whole prompt swap **+121.13**, prompting **+27.53**, **`R_random @ id_spans` +19.60**, `V1_gloss @ id_spans` +5.00, `V5_replace @ last_prompt` +0.11. **A random vector at the identifier spans clears the frozen support threshold on its own**, because "10 % of mean G_sum" was carried over from R2's *per-token* `SUPPORT_FRAC` — where the generic arms sat at 18–23 % — and lost its relation to the benchmark in the new unit. **The rule is NOT changed**; the verdict is reported exactly as it fires. What is added is an interpretive floor that makes a positive **harder**: a `W-STEERS` verdict that does not also beat **+19.60** and **+27.53** is written up as generic perturbation. Also records the design gap this exposes — stage 1 has **no operator-level null** — and **pre-registers H-W6 for the follow-up rather than adding an arm to a running experiment**, the forking path this project has avoided six times. |
| 2026-09-04 | `nla-harness` | [`2026-09-04_stage2-vacuous-correction`](nla-harness/2026-09-04_stage2-vacuous-correction.md) | **Correction filed BEFORE the run: H-W2 as pre-registered is vacuous.** The stage-1 operator *replaces* `h` at layer 32 and the AV/AR pair *reads* layer 32 — so "re-verbalize the written position and check the read moved toward the edit" returns what was just written, true by construction at cos ≈ 1. It measures the arithmetic of the assignment, not the model. **H-W2′** replaces it: with the write applied, read layer 32 at **`last_prompt`** and test `cos(Δh, Δedit) > 0` on > 50 % of items with a CI excluding 0.50, with foreign-edit and no-edit controls — both sides differences, so centred by construction and immune to the anisotropy that made raw cosine near-degenerate in stage 0. **This was the fifth measurement-definition defect in the family's short life** (R's ratio denominator, S's ungated veto, T's unreachable threshold, G's normalisation, now this) **and the first caught before the run rather than by a control afterwards**, which is the only reason it cost nothing. It also mattered structurally: a vacuous H-W2 would have guaranteed "registers" in every case, making the decision table's most important row — *null H-W1 × registering H-W2 = readout ≠ mechanism* — **unreachable**. |
| 2026-09-04 | `nla-harness` | [`2026-09-04_cycle-gate-results`](nla-harness/2026-09-04_cycle-gate-results.md) | **`NLA-LIVE` — the NLA round trip is span-specific, and raw cosine was hiding it.** 60 items / **476 spans** / 0 skipped / CJK **0.000** / 0 tag-less reads / 25 min on one H200. Mean-centred cosine: **matched +0.6807** [+0.6696, +0.6916] vs **within-item +0.0525** [+0.0420, +0.0639] vs **cross-item −0.0011** [−0.0100, +0.0077]. A read's reconstruction sits at **+0.68 with its own span and +0.05 with a different span in the same snippet** — the round trip is not merely encoding "this snippet", so a per-span edit has a real referent and **stage 1 is licensed**. Raw cosine agrees (+0.9877 / +0.9669 / +0.9646), so both metrics fire and there is no `W0-UNRESOLVED`. **The anisotropy check went OPPOSITE to the worry that motivated it:** H-W0c was frozen expecting centring might dissolve a separation raw cosine had manufactured; instead raw cosine was **compressing a 0.68 effect into 0.02 of range**. The evidence that centring is correct rather than flattering is the null itself — **cross-item centred cosine = −0.0011 with a CI containing zero**, exactly what an uninformative comparison should give. **This bears on every banked `rt_cos` in the project** (`NLACritic.score` L2-normalises but never centres), so real differences across several finished analyses may be far larger than they looked → **H-W4**, no GPU where vectors were persisted. **H-W3 (descriptive, no gate):** reads mention the decoy field **0.489** [0.430, 0.548] vs the true field **0.342** [0.262, 0.426]; the post-hoc paired split is decoy-only **0.300** vs true-only **0.153**, recorded as a lead because a paired test was not pre-specified. First use of the Gemma AV anywhere in the project — it injects correctly at `injection_scale 80000`. The verbalizer describes these positions in *surface* terms and reaches the identifier mainly by quoting it, which is what stage 1 now tests. |
| 2026-09-04 | `nla-harness` | [`2026-09-04_cycle-gate-amendment`](nla-harness/2026-09-04_cycle-gate-amendment.md) | **Two defects at smoke, and an anisotropy rule frozen before the full data.** Job **376102 FAILED in 40 s**: `ModuleNotFoundError: No module named 'sglang'` — installed in **none** of the three juno envs, it did not survive the cluster migration, and the migration table never listed it. Fixed by **removing** the dependency rather than installing it: `local_av.py` runs the AV in-process, reusing the vendored `NLAClient._build_embeds` injection path **verbatim** and replacing only the HTTP POST — reimplementing injection would be a silent correctness bug exactly where the vendored code warns about it. Job **376947**'s smoke then passed (**`NLA-LIVE`, CJK 0.000** — the Gemma AV injects correctly on first use anywhere) and **was cancelled at 4:13 anyway**, for three defects: **(1)** `MAX_NEW_READ` was **96** under a code comment falsely asserting it matched `capture_core.MAX_NEW_READ` (**180**), so **22 of 22** reads were cut off mid-sentence before their closing tag — caught *only* because an `n_no_tags` counter had been added for tidiness, where the vendored client merely prints a warning that scrolls past unread in a 400-read run; **(2)** the `h`/`rec` vectors were being discarded on the false claim that "every downstream question is answered by the cosines", which would have made any re-analysis cost a second GPU run; **(3)** every cosine sat near **0.97** — residual-stream anisotropy — so the frozen gate could fire on a round trip carrying almost no item information, which is precisely what stage 0 exists to rule out. **H-W0c** therefore adds a mean-centred recomputation as a **secondary that cannot override the primary**, with disagreement forcing **`W0-UNRESOLVED`** and no stage 1. That clause exists because 2026-09-03 produced four verdicts that were artifacts of a measurement definition, every one caught by a control rather than by the result looking wrong. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_nla-writeback-prereg`](nla-harness/2026-09-03_nla-writeback-prereg.md) | **PRE-REGISTRATION: the NLA's own protocol has never been executed in this project.** Every banked "NLA" steering arm fed the AR a **template string** (`"code whose identifiers are about X"`), not a verbalized read — `steer_run.py:480` says so outright, deferring the AV-read variant as "a different question". So the headline result **`V1 ≡ V3` with CI [0, 0] was close to guaranteed by construction**: AR(template with true ids) − AR(template with decoy ids) *is* a mean lexical-contrast direction, which is exactly what V3 is. That bounds the **template method**; it never tested the paper's protocol, which is read → minimally edit the read → reconstruct → write back. Three design corrections follow from the checkpoint metadata rather than from taste: the AR emits a **whole state at one position**, so the native operation is **replace**, not add; it is trained on `MSE = 2(1−cos)`, so its **norm carries no information** and norm-matched replacement removes **α** from the design entirely; and R2 says the only positions with a positive CI were **identifier spans**, while `V5_replace` — the exact clean state at `last_prompt`, all 33 layers — moves the clean half by **+0.0014 of G**. Gated on a stage-0 cycle-consistency test that can end the family for one job. Readout **`G_sum` on 60/60 items**, so the 6/60 flippable gate does not bind; support threshold **+12.11 nats** (10 % of mean G_sum = 121.127); three controls — round-trip null, foreign edit, and the NLA round trip of the *clean* state as the channel's own ceiling — plus the R2 behavioural veto whose absence produced the `S-LIVE` artifact. **The 2×2 decision table is the point:** null H-W1 × registering H-W2 = **readout ≠ mechanism**, the Phase-1b thesis measured rather than inferred; null × not-registering = **UNINFORMATIVE**, never a null. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_nla-and-coverage-results`](nla-harness/2026-09-03_nla-and-coverage-results.md) | **V1 ties V3 EXACTLY at every α, coverage changes nothing, and there is no regime where these writes help.** 1,320 rows across two jobs, 0 errors. **The pre-registered gate:** V1 − V3 = **0** at α 0.25, 1.0 and 4.0, with *symmetric* discordant pairs (1/1, 2/2, 3/3) and McNemar **p = 1.00** throughout. **N-0 ✗ — the NLA does no causal work a plain contrastive difference does not already do**, and B4's refutation **replicates on a second architecture** (different AR, layer 32 vs 20, width 3840 vs 3584). This is the **first permitted-host evidence** on the NLA's causal claim and it agrees with the Qwen result the model constraint put permanently out of reach. Sharpest number: **V1 at the primary α has CI [0, 0]** — 0 recovered, 0 damaged, *no effect on any individual item*, while rewriting **96.7%** of the reply text. **`A_antipodal` is the diagnostic row:** the *negation* of V1, built to be actively wrong, is exactly as harmless (+0.0000) — when the intended intervention, its opposite, a foreign vector and a prompt change are mutually indistinguishable, the result is about **the site**, not the directions. V2 ran on the full 60 for the first time (matched denominators at last), and swapping the whole gloss does what swapping one word does: nothing. **Coverage (C-0 ✗):** at equal injected budget, moving from 1 token to 44 identifier tokens changes **88–98% of replies** and leaves correctness at **exactly** baseline, churn **0/60** for V1 and V3 — the single-token choice was not the limitation. **C-2 FLAGGED at α=8**, where parse collapses to **0.283** (P0.2's failure mode reproduced) and movement tracks magnitude, not placement. **The finding that survives:** across N, C and the KV-bypass run — 1 layer and 33, 1 token and 44, NLA/contrastive/oracle/antipodal/foreign/random, exact clean-state replacement, and prompting — **not one arm ever recovered more than 2 of 60 items.** Too weak and the writes do nothing; strong enough and they only destroy. **The site controls wording, not correctness** — independent support for Phase 1b's readout hypothesis. **The sensitivity gap is closed:** the α=8 arm detects a **−0.4333** shift, so the pipeline demonstrably has power and the small-α nulls are bounded negatives, not a dead instrument. Mechanistic tie-back: at α=8 **V1 damages 3× harder than V3** (−0.3833 vs −0.1167) despite being the informed direction — exactly what a vector at cos = 0.0446 to the model's own contrast should do when pushed off-manifold. Bounds: one host, one layer, n=60, one tier; Gemma's L1b penalty is only 10 points vs the papers' 21.25% collapse, so this may be a weaker trap than the phenomenon. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_coverage-delivery-ceiling`](nla-harness/2026-09-03_coverage-delivery-ceiling.md) | **Identifier tokens can deliver at most ~49% of a direct edit, at any magnitude — the pre-registered matching rule REFUSED twice, and the refusal is the result.** Coverage arms must be matched on delivered perturbation (P0.2 destroyed generation by widening at unchanged α), so r(α) was measured at the last prompt token, the position every setting shares. `id_spans` (median **45** tokens, 18.4% of prompt) reached only 0.482 against a 1.268 reference at α=4 — refused — so the grid went to **α=64**. It **converged**: 0.482 → 0.552 → 0.589 → 0.609 → **0.618**, the final doubling buying **1.5%**. **A ceiling, not a grid limit: 48.8% of the reference.** Mechanically, a layer-32 write on identifier tokens reaches the answer position **only through attention in layers 33–47**, so pushing 45 source states arbitrarily hard still moves the readout by at most half what a direct edit does — the same geometry as the KV bypass, with influence capped by the attention mass the read position grants those tokens above. **An accidental vindication of a choice nobody validated:** the banked single-token site, inherited from the original NLA paper and hardcoded, turns out to be the *strongest available* site for moving the answer. It is not the right site for the *hypothesis* (adversarial renaming is about identifiers), but the banked nulls were not weak for want of delivery. Bounds: one host/layer, n=12 for the curves; that there **is** an asymptote is robust across 13 grid points, its exact level is an estimate. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_flippable-denominator`](nla-harness/2026-09-03_flippable-denominator.md) | **The rescue ceiling was the noise floor.** Only 6 of 60 items are L0-correct and L1b-wrong on Gemma, so a perfect rescue is +0.10 — exactly the day's support threshold, on a ±0.075 floor. Every accuracy-level null today was underpowered by construction. V1 rescued 0/6 in seven arms; antipodal and prompting hit 2/6, so 2/6 is churn. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_site-alive-prereg`](nla-harness/2026-09-03_site-alive-prereg.md) | **Pre-registration, frozen:** continuous clean-trace LLR metric M (R: re-score banked arms), optimised-vector site test with held-out splits (S: LIVE/DEAD decides 'directions wrong' vs 'site dead'), and a layer × position causal patching map (T). Decision table fixed; S/T do not run if M is insensitive. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_site-alive-results`](nla-harness/2026-09-03_site-alive-results.md) | **R/S/T results — the metric is confounded by damage.** M is well-formed (sanity 60/60, +0.905 nats/tok; floor 0.010) but monotone in degradation: the clean half moves the **wrong way in 21 of 28 arms**, and `R-1`'s supporting arm (+1.486) is Δclean −0.485 against Δcorrupt −1.758, with matched random (+1.189) overlapping. `S-LIVE` is a rule artifact — held-out ΔM up to +20.8 while L0 specificity reaches **−48.8** and greedy accuracy falls **0.767 → 0.000**; 1 flippable item rescued across 5 splits. T refused at its smoke gate (0.784 < 0.80 → T-VOID). Prompting beats every vector on the non-damage half. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_clean-half-prereg`](nla-harness/2026-09-03_clean-half-prereg.md) | **R2 rule frozen before scoring, with a non-blindness disclosure.** Clean half only (M2), unit G = the gain from swapping in the un-obfuscated prompt, mover = ≥ 10 % of G with CI excluding 0, random separation by **non-overlapping CIs**, plus a free behavioural veto and a GPU specificity veto **staged** to run only if an arm clears. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_clean-half-results`](nla-harness/2026-09-03_clean-half-results.md) | **`R2-GENERIC` — with damage controlled, belief writes point the wrong way.** G = **+0.3071** nats/tok, positive on 100 % of items. The only movers are `P_prompt` (**+0.0694, 23 % of G**) and `R_random@0.149 id_spans` (**+0.0555, 18 %**); **every** belief arm at `last_prompt` is negative and worsens with α (V1@4.0 −43 % of G). R's `R-1` arm inverts to **−158 % of G** with accuracy 0.25 vs 0.633. Random beats the best belief vector **3.7×** at matched energy. Pre-stated predictions held; **0 GPU-hours**. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_patch-alignment-impossible`](nla-harness/2026-09-03_patch-alignment-impossible.md) | **T's smoke-gate near-miss was the corpus, not the code.** Only **27.5 %** of 1,289 `replace` blocks are token-length-matched, **88.9 %** of replace tokens sit in mismatched blocks, and **0/60** items admit a complete 1:1 L0↔L1b map — median **19.4 %** of each L1b prompt is unpatchable, and L1b runs a median **+27** tokens longer. `all @ L0` cannot reach 1.0 at any effort, so **H-T2 ✗** and T is not executable on the banked corpus; the gate stays at 0.80. Root cause is stimulus-level (**60/328** renames length-matched; 180 originals are single tokens). The harness is proven correct by its own vacuous cells. Flags an uncontrolled **token-count confound** in any length-sensitive L0-vs-L1b measure, Instrument 1's attention fractions included. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_length-confound-prereg`](nla-harness/2026-09-03_length-confound-prereg.md) | **Frozen before running:** does adversarial renaming's +27-token inflation do work independent of the renaming? Readout G is length-invariant by construction; `L-CONFOUND` would justify the length-matched corpus fork on its own merits, `L-CLEAN` drops it. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_length-confound-results`](nla-harness/2026-09-03_length-confound-results.md) | **`L-CLEAN` — inflation does no independent work (ρ_partial = −0.159 [−0.420, +0.123], q = 0.228), retiring the corpus fork.** And **H-L1 ✗ wrong in sign**: pre-registered ρ ≈ +0.3…+0.5, measured **−0.166** — **the amount of renaming does not predict what obfuscation costs the model**, so L1b is not a dosage effect but a semantic trap. H-T5 retired on arithmetic (~12 % of identifier tokens alignable → null by construction): **T ends on this corpus.** |
| 2026-09-03 | `nla-harness` | [`2026-09-03_g-variance-prereg`](nla-harness/2026-09-03_g-variance-prereg.md) | **Frozen before running:** six cheap on-disk predictors of the per-item obfuscation cost, each partialled on clean-reply length, BH-FDR across the six, power stated in advance. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_g-variance-results`](nla-harness/2026-09-03_g-variance-results.md) | **`L3-SIGNAL` — one real predictor, one artifact, and a same-day correction.** `decoy_overlap` (**ρ_partial −0.352**, q 0.036, pre-registered direction) **replicates on the un-normalised total cost** (−0.382, q 0.006): decoys sharing terms with the truth cost less — semantic displacement as Papers 2–3 describe it. `decoy_verbosity` withdrawn as an artifact. **Root cause: ρ(G, 1/reply-length) = +0.889**, so the per-token normalisation injects item-level variance. **This corrects the L entry:** H-L1 goes from −0.166 to **+0.378 [+0.104, +0.606]** on the total, inside its original pre-registration — **renaming is dose-dependent after all**; `L-CLEAN` and H-T3's retirement survive on the unchanged H-L2. **R2 is unaffected.** |
| 2026-09-03 | `nla-harness` | [`2026-09-03_gemma-ar-alignment`](nla-harness/2026-09-03_gemma-ar-alignment.md) | **The Gemma AR is valid — and it points somewhere the activation contrast does not.** The released pair had been on disk for days but was never wired in: `steer_run.py` resolved a **hardcoded path** to the **Qwen** reconstructor (`Qwen2ForCausalLM`, 3584-wide), so any AR-derived run on Gemma would have driven a 3584-wide vector into a 3840-wide stream — it raises here, but **on a same-width host it would have run silently**. AR checkpoints are now per-host and the runner cross-checks the checkpoint's own `nla_meta.yaml` layer against `--layer`. Constants are not interchangeable: Gemma is layer 32/48, d 3840, mse_scale 61.968, **AV injection_scale 80000 against Qwen's 150** — the ~500× gap being the activation-norm gap, and exactly the constant the vendored code warns yields CJK gibberish when wrong. **Four checks, all PASS:** dimension 3840=3840 · scale, AR norm **56,806** vs real L32 activation norm **74,226**, ratio **0.765** · separation, 0/20 identical glosses (had the AR mapped both glosses to one vector, V1 would be identically zero and **every V1 null in the programme's history would be an instrument artefact**) · alignment, reported and never gating. **The finding: cos(V1, V3) = 0.0446** — z = **12.4** above zero (chance sd 1/√3840 = 0.0161, n = 20) so not noise, but **0.20% shared variance**, per-item range −5.2 to +5.7 chance-sd. **The NLA-derived direction and the model's own clean-minus-decoy contrast point independent ways.** This changes how the pending V1 result must be read: a V1 null now supports *"the AR is not addressing this space"* by **independent measurement** rather than inference from the null — a different claim from *"there is no item-level belief to edit"*, and **telling those apart is exactly what B4 could never do**. Low alignment does not invalidate V1; what it rules out is the never-stated assumption that V1 and V3 are aimed at the same axis. Bounds: n = 20, one host/layer/gloss construction; the AV was not exercised, so G0 stage B remains unrun and this is not a substitute. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_nla-and-coverage-prereg`](nla-harness/2026-09-03_nla-and-coverage-prereg.md) | **PRE-REGISTRATION of the two things the KV-bypass work did not touch.** (1) **NLA steering has never run on a permitted host** — every arm that day was V3/V4/V5/random, pure activation arithmetic, because the AR on disk is Qwen; the banked B4 refutation cannot be re-run under the model constraint, so **the NLA's causal claim rests on zero permitted evidence**. Experiment N reproduces B4's gate verbatim (V1 must beat V3, paired, at α=1) on Gemma L32. (2) **Every steering result ever produced here wrote at ONE token** — `last_prompt`, 0.4% of the prompt — while the stimuli have carried 11 annotated `identifier_spans` per item since Paper 2 and **not one run used them**, despite the adversarial-rename hypothesis being specifically about identifiers. Experiment C compares 1 token against a median 44 (18.4% of prompt) at matched delivered perturbation. **Adds the sensitivity clause the KV-bypass rule lacked**: if `R_random` moves the metric while V1/V3 do not, the null is reported as refuted-not-untestable — the fix belongs in a rule written in advance, not in the reading of a result already seen. UNINFORMATIVE is declared the *expected* outcome for N in advance, and both outcomes of every rule are declared publishable. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_kv-bypass-results`](nla-harness/2026-09-03_kv-bypass-results.md) | **UNINFORMATIVE by the frozen rule — and the reason is the finding.** Single-layer V3 does not move the task (Δ = +0.0000), so there was **no effect for multi-layer steering to rescue** and the registered contrast has nothing to measure; H-C1 **not supported**, H-C0 **not established**. The short-circuit fired as designed. **But the intervention is delivered and is not inert:** reply identity vs the single-layer reference is **0.000 across all 60 items** in every multi-layer arm — completely different text — and `R_random` at α = 1.0 damages by **−0.15** (10 damaged, 1 recovered). Meanwhile multi-V3 at matched energy churns **4 of 60** (2 recovered, 2 damaged, net 0), a 0.067 disagreement against a **0.10–0.15** reproducibility floor: *the steered run agrees with the baseline more closely than two baseline runs agree with each other, while rewriting every word.* **V5 exact state replacement** — `h_l := h_clean,l` at all 33 layers, the position's state IS the clean run's — moves **−0.0167 [−0.0833, +0.0333]**. H-C2 not flagged; parse 0.783–0.850, no P0.2-style collapse, so the empirical energy match held. **Where the rule was too conservative, flagged not overridden:** `R_random`'s −0.15 proves the setup detects a 15-point effect, so H-C1 was arguably refuted rather than untestable — but the frozen rule has no sensitivity clause, and rewriting a rule after seeing data is the forking path six retracted positives already taught. Any revised rule must be pre-registered before re-analysis. **Methodological finding: a rescue test needs something to rescue** — this design can only discriminate where the baseline intervention has a measurable effect, and neither Gemma nor banked Qwen provides one; future designs of this shape need an entry criterion checked before GPU time. Also: the mandated prompting baseline is **0.6333, Δ = +0.0000** — steering does not beat prompting, prompting does not beat nothing, and the only thing that moves correctness is random damage. 600 rows, 0 errors, 0 key collisions. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_alpha-sweep-saturates`](nla-harness/2026-09-03_alpha-sweep-saturates.md) | **A 16× α range delivers only a 1.63× perturbation — B4's sweep covered far less than its phrasing implies.** Calibrating the KV-bypass energy match required r(α) = ‖h_final^steered − h_final^unsteered‖ / ‖h_final^unsteered‖ at the steered position. On `gemma12b`/L32 the single-layer curve runs **0.9778 → 1.5978 across α 0.25 → 4.0**: it is already at r ≈ 0.98 at the smallest α tested, and quadrupling α twice more adds 63%. B4 is reported as *"V1 does not beat V3 anywhere across a 16× range of alpha"*; if the same compression holds on the banked host that is a **1.6× range of delivered perturbation**. **This does not refute B4 — it bounds what B4 established**, and the honest restatement carries the delivered figure rather than the coefficient. Scope is the weak point and is stated as such: measured on **Gemma L32, not the Qwen L20 host where B4 ran**, and under the 2026-09-02 constraint that measurement **cannot be made**, so the transfer is an inference. The mechanism that would carry it (RMSNorm on the residual path at every block) is generic to the panel, but *plausibly* is not *demonstrated*; n = 12, medians, one position, one layer — and this thread has already been burned once reading shape off n = 60. Incidental: the **empirical** match selected multi α = **0.1** against the analytic 1/√33 = **0.1741**, so the analytic rule would have injected **1.74× too much**; matched ratio 0.95; LOO coherence at L32 **0.4213**. Raises: report every α sweep in r, not α. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_kv-bypass-claim-precision`](nla-harness/2026-09-03_kv-bypass-claim-precision.md) | **Correction, filed before any result exists: the bypass is PARTIAL, and today's pre-registration overstated it.** The filed text said the intervention "never changes what the model subsequently reads" — false, and it contradicts the clause after it. Layers 21–27 are downstream of the hook and **are** cached in corrected form, so the write does reach later tokens; what it misses is layers 0–20, **21 of 28, 75% of the depth**. The gate's number was always the precise claim (max |Δ| = **0.000** across layers 0–20) — the prose gloss was what was wrong. This changes the *strength* of the mechanism, not its direction: H-C1 now predicts that closing the remaining 75% clears +0.10, not that the current instrument delivers nothing. **No decision rule changes.** Filed separately and before results so it cannot be an after-the-fact reinterpretation, and the phrase is banned from any report, abstract or caption. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_kv-bypass-v5-ceiling`](nla-harness/2026-09-03_kv-bypass-v5-ceiling.md) | **Pre-registered addition: an exact state-replacement ceiling — and the CPU test that caught it was not exact.** Every arm filed so far writes a direction derived at *unsteered* activations, so once layer 0 is edited layer 1 no longer sees the state its vector was measured at; the null outcome was therefore open to "the vectors were too weak" with nothing in the design able to answer it. V5 assigns `h_l := h_clean,l` at every layer 0..L, so the position's state **is** the clean run's wherever the KV cache is read. **My first implementation added the raw difference** on the reasoning that `h_obf + (h_clean − h_obf) = h_clean` — true for one layer, wrong past it, because layer *l+1* receives the state layer *l* already corrected and adds its own on top: measured error **0.0 / 2.63 / 5.27 / 7.90** across four layers, a ~3× overshoot at the top. A model-free CPU test is the only reason it surfaced; it would otherwise have run, produced plausible numbers, and been reported as an exact ceiling. Only an absolute assignment is idempotent w.r.t. what propagated up — re-verified at **exactly 0.0 error at every layer**. Four guards added because a silently-wrong ceiling is the hardest failure to notice: old banks **drop** V5 rather than falling back to the additive oracle; targets travel under a sentinel key so they cannot enter the additive path; `absolute + energy_matched` and `deltas + absolute` are refused; an absent V5 reports **ABSENT**, never a ceiling of zero. V5 is diagnostic and enters no verdict branch. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_kv-bypass-host-change`](nla-harness/2026-09-03_kv-bypass-host-change.md) | **Amendment: the KV-bypass test moves to Gemma-3-12B (L32 of 48); two jobs queued against Qwen were cancelled.** `steer_run.py` hardcoded `TARGET_MODEL = "Qwen/Qwen2.5-7B-Instruct"`, so the pre-registration filed hours earlier had queued a model the project is no longer permitted to run; the validation gate had already executed on Qwen before this was caught, and is recorded as Qwen-hosted rather than quietly relabelled. The port is cheap because multi-layer mode uses **V3/V4/random only** — pure activation differences, no NLA. **Four silent-failure modes were opened by the port and each now raises:** `--model qwen7b` requires an explicit acknowledgement; **the AR checkpoint on disk is the Qwen pair** (its own config says `Qwen2ForCausalLM`, `hidden_size 3584`), so AR conditions are pinned to a host and would otherwise have produced nonsense on any same-width model; `--layer` defaulted to the old constant and would have steered **layer 20 of 48** while every artifact said 32; and the vector bank's width and depth are now checked **against the live model rather than the filename**. All guards verified to exit 1 on CPU. The banked B4/B5 nulls are Qwen results and cannot be diagnosed directly on Gemma — but the general question is decidable there, since both arms run in one job and need no banked baseline. |
| 2026-09-03 | `nla-harness` | [`2026-09-03_kv-bypass-prereg`](nla-harness/2026-09-03_kv-bypass-prereg.md) | **PRE-REGISTRATION: the one-block hook leaves 75% of the depth reading the un-edited decoy, and no banked result controlled for it.** *(The entry's own text says "never changes what the model reads"; that phrasing is **withdrawn** — see [`2026-09-03_kv-bypass-claim-precision`](nla-harness/2026-09-03_kv-bypass-claim-precision.md). The entry file is append-only and keeps it.)* A write at layer 20 propagates to layers 21–27 *for that position only* — the K/V entries at layers 0–20 were computed before the hook fired. **Measured, not argued: max |Δ| across layers 0–20 at the edited position is exactly 0.000** under the banked instrument, against **682.698** for multi-layer prefill steering. Four-check gate all PASS (α = 0 byte-identical to unsteered; 21 writes for 21 hooked layers; **nothing moved at any other position**; prefill only). So every later token attends to the **un-edited decoy** through the bottom 21 of 28 layers, and "there is no belief to edit" versus "the channel cannot deliver" remain observationally equivalent for **B4, B5 and P0.2 alike**. Frozen in advance: H-C1 (≥ +0.10 with `R_random` failing to match) re-opens every write-side null; H-C0 lets the bounded causal negative stand; H-C2 attributes any movement to magnitude; plus an UNINFORMATIVE guard. Because coverage and magnitude are confounded — and **P0.2 already drove parse rate to 0.100 by widening coverage at unchanged α** — the energy match is **empirical**, computed on delivered displacement before any generation and consulting no outcome. V1/V2 excluded by construction: the AR exists at one layer only, so this tests the **channel**, not the NLA. |
| 2026-09-02 | `nla-harness` | [`2026-09-02_llama-l2-survives-repetition`](nla-harness/2026-09-02_llama-l2-survives-repetition.md) | **Llama L2 SURVIVES the repetition control — 1 of 9 cells across three hosts.** Beats a baseline of `max(length, static, repetition, combined, all-three)` by **+0.1062, p = 0.0149** on the replication draws, unchanged, because repetition explains essentially none of it. **The control is not inert, which is the diagnostic part:** repetition predicts nothing at L2 (**+0.0005 / +0.0363**) but is a strong predictor at L3 (**+0.2531 / +0.2615**), where it becomes the binding baseline and drives L3 to **−0.0845**. So it fires exactly where the Qwen precedent said it would and not at L2 — better evidence than a control that merely passes. **Full matrix: 8 of 9 cells reduce to a surface statistic** (Qwen L2 +0.0345, Gemma negative at every tier); the lone exception is llama8b/L2 at **+0.1109**. So on Llama-3.1-8B under dispatcher indirection a linear read of the residual stream predicts item-level correctness beyond reply length, static code shape, text repetition and every combination — replicated on independent draws by a rule frozen in advance, sibling tier null throughout. **The first result in this programme to reach that bar**; five earlier positives died to controls, and this one passed the specific control that killed the closest analogue. Scope stated plainly: one host of three, one tier of five, n = 58, ρ 0.31 against a 0.21 baseline, on the *weakest* host. **Beating every surface baseline is not knowing what is represented** — that gap is exactly what E3 exists to close. |
| 2026-09-02 | `nla-harness` | [`2026-09-02_llama-l2-replicated`](nla-harness/2026-09-02_llama-l2-replicated.md) | **Llama-3.1-8B L2 REPLICATES — the first effect in this programme to survive pre-registered replication and every surface control.** On draws 10–14, generated after the rule was frozen and scored without pooling the discovery set, L2 beats a strict `max(length, static, combined)` baseline by **+0.1062** against a required +0.075, **p = 0.0149**; the residual stream lands at +0.3099 against +0.3176 in discovery while the baseline it must beat *rose* to +0.2037. **The sibling control behaved**: L3 shares the relational route and stays flat in both sets (+0.0106 → **−0.0569**), so this is not a generic property of relational obfuscation or a pipeline artefact. **A defective control was fixed on the way and it changed a verdict:** the original combined-ridge baseline scores worse out-of-fold than its own best component in **6 of 9 cells** (by up to 0.118), and both positives sat on the two most degraded — under the strict baseline **Gemma L2 went +0.105 → −0.013 (artefact)** while Llama L2 held. **This is 1 cell of 9:** Gemma shows nothing anywhere, Qwen's analogous effect deflated to static complexity, and the dispatcher-span probe is *not beyond size and repetition* on all three hosts. Stated carefully: beating a baseline is not identifying what is represented, **the repetition control has not yet been run against this effect**, and it appears on the weakest of three models (L0 0.567 vs Gemma's 0.777) at n = 58 with ρ 0.31 against a 0.20 baseline. |
| 2026-09-02 | `nla-harness` | [`2026-09-02_llama-l2-prereg`](nla-harness/2026-09-02_llama-l2-prereg.md) | **PRE-REGISTRATION** of the one surviving cell. REPLICATED iff beats-strict ≥ **+0.075** with p < 0.05 on draws 10–14 only — half the discovery value, set before the draws existed. L3 declared the sibling control; a replication resting on a degenerate baseline explicitly does not count; **both outcomes declared publishable in advance**, since one cell of nine on the weakest host with a null sibling tier is at least as likely to be noise as signal. |
| 2026-09-01 | `nla-harness` | [`2026-09-01_gemma-ga-gates`](nla-harness/2026-09-01_gemma-ga-gates.md) | **Gemma port — both G-A gates PASS, and the port's decision point clears.** **Layer indexing:** fp32 residuals **1.33e-07 / 1.15e-08 / 9.39e-08** at layers 8/20/32, one position written, nothing else moved — so the `_layers()` fix put the extractor and the steerer on the same block despite Gemma-3 loading as `Gemma3ForConditionalGeneration`. **G0 stage A:** `ours=164 reference=164` tokens, **median rel err 0.00005**, max 0.01443, **0/164 over tolerance** — `<bos>` reproduces at 712,708.5 exactly. Layer index, chat template and extraction all reproduce the shipped reference on a host with a different architecture, different tokenizer and activation norms ~1000× Qwen's. **Deriving the end-of-turn token from the reference's own last row was load-bearing:** Qwen ends on `<|im_end|>` and Llama on `<|eot_id|>`, but **both Gemma references stop mid-sentence with none**, so assuming one by analogy would have given 165 tokens against 164 — a length mismatch that reads as a broken template rather than a wrong constant. Parsing the reference rather than transcribing its 639-character reply also caught two more footguns (mixed quote styles across the four files; a greedy `Var` capture swallowing a full stop). Stage B, the AV→AR round trip, is the remaining half — and is where Var(v_nrm) = 0.0302 will force the reported metric from `rt_cos` to `fve_nrm`. |
| 2026-08-31 | `nla-harness` | [`2026-08-31_span-repetition-and-n13`](nla-harness/2026-08-31_span-repetition-and-n13.md) | **Repetition explains most of the span result; the mid-reasoning region is quiet about the INPUT too; and my N13 flag was half wrong.** **(1)** The dispatcher-span signal was flagged in its own Next Steps as possibly reading repetition — it is. Five surface counts of the source text (max token freq, distinct/total tokens, duplicated lines, max line freq) predict span count at **ρ = +0.8346** alone; the residual stream reaches +0.8842 and adds only **+0.0647** over size+repetition, under the +0.10 bar. **NOT BEYOND SIZE AND REPETITION** — so "the model represents how tangled a program is" becomes *a signal largely predictable from how repetitive the text is*. The chain correctness → static complexity → repetition counts is **three deflations deep**. **(2)** Probing the mid-reply region for `n_dispatcher_spans` — a target known to be encoded at the prompt (+0.8879) — gives **+0.1773 at `reply_q50`**, the same U-shape the correctness probes found on a different target. **The deflationary reading is the right one:** activations at reply position *t* are about the reply, and the KV cache holds the prompt without re-representing it — which also explains all five correctness nulls there, more simply than anything offered earlier. **(3)** My 08-29 flag on N13 asserted a same-sample dependency from the ledger's description rather than the data. Checking: the reported 0.843/0.869 **are** `modal_correct` (same-sample), but `banked_correct` is a **separate greedy run**, so a held-out estimate always existed — **0.8400 / 0.8428**. Inflation is **0.026**, not the 0.18 seen on the greedy-draw instability measure. **N13's positive stands at ≈ 0.84.** What does fail is its corollary: hardening was said to strengthen the signal 0.843 → 0.869, but held out it moves **0.8400 → 0.8428 (+0.003)** — most of the gain lived in the label. |
| 2026-08-31 | `nla-harness` | [`2026-08-31_span-probe-and-l2-positions`](nla-harness/2026-08-31_span-probe-and-l2-positions.md) | **Dispatcher spans ARE encoded — directly measured — and L2 reads at the prompt, not at the answer.** **(1)** Probing the residual stream for `n_dispatcher_spans` itself (not correctness) gives **ρ = +0.8842 on L2, +0.3413 beyond a code-size baseline**, p = 0.005 — the mechanism entry's inference is now a direct measurement: **the model represents how many dispatcher sites a program has.** L1b (0 spans everywhere) gives the probe nothing, as a floor should. L3 fails the bar only because stacking renaming inflates code size and lifts its baseline to +0.7893; its residual ρ (+0.8723) matches L2's. **(2)** The position grid, generalised to any tier and run on L2: **`answer_line` − `last_prompt` = −0.0307** [−0.1647, +0.0978] → **POSITION DOES NOT MATTER**, point estimate *negative*. **The tiers dissociate exactly as the static account predicts:** on L1b nothing is readable at the prompt and the signal appears only once the answer is committed (0.6685 → 0.8229); on L2 it is already there at the prompt (**0.8143** vs a 0.6675 length baseline) and gains nothing from reasoning. **So what the model represents about dispatcher indirection is a property of the code as written, not a state that develops while it reasons — no hidden-state simulation on the tier where the charter expected it.** Reported as a **shallow** representation: ρ = 0.884 for a count is close to counting repeated structures, is tied to the documented r = −0.196 effect, and is **not** semantic understanding of control flow. **The mid-reasoning collapse replicates on a second tier** (`reply_q50` 0.5370, and 0.4289 at L20 — below chance), the fifth independent observation of that region being empty. |
| 2026-08-31 | `nla-harness` | [`2026-08-31_l2-mechanism-results`](nla-harness/2026-08-31_l2-mechanism-results.md) | **The replicated L2 effect is static dispatcher complexity, NOT state tracking — and the pre-registration named this outcome before the probe ran.** Against a baseline already knowing reply length and the code's static shape (`n_dispatcher_spans`, `code_chars`, `n_lines`, `has_while`, `is_javascript`), the residual stream adds **+0.0245** on L2 — bar was +0.10. Permutation p = 0.005 confirms the residual signal is real *versus chance*, but it is not incremental *versus the code's own visible structure*. **The route contrast has a complete deflationary explanation:** static features alone predict correctness at **ρ = +0.2989 on L2** and only **+0.1198 on L1b**, which has no dispatcher spans at all — so L2 simply carries an extra *static* predictor that reply length misses, and the residual stream encodes it. Control held (L1b −0.0383). **The state-tracking reading is refused; the replication itself stands** (it was about length alone) but its interpretation is now bounded. **What the result IS:** the first internal correlate in this programme of a documented behavioural finding — the dispatcher-complexity effect r = −0.196 (q = 3.1×10⁻²³) from Papers 2–3. **The model represents how tangled a program is; it does not represent how the tangle resolves.** Also corrected: L2 here is dispatcher *indirection*, not switch-based flattening — 0 of 70 items contain a `switch`. Fourth control-driven deflation of the week and the **first anticipated in advance**, so nothing had to be retracted. |
| 2026-08-31 | `nla-harness` | [`2026-08-31_l2-mechanism-prereg`](nla-harness/2026-08-31_l2-mechanism-prereg.md) | **PRE-REGISTRATION** testing whether the replicated L2 effect is more than static dispatcher complexity. Names the deflationary outcome in advance and says it would still be reportable — as an internal correlate of the r = −0.196 finding — but **must not be described as hidden-state simulation**. Puts language in the baseline deliberately (40 Python / 30 JS; N9 found 37% wrong-language reads with a 79.9%/1.0% JS/Python split). |
| 2026-08-31 | `nla-harness` | [`2026-08-31_ladder-replication-results`](nla-harness/2026-08-31_ladder-replication-results.md) | **REPLICATED — the first positive in the programme to survive a pre-registered replication.** On five independent draws scored by the frozen rule, **Δ = +0.3195** against a bar of **+0.15**, within 0.002 of the discovery's +0.3178. Per tier, beats-own-length-baseline: L0 −0.1016 · **L1 −0.4745** (mean ρ −0.0873 — nothing at any depth) · L1b **+0.0026** · **L2 +0.1253** · **L3 +0.0418**. Permutation nulls: L2 **p = 0.005**, L3 p = 0.015, L1b p = 0.010. Pooled split-half: **L2 positive on 100% of splits**, L3 on 90%. **The claim: under control-flow flattening the residual stream predicts item-level correctness better than reply length does; under renaming it does not.** Two honest qualifications: **L3 is weaker than discovery** (+0.0418 vs +0.0938) so the effect is carried by L2, not the stacked tier; and **a pre-registered secondary check was missed** — L1b was expected to be non-significant and reached p = 0.010, meaning the atom route is not representationally *empty* but **redundant with a token count** (+0.0026 over length). L1 stayed signal-free as required, so the pipeline did not change. **This gives the item-level null ledger its scope: B4, B5, N11, N13 and P0.4 were all run on L0/L1b — the clean anchor and the atom route, exactly where the residual stream is redundant with reply length. The nulls were route-specific and nobody knew, because the relational tiers had never been read.** Fits the hidden-state-simulation account from Papers 2–3 that E3 was commissioned to test. Of five attractive positives this week, three died to cheap controls and this one survived replication. |
| 2026-08-31 | `nla-harness` | [`2026-08-31_ladder-replication-prereg`](nla-harness/2026-08-31_ladder-replication-prereg.md) | **PRE-REGISTRATION** of the replication. Rule set on the **route contrast** rather than per-tier bars — Δ = relational mean − atom mean ≥ **+0.15**, half the discovery's +0.3178, because an absolute per-tier bar would have let L3's noise (+0.0938, near the +0.10 line) decide a claim about routes. Discovery draws explicitly **not pooled** into the primary. Secondaries: per-tier nulls, a requirement that L1 stay near zero, and a pooled split-half. States in advance that **both outcomes are publishable**. |
| 2026-08-31 | `nla-harness` | [`2026-08-31_ladder-relational-positive`](nla-harness/2026-08-31_ladder-relational-positive.md) | **A route-specific POSITIVE that survives its controls — the first of the programme.** Five tiers, 1,500 fresh generations under one regime, each tier against **its own** length baseline (the 08-30 retraction's lesson). Selection-free statistic (mean ρ over all 28 layers, no argmax): **clean −0.059 · atom −0.201 · relational +0.117**. Per tier, beats length by: L0 −0.0593, **L1 −0.3383**, L1b −0.0628, **L2 +0.1395**, **L3 +0.0938**. Permutation nulls on both statistics: **L2 p = 0.005 / 0.005**, **L3 p = 0.005 / 0.025**. **Two scoring errors of mine were caught first:** `beats_length` initially used the argmax over 28 layers, and the first permutation fixed the layer at that argmax — and the correction mattered, since **the max-over-28 null averages +0.17–0.18 on pure noise** and **L1b's apparent +0.0736 advantage collapses to p = 0.114** once selection is accounted for. **The finding: the residual stream carries item-level correctness information beyond reply length under RELATIONAL obfuscation (flattening, stacked) and not under ATOM-level obfuscation (renaming) or on clean code** — L1 sits at ρ = +0.0224 at *every* depth. This gives the item-level null ledger a scope: **B4, B5, N11 and P0.4 were all run on L0/L1b, the one route where there is nothing to read.** Status is deliberately **exploratory** — the threshold and grouping were fixed before the run, but design/run/score happened in one sitting and two attractive positives died to controls in the preceding 48 hours. **A pre-registered replication on independent draws is the next thing to run.** |
| 2026-08-30 | `nla-harness` | [`2026-08-30_behavioural-predictors`](nla-harness/2026-08-30_behavioural-predictors.md) | **Behavioural predictors: real, ~a third smaller than they look, and a flag on the programme's strongest banked positive.** Answer instability is computable for free from the ten unsteered draws — repeated *greedy* runs differ only through the nondeterminism floor, so no sampling is needed. Naive same-draw estimates: **AUC 0.8270 (L0) / 0.7834 (L1b)**. But instability and graded correctness come from the same draws and are mechanically entangled (y = 0.5 forces ≥ 2 distinct answers). **Split-draw control** — instability from draws 1–5, correctness from draws 6–10, sharing no generation — gives **0.6450 (L0) / 0.7368 (L1b)**: roughly a third of the L0 signal was the dependency. **The tier ordering also reverses** (held out, L1b > L0), so there is **no support for behavioural degradation under obfuscation**; only parse-failure weakens (ρ −0.5032 → −0.3089). **The flag: N13's free positive — answer instability → correctness at AUC 0.869, currently the strongest result in the programme — appears to have been estimated from the same generations that supplied its labels, and the identical dependency cost 0.18 AUC here. It should be re-estimated held out before being quoted again.** Note also that the nuisance became the instrument: the run-to-run nondeterminism characterised as a threat to validity is, measured deliberately, a better correctness predictor (0.65–0.74) than anything the residual stream adds at `last_prompt` (+0.04–0.06). |
| 2026-08-30 | `nla-harness` | [`2026-08-30_p1b-tier-control-retraction`](nla-harness/2026-08-30_p1b-tier-control-retraction.md) | **RETRACTION — the tier positive does not survive its own baseline.** With `l0_reply_chars` now recorded (new `--baseline-only` mode), each tier can finally be measured against its own length baseline: **L0 baseline +0.4770, residual argmax +0.5201 → beats length by +0.0431**; **L1b baseline +0.3203, argmax +0.3818 → +0.0615**. **Δ = −0.0184** — the renamed tier adds slightly *more*. **The entire +0.1383 raw-ρ gap reported hours earlier was a length-baseline gap** (+0.4770 − +0.3203 = +0.1567, almost exactly it). The claim that *adversarial renaming degrades the readability of the model's own correctness* is **withdrawn** — it came from comparing L0's ρ against L1b's baseline, the exact substitution the limitation note had flagged. **What is CONFIRMED and generalised: `last_prompt` ≈ reply length on BOTH tiers** — the residual stream at the final prompt token adds only **+0.04 to +0.06** over a token count, clean or obfuscated. Since every item-level read in this programme was taken at that site, that one sentence explains B4, B5, N11, N13 and P0.4 without invoking beliefs at all. **A real tier effect does exist but it is behavioural:** reply length predicts correctness much better on clean code (+0.4770) than renamed (+0.3203) — the model's verbosity tracks its correctness less well once identifiers are adversarial. Second control-driven reversal in two days; rule stated: **a positive not yet measured against its own baseline is not a positive.** |
| 2026-08-30 | `nla-harness` | [`2026-08-30_p1b-readout-controls`](nla-harness/2026-08-30_p1b-readout-controls.md) | **Readout controls: LATE READOUT confirmed on an independent target, and the week's first clean positive — obfuscation degrades the read.** **(A′)** Probing the *deviation* |len(answer) − len(truth)| — knowable only if you know what the model will emit — gives **+0.3650 at `last_prompt`, BELOW its own truth-length baseline of +0.3767**, versus **+0.5299 at `answer_line`** (Δ = +0.1649). **At the prompt the residual stream carries no information about what the model will actually emit.** The answer-line correctness signal (ρ = 0.5702) is therefore a **corollary of the answer being decided, not a prediction** — the pre-registered sceptical branch is confirmed and the readout paper's most attractive hypothesis is closed. (The first version of this control — answer *length*, ρ = 0.8531 vs 0.6966 — was **confounded**, since expected answer length is a task property readable from the code; it is recorded but not quoted.) **(B) Correctness is substantially more decodable on clean code than on adversarially renamed code at the same site: L0 argmax ρ = +0.5201 (L20) vs L1b +0.3818 (L26), Δ = +0.1383**, with L1b sitting essentially on its length baseline (+0.3626). **Adversarial renaming does not merely make the model wrong more often — it degrades the readability of the model's own correctness from its residual stream.** Limitation stated: `baseline.jsonl` records `reply_chars` for L1b only, so **no L0 length baseline exists on disk** and the "beats length" half is not yet established for L0 — a one-line fix and ~15 min of GPU converts this from suggestive to reportable. Also worth keeping: at the prompt, answer **length** is decodable at ρ = 0.6966 while **correctness** is 0.3730 — form is determined early, correctness is not. |
| 2026-08-30 | `nla-harness` | [`2026-08-30_p1b-graded-labels`](nla-harness/2026-08-30_p1b-graded-labels.md) | **Graded labels (k/10 over ten existing draws, zero GPU): the prompt site is worth a token count, and the answer-line signal is readout, not prediction.** **(1) At `last_prompt` the residual stream beats a length-only baseline by +0.010** (best layer ρ = +0.3730 vs length +0.3626). **Every item-level read in this programme — B4's vectors, P0.1, P0.2, P0.4, both probes this week — was taken at that site**, which is a quantitative explanation for the whole ledger of item-level nulls. **(2) Mid-reasoning carries nothing on a third independent instrument**: `reply_q50` mean ρ = **+0.0075** (−0.0704 at L20), joining `rt_cos`-over-reasoning (p = 0.59) and the binary probe (AUC 0.52). **(3) The answer-line effect is the largest internal signal in the programme** — ρ = **+0.5702** at L25 vs a +0.3443 length baseline — **but the pre-registered sceptical branch wins**: EARLY KNOWLEDGE required `reply_q50` to carry signal and it carries none, so this is **LATE READOUT ONLY**, one token from the value, and **no predictive claim is licensed**. Methodologically, **ten cheap label draws beat one careful one**: against binary labels that gave opposite curves, graded labels give split-half curve correlations of **0.7873** (`last_prompt`) and **0.9435** (`answer_line`) — though argmax at `last_prompt` agreed on **0 of 20** splits, the signature of a **flat top** (+0.32 to +0.37 across L16–L27) rather than a noisy measure. This sharpens the retraction: the shape is recoverable, the peak layer is not. Permutation p = 0.0199 with a null max of 0.4014 — the third time this week a single draw would have misled. |
| 2026-08-30 | `nla-harness` | [`2026-08-30_p1b-position-depth-results`](nla-harness/2026-08-30_p1b-position-depth-results.md) | **Position × depth — and a RETRACTION of the same morning's depth gradient.** **H-PD1 ✗ POSITION DOES NOT MATTER** as frozen: `answer_line` vs `last_prompt` at L20 gives Δ = **+0.1123** (clears the +0.10 threshold) beating length-only by +0.2245, but the CI **[−0.0303, +0.2564]** does not exclude zero at n = 59. Underpowered exactly as the pre-registration declared in advance. Permutation control clean (null 0.4752 ± 0.0936, observed 0.8229, p = 0.005) — and its **max over 200 draws is 0.7627**, vindicating distributions over single draws for the second time in two days. **THE RETRACTION:** two probes at the *same* position, same features and folds, differing only in which correctness labels they used (**agreeing on 86.7% — 8 of 60 items, exactly the reproducibility floor**), give **opposite depth curves** — argmax **L27** rising vs argmax **L13** falling, at near-identical mean AUC (0.6724 vs 0.6543). This morning's "decodability rises monotonically to the final layer" is **withdrawn**. Denoising does not rescue it: ten independent label draws on disk give **45/60 unanimous, 15 contested**, and four label treatments yield argmax L27 / L13 / L27 / L13. **The depth curve's shape is not identifiable at n = 60; its level (AUC ≈ 0.65–0.75) is.** The unanimous subset's 0.8563 is a **selection effect** — contested items are the borderline ones — not a better measurement. Exploratory and unclaimed: the position surface is a **U**, not a ramp — 0.75 at the prompt, **0.40–0.52 mid-reasoning**, 0.83 at the answer — agreeing in direction with the 08-29 `rt_cos` answer-line result. **The floor produced a false finding and caught it within 24 hours**; curve-shape claims at n = 60 now need a second label draw before reporting. |
| 2026-08-30 | `nla-harness` | [`2026-08-30_p1b-position-depth-prereg`](nla-harness/2026-08-30_p1b-position-depth-prereg.md) | **PRE-REGISTRATION** for the position × depth grid. Primary: `answer_line` vs `last_prompt` at layer 20, ≥ +0.10 with a CI excluding zero **and** beating a length-only baseline by ≥ +0.05 (length alone predicts correctness at 0.693, and position indices are a function of length). Writes the **sceptical branch first**: if only `answer_line`/`last_token` clear the bar the probe is reading a decided answer, not predicting one, and **no predictive claim is licensed** — EARLY KNOWLEDGE requires `reply_q50` to clear it too. Mandates a 200-draw permutation *distribution* after the previous probe's single-draw control misfired, and states in advance that n = 60 is underpowered. |
| 2026-08-30 | `nla-harness` | [`2026-08-30_p1b-read-depth-results`](nla-harness/2026-08-30_p1b-read-depth-results.md) | **Read-depth probe: the tier contrast is vacuous, correctness runs the other way, and the "wrong depth" explanation is retired.** **A control I wrote was defective and is corrected here:** the frozen rule tested the *maximum* single-draw permutation AUC over 28 layers against 0.60 — a single-draw threshold applied to a max-over-28 statistic — and fired at 0.6533, returning NOT REPORTABLE. A proper 200-draw null gives **0.4913 ± 0.0662** (L13) and **0.4967 ± 0.0654** (L20) with p = 0.005: **NO LEAK**; 0.60 sat at the 95th percentile of one draw, so ~1.4 of 28 layers were expected to exceed it and exactly 1 did. **H-R1 → UNINFORMATIVE (pre-declared branch):** the tier probe is at ceiling in **28/28 layers**, and the diagnostic is **AUC 0.9953 at layer 0** — adversarial renaming changes the literal tokens, so L0-vs-L1b is separable at the *embedding* layer. The probe measures surface token identity, not representation; **this retires a whole family of "a probe detects obfuscation" experiments that would have looked successful.** **H-R2 ✗ — no read advantage at 13**: correctness AUC **0.6775 at L13 vs 0.7299 at L20** (Δ = −0.0524, CI spanning zero), rising monotonically to **0.7612 at L27**. With P0.4 (no *write* advantage), P0.1's coherence peak predicts nothing causal and nothing decodable. **The charter's dense baseline now exists: AUC 0.7612** — beating reply length (0.693) and still losing to N13's free answer instability (0.869). The NLA's layer-20 choice costs only 0.031 AUC. |
| 2026-08-30 | `nla-harness` | [`2026-08-30_p1b-read-depth-prereg`](nla-harness/2026-08-30_p1b-read-depth-prereg.md) | **PRE-REGISTRATION** for the per-layer dense probe — the charter's mandated dense baseline. Primary: grouped-CV AUC for L0-vs-L1b at layer 13 minus layer 20, ≥ +0.05 with a CI excluding zero **and** beating a norm-only probe by ≥ +0.05. Declares in advance that folds must be **GroupKFold on `snippet_id`** (a snippet's two tiers would otherwise span train and test and the probe would memorise the snippet), that `C = 1.0` is **frozen not tuned** (p ≫ n at d = 3584, n = 120; per-layer tuning across 28 layers is a forking path), and — the branch that decided the result — that **AUC ≥ 0.95 at every layer is UNINFORMATIVE**, a property of the stimulus set and explicitly not "layer 20 is fine". |
| 2026-08-30 | `nla-harness` | [`2026-08-30_reads-exact-truncation-ruled-out`](nla-harness/2026-08-30_reads-exact-truncation-ruled-out.md) | **Reads are bit-exact; truncation is not the mechanism.** Two follow-ups in one job on one card. **(1) The reproducibility floor is GENERATION-ONLY** — the layer-20 activations that feed V3/V4 are **120/120 bit-identical, max |Δ| = 0.0**, both within-process and cross-process. So `rt_cos`, `act_norm`, the P0.1 layer curves, N10b's read scores and the 14,619-row unified table carry **no reproducibility caveat at all**; it is confined to graded generations. This materially de-escalates the 08-29 claim that *every* paired per-item table inherits the floor. **(2) Raising `MAX_NEW_GEN` 1100 → 2048 does not move the floor** (0.8833/0.8833 vs 0.9000/0.8500) **but eliminates censoring entirely** — parse rate **0.8833 → 1.0000** at unchanged accuracy. Censoring and irreproducibility are separate problems; the 3-of-10 parse-failure clue was a correlate, not a cause. The floor is now robust to different cards, deterministic kernels **and** a doubled budget, and its cause **remains unidentified** — 0.85–0.90 should simply be quoted. Also retracted: the "instability is largest in the obfuscated condition" reading, which reverses at 2048 and is within noise at n = 60. Recommendation independent of all this: **`--max-new-gen 2048` for new runs**, since 12% of items were being scored wrong for not finishing. |
| 2026-08-29 | `nla-harness` | [`2026-08-29_determinism-floor-structural`](nla-harness/2026-08-29_determinism-floor-structural.md) | **The reproducibility floor is STRUCTURAL — deterministic kernels do not fix it.** Frozen rule: same-card agreement of 1.000 would have made the floor a choice. With `CUBLAS_WORKSPACE_CONFIG=:4096:8`, `use_deterministic_algorithms`, `cudnn.benchmark=False` and SDPA forced to math, two runs on one card (UUID-verified) agree **0.8333 / 0.9000** — indistinguishable from the unpinned **0.9000 / 0.8500**. Every pairing tried, pinned or not, same card or different, sits in one 0.80–0.95 band. **The flags genuinely applied**: `warn_only=True` emitted **zero** warnings, so no op fell back to a nondeterministic kernel. **`--deterministic` also changes answers rather than only stabilising them** — agreement with default mode is **0.8000**, the lowest number in the table, at identical marginal accuracy — so it forks the corpus and stays off by default. **A partial mechanism emerged that is not the kernels:** disagreeing items generate **~25% longer replies** (2074 vs 1655 chars) and on 3 of 10 one run never emitted an `Output:` line — part of the churn is running out of generation budget, pointing at `MAX_NEW_GEN` rather than float reduction order. The residual cause is **not identified**. L1b reproduces worse than L0 in every pairing: the instability is largest in the obfuscated condition the programme exists to study. |
| 2026-08-29 | `nla-harness` | [`2026-08-29_reliability-floor-and-length`](nla-harness/2026-08-29_reliability-floor-and-length.md) | **Three calibration results, and a correction to the P0.4 entry.** (1) **The per-item reproducibility floor is run-to-run, not card-to-card.** Two runs of the identical command on the **same physical GPU** (UUID-verified), same seed, greedy decoding, disagree on **6 of 60** items — agreement **0.90** unsteered, **0.85** steered — while cross-node agreement (0.95 / 0.92) is *no worse*. The card-to-card explanation given in `2026-08-28_p04-depth-results.md` is **not supported**, and the problem is worse than stated there because pinning hardware cannot fix it; the seed never touches this path (greedy decoding draws no RNG). Marginals stay within 0.017–0.083 while 10–15% of items churn. (2) **N10b is POPULATION ONLY** — Spearman-Brown reliability of the 14-read instrument is **0.581 / 0.613 / 0.523** (`specific_frac` / `generic_frac` / `mm_mean`) against a pre-stated 0.70, falling to **0.477** for `mm_mean` on hard negatives. The programme's one positive separates crowds and **cannot rank a single file**; the best population discriminator is the *least* reliable per item, while half-AUC sd is only 0.015 — the AUC is stable, the ranking is not. (3) **B6 judge-free: `rt_cos` adds nothing over reply length** (LRT p = 0.27, ΔCV-AUC **−0.0027**, n = 512); its solo AUC 0.654 is the describability confound, measured. **Length alone has AUC 0.306 — inverted** — beating every internal signal but N13's answer instability. One lead survives BH-FDR: `rt_cos` at the **`answer_line`**, q = 8e-05, ΔCV-AUC **+0.0267** — faithfulness pays at the moment the answer is emitted and nowhere in the reasoning trace. |
| 2026-08-28 | `nla-harness` | [`2026-08-28_p04-depth-results`](nla-harness/2026-08-28_p04-depth-results.md) | **P0.4 ✗ NOT DEPTH-LIMITED — Phase 0's row-4 reading gets its third leg.** 2,880 rows / 0 errors across three depth arms on h200. Primary: V4_oracle at **L13 − L20, α = 1.0 = −0.0667** [−0.1833, **+0.0500**], p = 0.424 — the CI upper bound sits below the frozen +0.10, so the effect is *excluded*, not merely unfound. **The oracle at the most coherent depth in the network nets exactly 0.0000 — 5 recovered, 5 damaged.** Unlike P0.2 this is indifference, not destruction: parse rates run **0.82–0.95 across all 45 cells** and the whole dose-response surface is flat within ±0.133, `R_random` wandering as much as the oracle. Layer-indexing gate PASS (fp32 residual **8.5e-08 / 8.8e-08 / 4.3e-08** at ℓ = 6/13/20, one position written, nothing else moved) — the read-site/write-site identity the code has asserted in comments since `steer.py` was written and never tested. So: the channel is not the bottleneck (P0.2), the site is not inert (P0.3), the depth is not wrong (P0.4) — three pre-registered ways of blaming the apparatus, all closed. **Methodological finding, unplanned:** greedy bf16 decoding reproduces in aggregate but **not per item** — V4's Δacc matches the banked csr-94608 arm exactly (+0.0833 both) while **10 of 60 items disagree**, and two juno arms with *identical* 0.5500 baselines disagree on six. Every paired per-item table in the programme inherits an unmeasured noise floor. One secondary lead not banked: α = 2 clears criterion (a) at +0.1667, q_BH = 0.0254 — the first secondary alpha here to survive FDR — but it is L20 falling, not L13 rising. |
| 2026-08-28 | `nla-harness` | [`2026-08-28_p04-depth-prereg`](nla-harness/2026-08-28_p04-depth-prereg.md) | **PRE-REGISTRATION, frozen before a single depth arm existed.** P0.4 asks whether the belief-injection failure is a **depth** failure: P0.1 found cross-item coherence peaks at **L13 (0.543)** while magnitude peaks at **L20 (0.459)** and the two directions are nearly orthogonal (cos 0.21), yet every intervention in the programme has been written at 20 because that is where the released NLA pair was trained. V3/V4/R are pure activation arithmetic and defined at every layer, so this needs no autoencoder. Three arms — **L13** (coherence argmax), **L06** (band control: coherence 0.531, seven layers away) and **L20** (re-run on juno, *not* taken from the bank, so depth is not confounded with hardware) × 5 alphas × 60 items = 2,700 generations. Primary: **V4_oracle, L13 − L20, α = 1.0**, DEPTH-LIMITED only if it clears **+0.10** with a CI excluding zero **and** beats its own baseline **and** beats `R_random` **and** keeps parse rate **≥ 0.80** — the last clause because P0.2's negative was *destruction*, not a null. Records in advance that L13's 0.543 is the **argmax of eighteen near-ties** (ℓ ∈ [0,17] all sit in 0.49–0.54), so a layer-specific win is to be reported as unexplained and awaiting replication, explicitly **not** as "coherence predicts steerability". Two gates: a layer-indexing gate (read site == write site to 1e-3, or P0.4 is not reported at all) and a cross-cluster reproduction gate against the banked csr-94608 B4 arm. |
| 2026-08-28 | `nla-harness` | [`2026-08-28_p03-extension-prereg`](nla-harness/2026-08-28_p03-extension-prereg.md) | **PRE-REGISTRATION** for the P0.3 extension, frozen before any juno cell. SITE LIVE is recorded and not renegotiated; three things it does not settle are. **H-ext1 (layer specificity)** — layer-matched single-band controls at **[13,13]** and **[26,26]**: if perturbing any single layer costs about the same, "site live" is a fact about single-layer attention steering in general and says nothing about the injection site. **H-ext2** — a **third seed**, because E5b watched a five-transform family average flip sign between draws and two seeds is the floor, not the target. **H-ext3** — finish the `[20,27]` cells the migration stopped at ~25%. 21-cell grid; the four scope bands are to be reported as **four points, not a trend**, unless their ordering is identical at all three seeds. Cannot overturn P0.2 and therefore cannot by itself move Phase 0 off licensing row 4. |
| 2026-08-28 | `nla-harness` | [`2026-08-28_p0-triage-results`](nla-harness/2026-08-28_p0-triage-results.md) | **PHASE 0 DECIDED.** P0.1 **HARD** (min cos 0.277 < frozen 0.50; layer-index gate 0.0e+00; coherence peaks **L13** while magnitude peaks L20). P0.2 **NOT CHANNEL-LIMITED** — the oracle goes +0.0833 → **−0.3333** when the write widens to `all_reply`, a contrast of **−0.4166** against a frozen +0.10, with parse rates collapsing to 0.383 (V4) and **0.100** (V1): the channel over-delivers and destroys generation rather than failing to deliver. P0.3 **SITE LIVE** — [20,20] moves **−1.434/−1.658** pts against a **0.794**-pt baseline seed spread, same direction at both seeds, 5,790 cases per cell. → licensing row 4, *the site works but belief-shaped writes do not* → **Phase 1b**. |
| 2026-08-27 | `nla-harness` | [`2026-08-27_p0-triage-prereg`](nla-harness/2026-08-27_p0-triage-prereg.md) | **PRE-REGISTRATION, frozen before any run.** Three experiments to decide whether B4/B5's negatives mean "no belief to edit" or "the channel cannot deliver": P0.1 layer rotation (cos 0.50 → SOFT/HARD), P0.2 oracle at `all_reply` (CHANNEL-LIMITED at ≥ +0.10, CI excluding zero, `R_random` as disqualifier), P0.3 attention scope curve over four bands × two seeds (SITE LIVE / DEAD / UNINFORMATIVE). Includes the 2×2 licensing table and the clause that P0.1 is **not reported at all** if its layer-index gate fails. |
| 2026-08-26 | `nla-harness` | [`2026-08-26_b5-composition`](nla-harness/2026-08-26_b5-composition.md) | **B5 — the two levers do not measurably compose.** *(Owed ledger entry, written 2026-08-31 for a 2026-08-26 run; results had existed only in `data/nla/b5/` and the full-results report, never in the ledger.)* Validation gate **PASSED** (62.746 vs banked 61.710, |Δ| = 0.01036 against a 0.02 tolerance), so this is a null about the manipulation and not the plumbing. Six cells × 1,930 cases: none 64.04 · attn 62.75 · nla 61.92 · both 64.56 · rand 62.23 · disjoint 64.66. Point estimates read like composition — the pair beats either lever alone (**both − attention +1.81**) and beats a random direction by **+2.33** — and **not one interval excludes zero**. The collision control did its job: attention occupies layers 20–27 and the autoencoder sits at 20, so moving attention to 21–27 tested whether "both on" was a collision; it changed the result by **+0.10**. Two bounds recorded at the time: **one seed**, and V1 directions exported from `Qwen2.5-7B-Instruct` into the Coder sibling **B1 refuted transfer to**. **The run persisted no per-case rows**, so the six cells cannot be re-analysed from disk at all — P0.3 keeps per-run `score.json` and that property should be required of anything new. |
| 2026-08-26 | `nla-harness` | [`2026-08-26_e5b-seed-instability`](nla-harness/2026-08-26_e5b-seed-instability.md) | **B0's sign does not replicate.** 108 units / 15,018 generations / 0 failures. The 5-transform family goes **−1.24 [−4.41, +1.41] → +4.46 [+0.85, +7.09]**; four of five transforms flip (`opaque_predicates` swings 7.8). The published **+7.02 [+2.00, +10.09]** contrast is **not established** — its CI described sampling within one draw, not variability across draws. Only `arith_rewriting` survives (−8.22, −5.65). |
| 2026-08-17 | `nla-harness` | [`2026-08-17_b4-sweep-results`](nla-harness/2026-08-17_b4-sweep-results.md) | **B4 NOT SUPPORTED at every α.** 2,160 rows / 0 errors; primary gate V1 0.550 = V3 0.550, 7-v-7, **p = 1.00**. Exposes the drift confound — a **random** norm-matched direction gains **+0.083** by α = 4, which is why the bar is V3 and not zero. One exploratory lead (**V2 at α = 4**: 0.683 vs 0.550, 12-v-4, p = 0.077, monotone 0.000 → 0.183) explicitly not banked. Found V2 silently building **8 of 60** directions. |
| 2026-08-17 | `nla-harness` | [`2026-08-17_b1-transfer-refuted`](nla-harness/2026-08-17_b1-transfer-refuted.md) | **B1 ✗ REFUTED.** 1,640 reads / 0 errors at *identical token positions* (shared `tokenizer.json`). Median `rt_cos` **0.694** vs 0.864, gap **+0.170**, both frozen gates fail. **The scale explanation is ruled out by the injection maths, not by hand-waving** — injection is direction-only, so the Coder's +38% `act_norm` is normalized away and the `injection_scale` re-derivation branch is struck from the plan. Degraded, not broken (own-vs-foreign +0.355). |
| 2026-08-17 | `nla-harness` | [`2026-08-17_compose-port-validated`](nla-harness/2026-08-17_compose-port-validated.md) | **B5 unblocked** — the asserted "`steer.py` has no transformers coupling" is now tested in the target env: **byte-identical to unsteered at α = 0**, writes exactly `[5]`, neighbours bit-unchanged. A false `[5, 17]` alarm was **the test, not the hook** (`reset()` rewinds the cursor but leaves the spec installed). |
| 2026-08-16 | `nla-harness` | [`2026-08-16_b4-sweep-prereg`](nla-harness/2026-08-16_b4-sweep-prereg.md) | **PRE-REGISTRATION** before any sweep row: primary V1 > V3 at α = 1.0 / `last_prompt`, secondary alphas BH-adjusted with the primary excluded from its own correction. States in advance that the sweep is **expected to strengthen a negative** — the antipodal control already matched random exactly, so the direction has no consistent sign. |
| 2026-08-16 | `nla-harness` | [`2026-08-16_n13-torn-results`](nla-harness/2026-08-16_n13-torn-results.md) | **Torn-ness is not internally readable.** Matched 2×2 over 34 pairs: `rt_cos` AUC **0.491**, `act_norm` 0.442, instability 0.408 — all at or below chance (length-confound check 0.5117). Gate 1b **FAILS** (−0.0005): the trap does not induce indecision, it just makes the model wrong. Only `mean_instability` beats the length baseline (ΔR² 0.0166, p_perm 0.029); `act_norm` adds **0.0000**. Free positive: answer entropy → correctness at **AUC 0.843**. |
| 2026-08-15 | `nla-harness` | [`2026-08-15_b1-transfer-prereg`](nla-harness/2026-08-15_b1-transfer-prereg.md) | **PRE-REGISTRATION** with thresholds and the position-defect remedy frozen before the subject arm produced a read; the only numbers in existence (a 3-snippet plumbing smoke) disclosed in full. Records in advance that **both outcomes are reportable**, so there is no incentive to squint at the number afterwards. |
| 2026-08-16 | `nla-harness` | **Steering examples regenerated** — all 10 flips reproduce; V1's gloss edit is a name-list template, not an explanation; V1/V3 tie while disagreeing on 7 of 11 items, so the failed gate is about selection rather than redundancy. |
| 2026-08-16 | `nla-harness` | **Unified reading table** — 14,619 readings across obfuscation + malware passes in one schema (232 reused dropped; malware redacted at the boundary). Reasoning readings more faithful on malware, but 61% of the gap is length: adjusted +0.0152 [+0.0073,+0.0232]. Framing changes content, not fidelity. |
| 2026-08-15 | `nla-harness` | **N13 follow-on: mid-trace dip** — not length, not `act_norm` (which declines monotonically while rt_cos recovers; describability and magnitude dissociate). Reproduces on instability but the judge-free lexical measure is flat, so the AR-shared machinery confound stands. |
| 2026-08-15 | `nla-harness` | **N13 addendum** — answer-normalizer artifact inflated entropy (36.4% of items); hardened re-analysis withdraws the marginal instability result (p .029 → .234) and strengthens entropy→correctness to AUC 0.869. All three internal signals null. |
| 2026-08-15 | `nla-harness` | **N13 torn-ness NULL** — rt_cos/act_norm/read-instability all fail to separate torn from committed-wrong (AUC 0.491/0.442/0.408); read instability is describability inverted (class-level r = -0.958). Length alone explains 35% of answer-entropy variance. Behavioural by-product: answer entropy predicts correctness at **AUC 0.843**. |
| 2026-08-14 | `nla-harness` | **N10b SUPPORTED — first positive result.** Malicious vs matched-benign AUC **0.764** (neutral arm), **0.757 on hard negatives only**, length confound 0.477. Theme-level malice is readable; item-level capability is not (N10). |
| 2026-08-14 | `nla-harness` | **N12/B4 steering gate FAILS** — V1 (NLA) = V3 (no-NLA task vector) exactly, 7v7 discordant, p=1.00; prompt-only best (+0.100); antipodal = random. |
| 2026-08-14 | `nla-harness` | **N11 coupling follow-up: 3/3 becomes 3/10** (30% vs 8.3%, p=0.138). `CODE_FAR` flat at 0.054/0.063/0.063 — no elevation off the identifier even when enriched for deception. B3 recommended closed. |
| 2026-08-13 | `nla-harness` | Debug pass 2 (by execution, not reading): scorer crashed on empty cells at 4 sites — fatal for an unattended chain; now degrades to nulls + warnings. Per-hit control matching fixed. |
| 2026-08-13 | `nla-harness` | Unattended one-GPU pipeline + bug check: **142 cross-dataset snippet-name collisions** found in 3 keying sites (silent data corruption), plus 4 further fixes. 64 tests pass. |
| 2026-08-13 | `nla-harness` | **N11 deception NOT ESTABLISHED** — the 90% effect is lexical echo (CODE_ID 30.9% vs 0.8%, collapsing to ~3% off-identifier); echo-free primary 9.0% vs 2.0% (p=.039) sits inside its foreign-read null CI [0.02, 0.10]. |
| 2026-08-13 | `nla-harness` | **N10 malware two-arm results — item-level signal NULL.** Framing confound confirmed (generic malice 2.5x under the security preamble); own 0.874 < foreign-read null 0.903. Reads are theme-level, not item-level. |
| 2026-08-12 | `nla-harness` | **Grader validated on 5,090 real reads** — fire rate 0.205, not degenerate; `encoding` exposed as an obfuscation-surface detector and narrowed; `k>=3` recurrence rule found unaffordable (median 2 gradeable reads/item). |
| 2026-08-12 | `nla-harness` | **Belief-steering pipeline built, CPU only** — `steer.py` write hook (alpha=0 byte-identical), V1-V5 direction ladder with leakage guard, judge-free `belief_grade.py`. 45 tests pass; 4 grader bugs found, one only by end-to-end integration. |
| 2026-08-12 | `nla-harness` | N10 malware: 183-item corpus from DataDog (59% near-dups removed), quarantine + no-execute lint, base NLA reads running. Sleeper-agents CWE pair corpus (850 pairs) + Gate C1 AUC 0.638 PASS. |
| 2026-07-24 | setup | [`2026-07-24_register-foundational-papers`](setup/2026-07-24_register-foundational-papers.md) | Registered Papers 1–3 (moved PDFs → `papers/`, wrote `references.bib` + `REFERENCES.md`). |
| 2026-07-24 | setup | [`2026-07-24_charter-and-scaffold`](setup/2026-07-24_charter-and-scaffold.md) | Wrote `CLAUDE.md` (Instrument 3 = SAE + transcoder analysis); built `log/`; organized `docs/`; seeded scratchpad. |
| 2026-07-24 | setup | [`2026-07-24_hypotheses-checklist`](setup/2026-07-24_hypotheses-checklist.md) | Formalized HT1–HT8 ↔ E1–E8 + phased task list into `docs/CHECKLIST.md`. |
| 2026-08-03 | infra | [`2026-08-03_pipeline-scaffold-smoke`](infra/2026-08-03_pipeline-scaffold-smoke.md) | Phase-0 scaffold built (`configs/`+`src/`+`environment.yml`); smoke + CWD-independence pass; 34-agent adversarial review → 28 findings fixed (incl. a flagship-corrupting token-alignment bug → char-span schema). |
| 2026-08-04 | infra | [`2026-08-04_env-dictionaries-data`](infra/2026-08-04_env-dictionaries-data.md) | Env `transcoders-mi` built + frozen (circuit-tracer downgrades documented); **all 14 dictionaries + 12 model ids pinned** with HfApi-verified shas; Dataset A/B + Papers 2–3 behavioral tables located + symlinked (`data/DATA_SOURCES.md`). |
| 2026-08-04 | infra | [`2026-08-04_e1-path-live`](infra/2026-08-04_e1-path-live.md) | **E1 path live end-to-end:** stimuli converter (cross-tier spans; decoy↔true pairing 100% on A-L1b; resolution 1.0000), SAE loaders, real Llama-3.1-8B smoke on GPU. First transfer numbers **FVU 0.506 / cos 0.773 / L0 28** → **Q-norm** opened (scale mismatch vs real transfer gap). |
| 2026-08-04 | nla-harness | [`2026-08-04_g0-replication-and-code-probe`](nla-harness/2026-08-04_g0-replication-and-code-probe.md) | **G0 PASS** (Stage A 0/101 over tol; Stage B median |Δcos| 0.0037) after 3 fixes; **44-read code probe: feasibility ✓** — L0 reads name the recurrence, L1b shows decoy/true mixture (DRM_AR +0.047 toward decoy vs L0), L2 reads say "state machine", CoT reads track reasoning. N1–N3 designed, parked. |
| 2026-08-04 | nla-harness | [`2026-08-04_example-captures`](nla-harness/2026-08-04_example-captures.md) | **Capture gallery** (5 tasks, 42 reads → `data/nla/examples/EXAMPLES.md`): output-prediction ✅ on Set-intersection, fibfib(14) exhausts 900-token greedy budget; slicing 1/3 with a consistent print-line/initializer error pattern; **near-answer CoT reads are answer-shaped** — N1's raw material. |
| 2026-08-04 | nla-harness | [`2026-08-04_answer-line-reads`](nla-harness/2026-08-04_answer-line-reads.md) | **Answer-line reads** (12): the NLA reads the *act* of committing to a formatted answer exactly; digits are lossy variants — slice_2's read echoes the model's own wrong answer (faithful to the computation, not the world). Gallery rebuilt with full reads. |
| 2026-08-04 | nla-harness | [`2026-08-04_overnight-launch`](nla-harness/2026-08-04_overnight-launch.md) | **Overnight capture launched**: 380 tasks (300 graded output-prediction across all tiers + 50 prompt-read + 30 synthetic slicing), rich read package + DRM; resumable rig, smoke+resume proven; ETA ~8 h in tmux `nla-overnight`. |
| 2026-08-05 | nla-harness | [`2026-08-05_overnight-abort-fix-relaunch`](nla-harness/2026-08-05_overnight-abort-fix-relaunch.md) | Overnight run aborted 19 min in — **CJK monitor false positive** (benign reads quoting Chinese; incl. a read reporting Qwen internally concluding in Chinese). Detector made fraction-based; error-row resume + summary-table fixes; **relaunched 06:09 with resume** (361 to go, ETA ~7 h). |
| 2026-08-05 | nla-harness | [`2026-08-05_overnight-results`](nla-harness/2026-08-05_overnight-results.md) | **Overnight capture COMPLETE** (12:38, ~6.5 h unattended, 0 errors): 380 rows, 5,043 reads. Tier accuracy **non-monotone** — A-L1b .80 ≥ L0 .70, **L3 floor** (.50/.54) — the papers' pattern out-of-the-box; slicing 0.33; DRM_AR true-leaning at trap tokens. N1-ready dataset banked. |
| 2026-08-06 | nla-harness | [`2026-08-06_faithfulness-by-token-type`](nla-harness/2026-08-06_faithfulness-by-token-type.md) | **Faithfulness analysis** (5,090 reads, no new compute): neutral renames read worst (.847), **misleading names read as well as real ones** (.879 vs .878, n.s.) — replacing meaning doesn't degrade readability, removing it does; mid-reasoning dip .94→.83→.87; **correct runs more describable than wrong ones in every tier** (all CIs exclude 0) — a cheap preview of N1. |
| 2026-08-06 | nla-harness | [`2026-08-06_n4-first-error-oracle`](nla-harness/2026-08-06_n4-first-error-oracle.md) | **First-error oracle** (S0 refactor gate 804/804 anchors, then D0–D2): flagship known-answer gate **PASSED 4/4**; coverage 41/117 localized. **0 of 140 wrong traces contains internally false arithmetic** — the model computes the wrong thing correctly, so ground-truth execution is the only mechanical detector. |
| 2026-08-06 | nla-harness | [`2026-08-06_n4b-judge-and-validation`](nla-harness/2026-08-06_n4b-judge-and-validation.md) | **D3 judge** catches the L2 dispatcher mis-decode D2 structurally cannot — but **validation FAILED**: D2-state vs D3 agree 39% (r=+0.10) and D3 self-agrees on only 46% across seeds. Precise localization declared unsolved; N5 redesigned around a fixed 0.70–0.90 strip instead of per-case bursts. |
| 2026-08-06 | nla-harness | [`2026-08-06_s1-async-gate-failed`](nla-harness/2026-08-06_s1-async-gate-failed.md) | **Async AV gate FAILED**: 6.3× faster but only 15% byte-identical; AV proven deterministic *sequentially* (60/60) → batching, not sampling, is the cause. Deterministic-inference mode gives 100% async agreement but 0% against the banked corpus. Option B (default config + sequential) adopted to preserve corpus comparability. |
| 2026-08-06 | nla-harness | [`2026-08-06_n6-n8-preregistration`](nla-harness/2026-08-06_n6-n8-preregistration.md) | **PRE-REGISTRATION** — HT12/HT13/HT14 decision rules frozen (BH-FDR as one family) *before* any of N6/N7/N8 ran, including the refute clause \|β\|<0.005 and the standing honesty clause that the banked corpus can sharpen but never independently confirm its own post-hoc findings. |
| 2026-08-06 | nla-harness | [`2026-08-06_n5-dense-results`](nla-harness/2026-08-06_n5-dense-results.md) | **N5 dense capture COMPLETE**: 91/91 length-matched pairs, **4,653 reads**, 0 errors, ~4.4 h unattended; median 24 reads/case (~4× prior density). Drift gate literally failed but substantively passed (232 reused reads match **exactly**; position-matched within ±0.017) — the 0.02 shift is composition, not drift. |
| 2026-08-07 | nla-harness | [`2026-08-07_n6-ht12-refuted`](nla-harness/2026-08-07_n6-ht12-refuted.md) | **HT12 ✗ REFUTED** — faithfulness does **not** predict correctness once cases are length-matched (dense β=−0.0002, CI [−0.0051,+0.0047], p=.93), and the null is *informative*: it excludes the banked effect size. Decomposition pins the cause on **case selection / reply length**, not position composition — restricting the *same banked reads* to matched cases already collapses Δ 0.0145→0.0035. Pre-registration deviation declared: `(1\|case)` is unusable for a group-constant predictor → GEE. |
| 2026-08-07 | nla-harness | [`2026-08-07_n7-prerun-power-addendum`](nla-harness/2026-08-07_n7-prerun-power-addendum.md) | **Pre-run addendum** (dated before the judge): HT13 analyzable-n and power declared (0.90 at 0.4 SD), primary/sensitivity arms split, and HT14 checked for the group-constant-predictor trap that broke HT12. |
| 2026-08-07 | nla-harness | [`2026-08-07_n7-instrument-null`](nla-harness/2026-08-07_n7-instrument-null.md) | **N7 = INSTRUMENT NULL → HT13 not adjudicated.** Blinding verified by dataflow (geometry gate 4,421/4,421); 5,653 items judged, 0 unparsed. G1 shuffled-AUC **0.757** ✅ and G2 beats the AR baseline (0.757 vs 0.733) ✅, but **G3 κ = 0.049** ❌ — Phi-3.5 answers "2" on 91% of items (AUC 0.540) and per-item agreement is nil (r 0.11); judge vs AR baseline ρ **0.03**. Population separation ≠ item-level reliability. **Alignment sort stays disabled**, as pre-registered. |
| 2026-08-07 | nla-harness | [`2026-08-07_n8-ht14-refuted`](nla-harness/2026-08-07_n8-ht14-refuted.md) | **HT14 ✗ REFUTED.** 92% of cases never reach onset → the median is undefined, so the "≥0.10 earlier" criterion is unmeasurable. Own-answer hit rate **2.8% is BELOW the 3.6% foreign-answer null** — the reads do not carry the answer. The significant log-rank (p=.002) is an **answer-commonness confound**: wrong answers are rarer and longer (median 0 vs 5 shared cases, 16 vs 7 chars, p=1.3e-14) and the frozen null guarded the trajectory but **not the onset statistic**. |
| 2026-08-07 | nla-harness | [`2026-08-07_artifact-f1-f2`](nla-harness/2026-08-07_artifact-f1-f2.md) | **Artifact F1/F2 + a real bug.** Refuted claim **retracted in place** (chart kept, uncorrected); verdicts section added; faithfulness sort shipped with an honest caption and **nulls last in both directions**; alignment sort deliberately absent (HT13). `cotHTML` rebuilt on a token-span layer — the old loop clamped spans so **highlights were shifted right (OLD misplaces 4/5, NEW 0/5)**; bidirectional linking scrolls only the un-clicked pane; `role="button"` finally has a key handler. Real corpus: 330 cases / 2,828 marks, **0 lost, 0 text corruption**. |
| 2026-08-07 | nla-harness | [`2026-08-07_n9-confabulation`](nla-harness/2026-08-07_n9-confabulation.md) | **Confabulation measured, planning refuted** (exploratory). **91%** of 9,511 readings name a language; **37% name the wrong one** — and it is a *prior*, not noise: **79.9% wrong on JavaScript** (says "Python" 3,162× vs "JavaScript" 798×) against **1.0% on Python**. Structured by what is read: code tokens **12%** wrong vs reasoning prose **46%**. Faithfulness barely helps (43.9%→31.4% across rt_cos quintiles) — the sharpest measured proof that recovery ≠ truth. **No evidence of look-ahead**: own readings anticipate 0.548 words vs a foreign-reading null of 0.532 (CI −0.022 to +0.053). Four readings curated for the artifact. **Addendum:** both ad-hoc results promoted to version-controlled scripts (`n9_lookahead_null.py`; positional breakdown into `n9_confabulation.py`); page now shows the planning null as a figure, plus cards for the failed prediction and the open question — the verbalizer-vs-model run is **not started, GPUs busy**. |

## Changelog
- **2026-09-07** — Added [`RESULTS.md`](../RESULTS.md), a master index over all 120 dated entries; writing it turned up the L3 composition defect (L3 = L1 ∘ L2, not L1b ∘ L2), filed the same day.
- **2026-09-07** — **Index caught up with the ledger again.** `nla-harness` goes 99 → **114** entries: fifteen were on disk but in neither index (09-05 dose-and-tier-prereg, all nine 09-06 entries, all five 09-07 entries). Both indexes had read *Last updated: 2026-09-05*. Science since then: `W13-ACCUMULATIVE-TIER-GENERIC`, H-W15 ✓ / H-W16a ✓, H-W17a ✓, H-W19 ✓ (pipeline defect), H-W23 ✓ `W16-MEANING-CARRIES-IT`, the flippable census, and H-W31 pre-registered and submitted (job 382366).
- **2026-08-28** — **Phase 0 decided, and the index caught up with the ledger.** `nla-harness` goes 29 → **42** entries: nine were on disk but had never reached either index (08-15 b1-prereg, 08-16 b4-prereg + n13-torn-results, three 08-17 entries, 08-26 e5b, 08-27 p0-prereg) plus today's results entry. Both indexes had read *Last updated: 2026-08-14* while the ledger ran to 08-27.
  **The science:** P0.1 **HARD**, P0.2 **NOT CHANNEL-LIMITED**, P0.3 **SITE LIVE** → licensing-table row 4, *"the site works but belief-shaped writes do not"*, which the pre-registration names as the **strongest** support for the no-item-level-belief reading → **Phase 1b, the readout paper.**
  **Two things worth carrying forward.** (1) P0.2's negative is stronger than "no delivery": widening the write collapses V1's parse rate to **0.100**, so the channel over-delivers and breaks generation rather than failing to carry content — B4's negative is about representations, not plumbing. (2) The P0.3 verdict was reachable from cells already finished: SITE LIVE is defined purely on [20,20] vs baseline, and only the SITE DEAD / UNINFORMATIVE branches need the partial [20,27] cell. `nla/src/p0_verdict.py` was added so the frozen rules are evaluated arithmetically instead of by eye, and it returns `NEEDS_DATA` rather than guessing on branches whose inputs are incomplete.
  **Provenance debt recorded, not papered over:** the ~400 MB per-run P0.3 result tree did not survive the cluster migration to `juno-l-01`, so the cell table is a restored copy (`data/nla/p0/p03/cells_scored.PROVENANCE.md`) and no P0.3 cell can be re-scored on a different denominator until that tree is re-copied or re-run.
- **2026-08-07d** — **N9 (nla-harness now 18 entries):** the project's long-standing qualitative claim — "themes reliable, specifics confabulated" — is now measured, and it is a **Python prior** rather than random invention. Notably the most trustworthy measurement in the whole programme needed **no model at all**: a regex against a known ground-truth field labelled all 9,511 readings for free, where N7's two-GPU judge run could not rank a single item. A look-ahead/planning story was found and then **killed by a foreign-reading null** in ten minutes; two of my own sub-hypotheses failed inside the same entry.
- **2026-08-07c** — Artifact F1/F2 shipped (nla-harness now 17 entries): the page no longer states the refuted HT12 claim, and a genuine highlight-misplacement bug in `cotHTML` was found, fixed and regression-tested. Builder moved into version control at `nla/tools/build_page.py`; tests at `nla/tests/`. **Addendum:** the N5 dense corpus is now merged into the browser itself (5,090 → **9,511** readings; 182 cases at a median 37 each) via the new `anchor_dense_reads.py` producer (token gate 4,421/4,421), plus two evidence panels built from the N7/N8 result JSONs. Two further self-inflicted bugs caught and fixed (missing token context on dense reads; a parent-word walk that reported "piece of `step by`"). **Third addendum:** the verdicts are now **six inline-SVG figures** (judge score distributions · ROC vs the free AR baseline · the Llama×Phi agreement matrix that vetoed HT13 · alignment across the trace · HT12's decomposition forest · HT14's excess-over-decoy trajectory), with every colour run through the data-viz validator in both themes. Rendering them to PNG and looking caught five layout/contrast bugs that the structural checks passed.
- **2026-08-07b** — **Confirmatory family closed (nla-harness now 16 entries).** All three pre-registered hypotheses resolved negatively, each by a *named mechanism* rather than weak data: HT12 = reply length, HT14 = answer-string commonness, HT13 = not adjudicable (the judge's score is not a stable per-item property; κ 0.05, ρ 0.03 vs the AR baseline). **BH-FDR is vacuous** — no p-value entered the family — recorded explicitly so its absence is not read as an oversight. Artifact consequences: faithfulness sort must be re-captioned, alignment sort stays disabled.
- **2026-08-07** — **HT12 refuted** (nla-harness now 13 entries; the five missing 2026-08-06 rows — N4, N4b, S1, pre-registration, N5 — backfilled into the Timeline at the same time). The 2026-08-06 "correct runs are more describable" result is now identified as a **reply-length artifact**; anything downstream that leaned on it (incl. the artifact's faithfulness sort) must be re-captioned. Pre-registration held, including its refute clause.
- **2026-08-06** — Faithfulness-by-token-type analysis added (nla-harness now 7 entries); charts folded into the results-browser artifact. Caveats logged: read-length confound, read-level (not case-level) bootstrap.
- **2026-08-05b** — Overnight capture COMPLETE; results entry added (nla-harness now 6 entries). 380-row / 5,043-read dataset banked at `data/nla/overnight/2026-08-04/` — the N1 raw material.
- **2026-08-05** — Overnight post-mortem + relaunch entry; nla-harness now 5 entries. Run live again in tmux `nla-overnight`.
- **2026-08-04e** — Added the `overnight-launch` entry; nla-harness now 4 entries. 380-task capture running unsupervised (wall guard 11 h, monitor armed).
- **2026-08-04d** — Added the `answer-line-reads` addendum entry; nla-harness now 3 entries. Gallery rebuilt: full untruncated reads + answer-line section.
- **2026-08-04c** — **nla-harness thread activated** (1 entry): G0 replication gates pass, out-of-the-box code-reads probe confirms feasibility; the N1–N3 faithfulness program (HT9–HT11) is designed but parked downstream at the user's direction.
- **2026-08-04b** — Added the `e1-path-live` entry; infra now 3 entries. Everything between raw stimuli and SAE features now runs on the real model; the one open question before the full E1 sweep is Q-norm. Discovered mid-run that the alignment parquets are from a different L1b generation round (documented in `DATA_SOURCES.md`); `/`-in-snippet-id filename bug fixed.
- **2026-08-04** — Added the `env-dictionaries-data` entry; infra now 2 entries. Phase-0 blockers cleared: env READY, registries fully pinned (zero `repo: null` left), data linked. Remaining before E1: stimuli JSONL converter, `apply_dictionary`, 3 gated-license acceptances.
- **2026-08-03** — **infra thread activated** (1 entry): pipeline scaffold + smoke + adversarial review. Span-resolution smoke green (mean 1.000); dictionary repo ids + conda env creation remain open.
- **2026-07-24** — Log initialized with the `setup` thread. Migrated the flat `LOG.md` into `setup/2026-07-24_register-foundational-papers.md`; added `setup/2026-07-24_charter-and-scaffold.md` (charter + scaffold) and `setup/2026-07-24_hypotheses-checklist.md` (hypotheses/task checklist). setup now 3 entries.
