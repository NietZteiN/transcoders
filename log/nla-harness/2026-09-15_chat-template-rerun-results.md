### Target Date: 2026-09-15 (H-R7 results — the bake-off re-run with a working prompt: damage comes back, and nothing restores it)

- **Hypotheses / what we're testing:** exactly the rules frozen in [`2026-09-15_chat-template-rerun-prereg.md`](2026-09-15_chat-template-rerun-prereg.md), no additions. **H-R7·damage** — with the chat template applied inside *their* runtime, does the renaming deficit their method exists to repair actually exist? `DAMAGE-PRESENT` (`original − unsteered` > 0, 95 % CI excludes 0) / `DAMAGE-WEAK` (≥ +0.03, CI includes 0 → every downstream verdict provisional) / `DAMAGE-ABSENT`. **H-R7a** — `codesteer` and `codesteer_auto` vs `unsteered` at Bonferroni α/2: `CODESTEER-RESTORES` (≥ +0.03, 97.5 % CI > 0) / `CODESTEER-INERT` (|Δ| < 0.05, CIs include 0) / `CODESTEER-HARMS`. **H-R7b** = H-R2a verbatim (best-of-ours − better-of-theirs; `SURPASS` / `MATCH` / `BELOW`). **H-R7c** = H-R6a/b/c verbatim (`ridge_map − erasure`, `role_proto − foreign`, best latent − `prompt_types`, bar 0.05) — and `role_proto − foreign` is **H-R10's pre-registered primary**, moved onto this corpus before it was scored. **H-R7d** = H-R2b verbatim (`codesteer − rand_prior`; `SLICE-MATTERS` / `SLICE-IRRELEVANT`). **H-R7e** descriptive: chat − raw per arm, and `unsteered_chat` against the H-R1 harness. Predictions stated in the prereg: `DAMAGE-WEAK` and `CODESTEER-INERT`.
- **Setup:** one change from the H-R2/H-R6 runs — `install_chat_template` replaces `SteeredCausalLM._build_prompt` on the instance so their prompt is wrapped in CodeLlama's own chat template (`tok.add_bos_token = False`, literal `<s>` kept; the gate `tok(text)["input_ids"] == apply_chat_template(..., tokenize=True, return_dict=False)` passed on all 50 prompts, 1 293 spans aligned, BOS offset 0). Everything else is theirs and unchanged: `run_llama` → `_generate_with_attn`, their slice/AST prior, their sampler (T 0.7, top_p 1.0, top_k 7), 3 runs × 512 new tokens, `parse_predicted_labels` strict JSON, SEED 20260724, `--deterministic` off.
  Pool + vectors refitted under the template (`../../nla/configs/ase_vectors_chat.yaml`, job 403728 on g-08-11): 3 506 pool spans / 98 snippets, 1 293 test spans, ridge λ = 100 rank 256 held-out **cos 0.3791** vs mean-only 0.1751 (margin +0.2040, **gate PASS**), prototypes resolved `{type_role: 1214, type: 64, kind: 15}`. Residual arms: `PositionReplacer` layer 7, β = 1; every residual arm's self-gate PASSED (worst max|Δlogit| 0.0000, tol 0.05); attention arms' effect gate 4 096/4 096 effective level-2 calls, mean L1 shift 0.269.
  15 arms, `--partition=h200,h100 --gres=gpu:1`, three `--dependency=singleton` lanes (≤ 3 GPUs): 403728 pool · 403729 `unsteered` g-07-12 · 403730 `original_unsteered` g-08-03 · 403731 `ridge_map` g-08-03 · 403732 `role_proto` g-05-01 · 403733 `codesteer_auto` g-06-01 · 403734 `prompt` g-04-02 · 403735 `codesteer` g-04-02 · 403736 `rand_prior` g-04-02 · 403737 `uniform_prior` g-07-08 · 403738 `combined` g-06-01 · 403739 `swap_oracle` g-06-01 · 403740 `foreign` g-04-02 · 403741 `erasure` g-07-08 · 403743 `prompt_types` g-07-02 · 405637 `codesteer_beta0` g-04-02 (all 2026-09-15T15:12Z–22:37Z, logs `log/slurm/<jobid>_nla_r7_r7_lane*.out`).
  **Provenance of the two runner faults (below):** arms 1–14 ran `nla/src/ase_steer_run.py` sha `f99961e338fcebd8…`; the identity floor ran sha `cad05d864ac91eaa…` (the diff touches arm dispatch and the effect gate only — no code path any of the 14 arms takes). Scoring: `ase_bakeoff_stats.py` sha `383fe716…` `--scoring cn` (accuracy over 434 cases × 3 runs, case-weighted; unparsed = wrong), cluster bootstrap over the 50 snippets N_BOOT 10 000 seed 20260724 → `../../nla/results/2026-09-15_ase_bakeoff/2026-09-15_bakeoff_stats_chat_cn.json`; `ase_chat_vs_raw.py` sha `249f9990c49ed6ad…` → `../../nla/results/2026-09-15_ase_bakeoff/2026-09-15_chat_vs_raw_cn.json`.
- **Results:** accuracy `c/n`, 50 snippets / 434 cases × 3 runs. Chance = 0.500. Per-case parse = `pred.get(case_id) is not None` over cases × runs; acc|p restricted to parsed cases.

  | arm | NLA? | acc `c/n` | vs `unsteered` [95 % CI] | parse | acc\|p | chat − raw [95 % CI] |
  |---|---|---|---|---|---|---|
  | `original_unsteered` (clean code) | — | **0.7727** | +0.0584 [−0.0190, +0.1401] | 0.89 | 0.87 | +0.227 [+0.154, +0.300] |
  | `ridge_map` | ✔ | 0.7604 | +0.0461 [−0.0269, +0.1196] | 0.92 | 0.82 | +0.191 [+0.099, +0.288] |
  | `swap_oracle` (oracle) | ✔ | 0.7366 | +0.0223 [−0.0665, +0.1078] | 0.88 | 0.84 | +0.062 [−0.029, +0.154] |
  | `role_proto` | ✔ | 0.7281 | +0.0138 [−0.0574, +0.0910] | 0.96 | 0.76 | +0.127 [+0.062, +0.198] |
  | `prompt` (baseline) | — | 0.7273 | +0.0131 [−0.0547, +0.0842] | 0.91 | 0.80 | +0.186 [+0.096, +0.278] |
  | **`unsteered`** | — | **0.7143** | — | 0.90 | 0.79 | +0.118 [+0.023, +0.212] |
  | `codesteer_auto` (theirs, Eq. 10) | — | 0.7120 | −0.0023 [−0.0883, +0.0842] | 0.89 | 0.80 | +0.131 [+0.061, +0.203] |
  | `erasure` | ✔ | 0.7089 | −0.0054 [−0.0924, +0.0818] | 0.90 | 0.79 | +0.142 [+0.050, +0.236] |
  | `combined` | ✔ | 0.6982 | −0.0161 [−0.0963, +0.0675] | 0.88 | 0.80 | +0.149 [+0.046, +0.257] |
  | `codesteer` (theirs, README) | — | 0.6974 | −0.0169 [−0.1063, +0.0753] | 0.88 | 0.79 | +0.148 [+0.067, +0.230] |
  | `uniform_prior` | — | 0.6959 | −0.0184 [−0.0790, +0.0429] | 0.89 | 0.78 | +0.134 [+0.053, +0.220] |
  | `foreign` (wrong-item control) | ✔ | 0.6851 | −0.0292 [−0.0962, +0.0417] | 0.88 | 0.78 | +0.162 [+0.078, +0.250] |
  | `codesteer_beta0` (identity floor) | — | 0.6767 | −0.0376 [−0.1093, +0.0401] | 0.85 | 0.80 | +0.108 [+0.029, +0.189] |
  | `rand_prior` | — | 0.6697 | −0.0445 [−0.1193, +0.0267] | 0.83 | 0.81 | +0.113 [+0.021, +0.203] |
  | `prompt_types` (prompt ablation) | — | 0.5707 | −0.1436 [−0.2267, **−0.0519**] | 0.77 | 0.74 | +0.192 [+0.102, +0.286] |

  Frozen contrasts, verbatim from the scorer's summary:
  - `H-R7 damage DAMAGE-WEAK: original − unsteered = +0.0584 [−0.019, +0.1401]` (α/3 interval [−0.0343, +0.1595])
  - `H-R7a CODESTEER-INERT: codesteer −0.0169 adj [−0.1153, +0.0859] · codesteer_auto −0.0023 adj [−0.1000, +0.0954]`
  - `H-R2a MATCH-CODESTEER: prompt − codesteer_auto = +0.0154 95 % [−0.0632, +0.0971] adj [−0.0785, +0.1166]` (= H-R7b)
  - `H-R2b SLICE-IRRELEVANT: codesteer − rand_prior = +0.0276 [−0.0571, +0.1122]` (= H-R7d)
  - `H-R6a MAP-NOT-BETTER: ridge_map − erasure = +0.0515 [−0.0206, +0.1293]` · `H-R6b CATEGORY-MEANING-INERT: role_proto − foreign = +0.0430 [−0.0218, +0.1174]` · `H-R6c LATENT-BEATS-PROMPT: best latent − prompt_types = +0.1897 [+0.1051, +0.2722]` (= H-R7c)
  - `H-R9 scoring=cn floor_gate=PASSES (codesteer_beta0 −0.0376; tol ±0.05)` · `codesteer_auto − codesteer = +0.0146 [−0.0548, +0.0870]`
  - H-R2c restoration ratios are again unreadable on a +0.058 denominator (`ridge_map` 78.9 % [−351, +506], `codesteer` −28.9 % [−956, +588]); reported, no reading taken.
  - **H-R7e anchor:** `unsteered_chat` 0.7143 and `original_chat` 0.7727 against the H-R1 harness's 0.714 / 0.780 on the same 50 programs (top_k 50 there vs their 7) — a 0.000 / −0.007 difference.
- **What worked / hypothesis verdict:**
  - **H-R7·damage — `DAMAGE-WEAK` (as predicted).** +0.0584 ≥ the +0.03 bar but the CI includes 0, so **every verdict below is provisional by the frozen rule**. The sign is nevertheless repaired: their raw prompt gave −0.0507 (renamed code scoring *higher*), the template gives +0.0584.
  - **H-R7a — `CODESTEER-INERT` (as predicted).** Both readings of their method sit on top of doing nothing: `codesteer` −0.0169, `codesteer_auto` −0.0023, both α/2 CIs spanning 0, and neither reading beats the other (+0.0146). The effect gate proves the steering ran (4 096/4 096 rows changed, L1 shift 0.269), so this is real steering with no effect, not another identity run.
  - **H-R7b — `MATCH-CODESTEER`.** `prompt − codesteer_auto` = +0.0154, α/3 CI [−0.0785, +0.1166]. A one-line prompt instruction equals their attention-steering machinery, now on an undamaged prompt.
  - **H-R7c — `MAP-NOT-BETTER` (+0.0515 [−0.0206, +0.1293]) · `CATEGORY-MEANING-INERT` (+0.0430 [−0.0218, +0.1174]) · `LATENT-BEATS-PROMPT` vs `prompt_types` (+0.1897) but `ridge_map − prompt` = +0.033 [−0.036, +0.098] against the intact baseline, so the conservative `PROMPT-SUFFICES` reading is the one carried.** Every H-R6 verdict word survives the change of prompt form.
  - **H-R10 ✗ NOT SUPPORTED as posed.** Its pre-registered primary `role_proto − foreign` moved onto this corpus is +0.0430 [−0.0218, +0.1174] — *smaller* than the +0.0776 that raised it, and still short of the 0.05 bar with a CI that includes 0. Installing a role/type meaning is not measurably better than installing a wrong item's meaning.
  - **H-R7d — `SLICE-IRRELEVANT`.** +0.0276 [−0.0571, +0.1122]: their AST/slice prior buys nothing over a random prior of matched mass, the same verdict as at raw prompts.
  - **H-R7e — the runtime was the whole story.** Every one of the 15 arms gains under the template, +0.108 to +0.227, and parse rises 0.77–0.96 from 0.54–0.86. `unsteered_chat` reproduces our independent H-R1 harness to 0.000, so their sampler (top_k 7) was never the issue.
  - **The H-R9 floor gate PASSES** (−0.0376, tol ±0.05), so the intervals above are read as sampling noise around a real zero rather than a code-path artefact.
- **Observations:**
  - **The precondition is now satisfied about as well as this host allows, and the answer does not change.** At raw prompts one could argue CodeSteer had nothing to repair (0.545 vs chance 0.500, damage inverted). With the template CodeLlama-7B-Instruct sits at 0.714/0.773 with a +0.058 deficit in the right direction — and their method still returns −0.017/−0.002. The programme's question ("can we match or surpass CodeSteer") has the deflationary answer on this host: **everything matches it, because it does nothing.** Our best steered arm `ridge_map` (+0.046) is the largest positive in the table and still cannot clear zero on 50 snippets.
  - **Two runner faults, both mine, both in the identity-floor arm only.** (1) 403742 died rc=2 in 10 s: `--arm codesteer_beta0` was never a valid choice — in the raw-prompt bake-off that arm existed only as a *consequence* of the 2026-09-14 `beta_post` fault, and the guard added after that fault (`assert cfg["beta_post"] > 0`) refuses β = 0 by design. Fixed by asking for the floor explicitly. (2) 405553 then died rc=3 on `Java_000` with `l2_calls=4096, l2_effective_calls=0` — the effect gate treating "changed nothing" as fatal, which is exactly what the floor arm is *for*. The gate now reads two-sided: the floor fails if it changes any attention row (that would mean β_post = 0 does **not** neutralise level-2, and the raw-prompt floor was mislabelled), real arms still fail on zero. It passed with 0 effective calls on all 50 snippets, which independently confirms the raw-prompt floor's label.
  - **`prompt_types` was mostly a prompt-form artefact.** Its raw 0.379 at parse 0.54 (read then as "the baseline collapses on format") is 0.571 at parse 0.77 under the template. The `LATENT-BEATS-PROMPT` margin it anchors (+0.19) is therefore still a measurement against a *degraded* baseline, and the intact `prompt` arm remains the honest comparator.
  - **`swap_oracle` gains least from the template (+0.062, CI ∋ 0)** — expected: it is the one arm already writing the clean state, so it had least to lose from a bad prompt. Its +0.022 over `unsteered` is the ceiling an oracle reaches here, which bounds what any non-oracle steering could deliver on this corpus to roughly the width of one CI.
  - **Power, stated plainly:** a ±0.07–0.09 CI half-width against a +0.058 damage ceiling means no arm in this table can be separated from `unsteered` in either direction (only `prompt_types`' −0.144 clears). 50 snippets is the binding constraint, not the steering method.
- **New questions / new hypotheses:**
  - **H-R12 (raised).** Every verdict here is provisional because the damage CI includes 0. The fix is corpus size, not method: at the observed per-snippet variance, separating a +0.05 effect needs ≈ 150–200 snippets (≈ 3–4× this corpus). Pre-register a power calculation from these 50 snippets' bootstrap distribution *before* extending the corpus, and state the smallest effect the extended corpus could certify.
  - **H-R13 (raised).** `ridge_map` is the top steered arm on both prompt forms (+0.046 chat, +0.025 raw over `erasure`) and its held-out cos is unchanged by the template (0.3791 vs 0.38). It is the only arm whose point estimate never goes the wrong way. Worth one honest replication at a second seed before any claim: `RIDGE-REAL` if the paired Δ vs `unsteered` stays ≥ +0.03 at a fresh sampling seed, `RIDGE-NOISE` otherwise.
  - **H-R11 stands** (does attention fixation, the mechanism CodeSteer's prior encodes, fire on CodeLlama at all?) and is now the more diagnostic question: an inert steering method on a model whose attention never fixates is expected behaviour, not a failed replication. That is Instrument-1 territory.
- **Next Steps:** update `../../reports/2026-09-15_ase-bakeoff/REPORT.md` with this table as the headline result and keep the raw-prompt table as the runtime comparison; mark H-R7 and H-R10 in `../../CHECKLIST.md`; H-R12's power calculation before any corpus extension.
