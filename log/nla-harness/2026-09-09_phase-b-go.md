### Target Date: 2026-09-09 (Phase B go/no-go: 4B accuracy in band → 34-layer array submitted)
- **Hypotheses / what we're testing:** the pre-registered go/no-go from [`2026-09-09_multilayer-train-prereg.md`](2026-09-09_multilayer-train-prereg.md): the Gemma-3-4B-it host's L0 accuracy on the 60 W items must lie within **0.633 ± 0.15** (= [0.483, 0.783]) of the 12B's, so the 4B corpus retains a usable flippable set and the same readout; and the 40-doc end-to-end smoke (job 384622) must pass every stage. Both are decision rules, not hypotheses.
- **Setup:** job **384619** (`nla/scripts/nla_ml_traces.sh`, h100 g-06-01, `nla/src/host_traces.py --model gemma4b`, greedy, `MAX_NEW_GEN` as banked) → `data/nla/p0/trace_llr/gemma4b/{traces.jsonl,traces_summary.json}`; host `google/gemma-3-4b-it` through the multimodal `AutoModelForCausalLM` path (hidden states verified bit-identical to the text-only checkpoint, [`2026-09-09_multilayer-train-prereg.md`](2026-09-09_multilayer-train-prereg.md)). Smoke stages of 384622 as recorded in [`2026-09-09_multilayer-gate-harness.md`](2026-09-09_multilayer-gate-harness.md).
- **Results:** `n 60 · acc_l0 0.5667 · acc_l1b 0.5333 · flippable 7` (`humaneval-x-python/Python/100`, `JavaScript/63`, `humaneval-x-javascript/JavaScript/104`, `humaneval-x-python/Python/101`, `cruxeval-x-javascript/108`, `cruxeval-x-python/101`, `leetcodedataset/assign-elements-to-groups-with-constraints`) · `n_tok_l0_median 503.5` · finished `2026-09-09T05:44:34Z`. For reference the 12B: acc_l0 0.633, 6 flippable.
- **What worked / hypothesis verdict:** **GO.** 0.567 ∈ [0.483, 0.783]; the 40-doc smoke passed all stages. Submitted, in order and each `--dependency=afterok` on its predecessor: `nla/scripts/nla_ml_layer.sh` → **array 384655** (`afterok:384636`, 0-33, h200, 8 h each: `sft_av → sft_ar → check` per layer); `nla/scripts/nla_ml_vectors.sh` → **array 384656** (`afterok:384655_*`, 0-33, gate `--stage vectors --layer K`); `nla/scripts/nla_ml_score.sh` → **job 384657** (`afterok:384656_*`, gate `--stage score`, exit 3 = SELF identity failure). Verified with `scontrol`: `384655 Dependency=afterok:384636(unfulfilled)`, `384656 Dependency=afterok:384655_*(unfulfilled)`, `384657 Dependency=afterok:384656_*(unfulfilled)`. Full chain: 384622 → 384635 → 384636 → 384655 → 384656 → 384657.
- **Observations:** the 4B's L1b penalty is small (0.567 → 0.533, 7 flippable vs 6 on the 12B) — the same weak-denominator caveat as H-W11 applies to any accuracy statement on this host; the gate's primary readout is `G_sum`, not accuracy, by design. The gate smoke (384634) is still queued on resources; it is *not* upstream of the chain. If it fails, 384656/384657 are cancelled and resubmitted after the fix — they cannot start before 384655 finishes anyway (≥ several hours).
- **New questions / new hypotheses:** none.
- **Next Steps:** watch 384634; watch 384622's extract → 384635 → 384636; the layer array's `check.json` files decide liveness per layer (to be tabulated in the results entry before any verdict). Results entry when `data/nla/ml/gemma4b/gate/gate_stats.json` lands.
