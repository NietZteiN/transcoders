### Target Date: 2026-09-14 (H-R2 harness — two integration faults in wrapping their runtime, both fixed; steering has still not produced a number)

- **Hypotheses / what we're testing:** none new — this is a **harness entry**. It records why the H-R2 bake-off ([`2026-09-14_steering-bakeoff-prereg.md`](2026-09-14_steering-bakeoff-prereg.md), arms amendment [`2026-09-14_bakeoff-amendment-arms.md`](2026-09-14_bakeoff-amendment-arms.md)) had, by the end of the day, **produced zero steering results**, so that the timeline is honest about it: the replication (H-R1, [`2026-09-14_ase-replication-results.md`](2026-09-14_ase-replication-results.md)) is done; the comparison the user asked for is not. No threshold changes.
- **Setup — what failed, in order:**
  1. **Jobs 398922–398926 (all five arms), model load.** Their `models.DEFAULT_CACHE_DIR` is the *relative* path `.cache/models`; under `HF_HUB_OFFLINE=1` their `build()` raised `LocalEntryNotFoundError` although CodeLlama-7b-Instruct is in the hub cache. Fix: `ase_steer_run.py` passes `cache_dir=$HF_HOME/hub` to their `config()`. Batch cancelled before any spent GPU time beyond the load.
  2. **Job 399325 (smoke, steering ON), first generation.** `ValueError: No layer vectors to pool.` from their `attn_postprocess._pool_layer_vectors`. Root cause, read off `models.py:688-689` and `:744`: `run_llama` defaults `record_layers=True` → `record_attention=True` → `generate(output_attentions=True)`; under transformers **5.12.1** the per-step attention tuple comes back **empty** even with `attn_implementation="eager"`, so the post-processor receives an empty list. Their own CLI carries `--record-layers off` for exactly this path (`obfuscation/main.py:248`); my wrapper never passed it. **The steering hooks live inside the forward pass (`_install_steering_hooks`) and never touch the recorder**, so disabling recording changes nothing about the intervention. Fix: `run_llama(..., record_layers=False, record_attention=False)`.
  3. **Liveness proof added**, because a steered arm whose hooks never fire is an unsteered arm wearing a label: every run now stores `steering_debug.steer_calls` and `enabled`; a steered arm with `steer_calls == 0` on any snippet **exits 3** rather than writing a row. The unsteered control shares the identical object with `steering_config=None`.
  - **Not chosen:** driving their `obfuscation/main.py` directly with a `<root>/<snippet>/identifier_renaming/*.java` corpus. It would have been their tested path, but it (a) rebuilds packs per variant (already banked here), (b) writes the full per-run artifact tree, and (c) needs `--record-layers off` for the *same* transformers-5 reason — so it would have been the same fix in a heavier wrapper. If the smoke below fails inside their steering code itself, that path is the fallback.
  - `nla/src/ase_steer_run.py` sha256 `05d7d50e91f82fec…`; smoke `nla/scripts/nla_r2_smoke_sbatch.sh` = 2 snippets × 1 run, steered (`slice_hybrid`) **then** unsteered, both must exit 0. **Job 399430**, submitted 2026-09-14, pending behind the shared 5-job cap (obtune's `tr_*`/`ev_*` batch holds it).
- **Results:** none yet — the smoke is queued. Filled in by the next entry.
- **What worked / hypothesis verdict:** n/a. What did **not** work is the choice to wrap their runtime rather than drive it: two integration faults cost the day's steering budget, and I reported the ASE replication as progress toward "match CodeSteer" when it was progress toward *understanding* CodeSteer's number. Those are different deliverables and the user asked for the first.
- **Observations:** the retracted docstring hypothesis is also recorded here so it is not re-raised: their `build_prompt_source_view` (strips comments) has **no call site**, so their model sees the NL docstring exactly as ours does; the 3× damage gap between their Qwen2.5-7B (+0.363) and our CodeLlama-7B (+0.113) is not explained by prompt content. Remaining candidates unchanged: model family (untestable) and renamer strength (H-R3, unrun).
- **New questions / new hypotheses:** none.
- **Next Steps:** smoke 399430 → if both passes exit 0 with `steer_calls > 0` on the steered pass, submit the five arms (`unsteered`, `codesteer`, `rand_prior`, `uniform_prior`, `prompt`) via `nla_r2_arm_sbatch.sh` on `gate/packs_ren_subset.jsonl` (50 snippets, 3 runs each); then the NLA residual arms once the Llama-architecture capture/write pass exists.
