### Target Date: 2026-08-04 (G0 replication PASS + out-of-the-box code-reads probe)
- **Hypotheses / what we're testing:** **HT-probe (feasibility):** the pretrained NLA pair (Qwen2.5-7B-Instruct L20) produces theme-informative reads on CODE out of the box — obfuscated-code identifiers, dispatcher state, and CoT tokens. CONFIRM: reads verbalize the actual computation (recurrence at `fibfib`, state-machine at dispatcher vars). REFUTE: generic/confabulated reads unrelated to the code. (The full N1–N3 faithfulness program is designed but **parked downstream** at the user's direction — this probe is its go/no-go.)
- **Setup:** Host `csr-94608`, env `nla-mi`, GPUs 0 (extractor+AR) / 1 (SGLang AV server), seed 20260724. Fixes applied first: (1) `nla_inference.py:187,403` `return_dict=False` (transformers 5.12 break) → [`../../nla/patches/transformers512_chat_template.patch`](../../nla/patches/transformers512_chat_template.patch); (2) `replicate_example.py` reply layout was missing the trailing `<|im_end|>` (reference position 100 is that token — 66 text + end-of-turn = 67 reply tokens); (3) `pip install ninja` into nla-mi (flashinfer JIT crash). Commands: `python -m src.replicate_example --stage A|B`; server `sglang.launch_server --model-path nla/data/checkpoints/av --disable-radix-cache --mem-fraction-static 0.6`; probe `python -m src.probe_code_reads --device cuda` (new script). Probe stimuli from `data/stimuli/dataset_a/dataset_a.jsonl`: flagship JavaScript/63 L0+L1b, Python/31 L2, + the model's own greedy CoT on the L1b trap. 44 reads total; AV temp 0.
- **Results:**
  - **Stage A PASS:** 101/101 tokens, median rel err on ‖v‖ **0.00121**, max 0.0041, 0 over tol — extraction chain (layer 20 = `layers[20]` hook = `hidden_states[21]`, chat template, reply layout) exactly matches the reference transcript.
  - **Stage B PASS:** mean round-trip cos **0.8501 vs reference 0.8494**, median |Δcos| **0.0037** (tol 0.10); CJK 3/101 (reference-consistent; not injection failure).
  - **Probe (44 reads, 0 CJK, round-trip cos 0.70–0.96 on code — comparable to chat text, so code is not catastrophically OOD):**
    - **L0 `fibfib`:** every identifier read names Fibonacci/recursion; pos 137 reconstructs the recurrence nearly verbatim ("fibfib(n) = fibfib(n-1) + fibfib(n-2)").
    - **L1b `smoothArea`:** position-resolved decoy/true mixture — at the definition site the reads adopt the decoy ("smoothing algorithm", "Moving Average", "sensor readings", "audio volume meter"); at recursive call sites the true structure surfaces ("smooth(n-1)+smooth(n-2)", "three recursive calls (n-1),(n-2),(n-3)").
    - **DRM_AR (cos to decoy gloss − cos to true gloss, AR space):** L0 **−0.102**, L1b **−0.055**, CoT **−0.072** — all true-leaning in absolute sign (gloss anchors crude), but the decoy shifts L1b **+0.047 toward decoy semantics vs L0**, the E1/HT9-sub prediction, with per-position gradient (trap-name tokens reach +0.01/+0.02).
    - **L2 dispatcher (`state_1395`):** word-token reads correctly say "state machine", "iterative state transitions", "while count < threshold"; **digit-piece tokens read as numerology** (noise — exclude from quantitative sets).
    - **CoT reads:** track the reasoning structure ("numbered steps", "base cases", "expanded expression fib(4)=…") — L20 during generation encodes the ongoing reasoning and the NLA reads it. The model's CoT itself traces the 3-term recursion correctly (not surface-fooled on this item).
- **What worked / hypothesis verdict:** **HT-probe ✓ SUPPORTED.** Reads are theme-accurate on code at every level probed; tier contrast moves the predicted direction; CoT reads are reasoning-tracking. The N1–N3 program is feasible with the existing checkpoints — no fine-tuning needed.
- **Observations:** Confabulation is exactly as documented — themes reliable, specifics not (JS read as "Python", invented `Math.js`/memoization). Ops: server cold-start ~85 s incl. flashinfer JIT; sequential read ≈ 1.5 s. Server stopped after the probe (shared box).
- **New questions / new hypotheses:** (1) Does the definition-site vs call-site decoy/true gradient replicate across the 20 A-L1b items (cheap: ~240 reads)? (2) Better DRM anchors — contrastive activations instead of AR-glosses? (3) Do CoT reads *diverge* from the stated CoT on items the model gets wrong (the N1 question proper)?
- **Next Steps:** Un-park N1 when ready (design + gates in the plan file; HT9–HT11 to be promoted into `docs/CHECKLIST.md`). Before any >1k-read run: async AV client + add `ninja` to `nla/environment.yml`.
