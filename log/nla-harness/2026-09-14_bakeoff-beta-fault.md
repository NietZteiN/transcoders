### Target Date: 2026-09-14 (HARNESS FAULT + AMENDMENT to H-R2 — the first `codesteer`/`rand_prior` arms were identity steering; fix, effect gate, and a second paper arm, all before any steered number enters a verdict)

- **What was seen.** Lanes B and C finished their steered arms: `codesteer` (job 399523) and `rand_prior` (job 399524) on the frozen 50 snippets / 434 cases gave Pass@1 **0.5991 and 0.5991** — and not by coincidence: **150/150 generations are byte-identical** between the two files (same `torch.manual_seed(20260724)`, same sampling path). Two arms whose only difference is the attention prior cannot agree on every token unless the prior is never applied.
- **Cause, verified in their code.** `steering/levels.py:16-18`: `def level2_post(attn_probs, prior, beta, …): if beta == 0.0: return attn_probs`. My `PAPER_CFG` in `nla/src/ase_steer_run.py` set `beta_post=0.0` — I had copied the **`SteeringConfig` dataclass default** (`steering/config.py:27`) and mis-read it as the paper's setting. The paper states it in words ("if β = 0 … the steering operator reduces to the identity", §Method) and the README's reproduction command states it in numbers: `--steer --prior slice_hybrid --beta-post 0.8 --steer-last-n-layers 8 --head-subset-mode none`. So both arms were **unsteered generations wearing steered labels**. The liveness gate I wrote (`steer_calls > 0`, meant to catch exactly this) was vacuous: `llama_backend.py:250` increments `runtime.steer_calls` **before** the β check, so it read 1 352 calls/run on the smoke test and on the identity runs alike. `mark_layer_steered` (the flag that does depend on β ≠ 0, `llama_backend.py:252-253`) never reaches `steering_debug` and the runtime object is discarded after each generation (`models.py:752`), so it cannot be read post hoc either.
- **What the identity runs are worth.** They are the **noise floor** of this design: the same model, prompts, seed and split-prefill code path as `unsteered`, with the level-2 hook installed but inert. Paired against `unsteered`: **+0.0668 [−0.0777, +0.2101]** (n = 50). Two readings follow. (i) A steered arm must beat *this*, not zero — the ±0.14 half-width is what 50 snippets × 3 sampled runs at T = 0.7 buy; H-R2a's frozen `MATCH_TOL` 0.05 is well inside it, so a `MATCH-CODESTEER` verdict on the α/3 interval is the likeliest outcome for any arm and will be reported as such, not as evidence of equivalence. (ii) The +0.067 is *not* zero even though the steering is: it is the split-prefill path (`split_prefill=True` runs the prompt in two chunks, changing the KV-cache numerics) plus sampling variance. Kept, renamed with `mv` (nothing deleted): `bakeoff_codellama7b/codesteer_beta0.jsonl`, `rand_prior_beta0.jsonl`. They enter **no verdict**.
- **Fix (`nla/src/ase_steer_run.py`, sha256 `60f5c61ad80c8f4e…`, previous `4c5d…` per job logs):**
  1. `PAPER_CFG["beta_post"] = 0.8`; `steer_layer_start/end = num_hidden_layers − 8 … num_hidden_layers − 1` set after `build()` exactly as `obfuscation/main.py:378-388` does (CodeLlama-7B: layers 24..31); an `assert cfg["beta_post"] > 0.0` so this cannot recur silently.
  2. **Effect gate replaces the liveness gate.** `Level2Effect` wraps `level2_post` in `steering.backends.llama_backend`'s namespace and counts the applications whose output attention **differs from the input** (per-row L1 shift > 0); any generation of a steered arm with zero effective calls exits 3 (`S-HARNESS-FAULT`, nothing reportable). Unit-checked against their real module: β = 0 → 0 effective of 1 call; β = 0.8 → 1 of 2, mean L1 shift 0.486 on random rows, rows still sum to 1. Every run row now records `l2_calls`, `l2_effective_calls`, `l2_mean_l1_shift`, `head_mask_active_total`.
  3. **Second paper arm `codesteer_auto`.** The paper's method text describes a *calibrated sparse head subset* (Eq. 10, ~¼ of heads); its README command uses `--head-subset-mode none` (all heads). Both are "their method". `codesteer` = README-exact; `codesteer_auto` = `head_subset_mode="auto"` with their per-snippet `lm.calibrate_head_subset(code_snippet, instruction, language="java", vocab_tokens=[], snippet_name)` (`obfuscation/main.py:454-460`; top-4 heads/layer over the same 8-layer window, 3 sampled 64-token calibration runs with β forced to 0). `rand_prior`, `uniform_prior`, `combined` take the same β/window.
  4. `nla/src/ase_bakeoff_stats.py` (sha256 `6366f534977bb9ff…`): `STEERED` gains `codesteer_auto`; `CONTROLS = (codesteer_beta0, rand_prior_beta0)` are reported paired vs unsteered as the noise floor only; **H-R2a's comparator is the better of `codesteer` / `codesteer_auto` by Pass@1** (conservative against us — our arm must beat their best reading); H-R2b keeps `codesteer − rand_prior`; a descriptive `codesteer_auto − codesteer` line is added.
- **Hypothesis status.** Nothing resolved. H-R2a/b/c, H-R4 (ASE) unchanged in rule; **the comparator in H-R2a is amended as in (4)** before any steered file exists. Prediction added for the record: on this noise floor, `codesteer − unsteered` will land inside ±0.14 and H-R2b will read `SLICE-IRRELEVANT` (the paper's own ablations report the prior choice mattering by a few points, below this design's resolution).
- **Setup / lanes (≤ 3 GPUs at once, chained with `afterany`):** running/queued from before: `prompt` 399686, `erasure` 399688, `swap_oracle` 399685 (independent), `original_unsteered` 399863 ← 399685, `foreign` 399687 ← 399686. Resubmitted today: `codesteer` **400250** ← 399688, `codesteer_auto` **400251** ← 399863, `rand_prior` **400252** ← 399687, `uniform_prior` **400253** ← 400250, `combined` **400254** ← 400251. Cancelled earlier: 399684 (`uniform_prior`, β = 0) and 399689 (`combined`, β = 0) before they started.
- **Next Steps:** read nothing until all files exist; the scoring command is unchanged (`--original …/original_unsteered.jsonl`). Results entry reports the noise floor first, then H-R2b, H-R2a, H-R2c, H-R4.
