### Target Date: 2026-09-09 (gate re-smoke 384664: same-shape capture makes SELF bit-exact — harness cleared for the array)
- **Hypotheses / what we're testing:** the prediction made in [`2026-09-09_gate-smoke-self-drift.md`](2026-09-09_gate-smoke-self-drift.md): if the +1-nat SELF drift was capture-shape numerics (vectors captured from a prompt-only forward, written into a prompt + reply forward), then capturing from the score-shaped sequences makes `S_self_L5` ≈ 0 on all three smoke items while every other arm is unchanged. REFUTE if any item's `dG_SELF` stays ≳ 0.1 nats, or if the non-SELF arms move by more than bf16 noise.
- **Setup:** job **384664** (`nla/scripts/nla_ml_gate_smoke.sh`, h200 g-07-13, 2026-09-09 11:50–11:54Z; same pipeline as 384634). `nla_ml_gate.py` sha256 **`1f7a9d3191a9ce328d5789abd3e451090c43759f3b72b99cb673d2113079c419`** (recorded in the manifests as `gate_script_sha256`; the manifests' `script_sha256`/`config`/`config_sha256` keys are the *trainer's* — `nla_train.py` `b924ff21…`, `nla_ml.yaml` `b31f5e27…` — i.e. the dictionary-training provenance, inherited through `nla_train.provenance`), gate config `c7c1d9e3ddc1ddde…`, seed 20260724, smoke pair `data/nla/ml/gemma4b_smoke/L5`, 3 items, `--ignore-liveness`.
- **Results:** vectors: 22 spans / 3 items, editable 1, `cos(h0,c3) 0.951 · cos(h1b,rt) 0.952 · cos(h0,h1b) 0.954` (384634: 0.951 / 0.953 / 0.954). Score (48 forwards, 172 ms/forward): `G_prompt_swap +174.11` (identical — it does not involve any write); L5 `swap +86.02 · c3 +34.31 · edit +34.81 · random −75.47 · foreign +34.49 · rt +34.93 · self +0.000` (384634: +85.95 / +34.35 / +34.82 / −75.47 / +34.73 / +34.78 / **+0.712**). **Per-item `dG_SELF`: 0.0, 0.0, 0.0** on `cruxeval-x-python/102`, `humaneval-x-python/Python/100`, `humaneval-x-javascript/JavaScript/1` (384634: +1.123, +1.015, 0.000). Verdict line unchanged: `NO-PRIMARY-SET · identity passes · reportable=False`, exit 0.
- **What worked / hypothesis verdict:** **SUPPORTED.** SELF is now an exact identity on every item, and every other arm moved by ≤ 0.44 nats (foreign +34.73 → +34.49; the vectors themselves are re-derived from states that differ by bf16 kernel noise, so this is the expected size). The random arm is identical to two decimals because its vector is seeded and norm-matched to the live state. The harness is cleared for the 34-layer vector array (384656) and the score job (384657), which call this file from disk.
- **Observations:** the two numbers that matter for budgeting: 172 ms/forward on the 4B at T 320–1070, and the forward count — per item 2 references + 7 × (live layers) singles + 7 × 3 set arms ≈ 261 at 34 live layers → 60 items ≈ 15.7 k forwards ≈ 45 min plus loading, far inside the 7 h `--max-hours`. The vector stage is AV-decode-bound (≈ 2.4 min for 22 spans at L5 with the 4B AV → ≈ 50 min for ~470 spans per layer, 34 layers in parallel 4-wide under the QOS cap). A side note for the eventual results entry: the dead 40-doc AR's near-mean vector recovering 40 % of the L5 swap (`c3 ≈ rt ≈ edit ≈ foreign ≈ +34.5`) is decoy *removal* at a second host and a fifth of the depth — the same ≈ 49 % component W measured at L32 on the 12B. It is a smoke observation on 3 items with `--ignore-liveness`, not a result; the real per-layer `S_rt_ℓ` column of the gate will say whether it holds at 60 items across depth.
- **New questions / new hypotheses:** **H-M4 (descriptive, no verdict word):** `S_rt_ℓ / S_swap_ℓ` — the share of the swap recovered by writing back the *unedited* round trip of the L1b state — as a depth profile. If it tracks `S_c3_ℓ / S_swap_ℓ`, the NLA round trip is removing the decoy rather than transporting the truth at that layer; the gap `S_c3_ℓ − S_rt_ℓ` is the clean-content contribution per layer. Falls out of the banked arms at zero cost.
- **Next Steps:** nothing to change in the queued chain. Explain shards 0–6 complete (all exit 0), 7–9 running; then join 384636 → layer array 384655 (4-wide) → vectors 384656 → score 384657. Results entry when `gate_stats.json` lands, liveness table first.
