### Target Date: 2026-08-04 (Overnight capture run LAUNCHED — 380 tasks × rich read package)
- **Hypotheses / what we're testing:** Exploratory collection at scale (approved plan in the plan file): the example package (graded greedy answer + identifier/dispatcher/CoT/answer-line NLA reads + DRM_AR at trap positions) over **problems × task kinds × all 5 obfuscation tiers**, unsupervised overnight.
- **Setup:** New rig: [`../../nla/src/task_bank.py`](../../nla/src/task_bank.py) (call shim incl. renamed-fn mapping + code-derived fn fallback for leetcode's `myFunct` placeholder; 30 seeded synthetic slicing tasks with ground truth by construction + stated print-line convention; DRM gloss builder), [`../../nla/src/overnight_capture.py`](../../nla/src/overnight_capture.py) (resumable fsync'd JSONL; per-task try/except; one server auto-restart; CJK>5%/100 abort; 11 h wall guard; summary+manifest always written), [`../../nla/scripts/overnight.sh`](../../nla/scripts/overnight.sh) (2-idle-GPU gate, `setsid` server + pgid/port-scoped cleanup trap). Task array: **300 output-prediction** (50 A-js + 250 B — full call coverage after 3 shim iterations), **50 prompt-read packages** (python-A rows verified to carry NO ground truth — design-agent finding), **30 synthetic slicing**. Order: flagship problem → A problem-major (tiers L0→L1b→L2→L3→L1, complete matched pairs early) → py-A → slicing → B shortest-first. Seed 20260724, greedy, max_new 1100.
- **Results (at launch):** Driver smoke (--limit 4) passed end-to-end; **resume proven** (rerun → "4 already done, 0 to run"); smoke rows correct incl. teaser: **flagship L0 ✅ (fibfib(14)=927 solved at max_new 1100!) vs L1b ❌ on the same problem** — the trap live, with 6 DRM reads. One driver bug found+fixed in smoke: server launched in the driver's process group made cleanup unsafe and sglang scheduler children survived — now `setsid` + pgid kill + port-scoped pkill fallback. Full run launched **2026-08-04 evening** in tmux `nla-overnight` (runner GPU 0, AV server GPU 1); monitor armed (50-task heartbeats + failure signatures).
- **What worked / hypothesis verdict:** N/A at launch — results entry to follow in the morning.
- **Observations:** Diagnosis trap worth remembering: `pgrep -f sglang` matches the probe's own wrapper shell — check the *port* (`ss -ltnp`) not the pattern. Design-agent contribution materially improved the run: python-A ungradeability, renamed-fn call mapping, L0-first ordering.
- **New questions / new hypotheses:** Morning analysis: per-tier accuracy curve vs Papers 2–3 shape; DRM_AR by tier at scale (probe showed +0.047 L1b-vs-L0 shift, n=12); do answer-line reads echo wrong answers systematically?
- **Next Steps (morning checklist):** `tmux capture-pane -pt nla-overnight | tail`; `wc -l captures.jsonl` ≈ 380; `summary.md` (accuracy table sane, CJK 0, errors ~0); `nvidia-smi` clean + port 30000 closed; if PARTIAL → rerun `overnight.sh` same dir to finish tail; then the results log entry + gallery regeneration from the new data.
