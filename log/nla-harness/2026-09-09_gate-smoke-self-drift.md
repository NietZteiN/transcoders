### Target Date: 2026-09-09 (gate smoke 384634: mechanics pass, SELF drifts +1 nat from capture-shape numerics → same-shape capture)
- **Hypotheses / what we're testing:** harness check, pre-data (no trained pair exists; the smoke pair is the 40-doc L5 AV/AR from job 384622, whose AR reconstructs nothing — fve −1.007). Question: does `nla_ml_gate.py` run end-to-end on real 4B states with the identities holding? Secondary: is the SELF identity exact, as it was on the 12B (banked −0.018 nats)?
- **Setup:** job **384634** (`nla/scripts/nla_ml_gate_smoke.sh`, h200 g-07-09, 2026-09-09 05:53–05:56Z): 18 pytest pass (gate + arm_guard, 34.8 s) → `--stage vectors --layer 5 --root data/nla/ml/gemma4b_smoke --smoke` (3 items) → `--stage score --smoke --ignore-liveness`. Script sha at run `2c7a03312ac2140f…`, config `c7c1d9e3ddc1ddde…`.
- **Results:** vectors: 22 spans / 3 items, editable 1 (`cos(h0,c3) 0.951 · cos(h1b,rt) 0.953 · cos(h0,h1b) 0.954` — L5 anisotropy). Score, 3 items, 48 forwards in 0.1 min: `G_prompt_swap +174.11 [146.9, 192.6]`; L5 single arms `swap +85.95 · c3 +34.35 (ratio 0.40, CI lo 0.26) · edit +34.82 · random −75.47 · foreign +34.73 · rt +34.78 · self +0.712`; set `all=[5]`: gate0 0.197 [0.131, 0.246] FAIL, H-M1 +0.00 [0,0] (one layer — the single is the multi), H-M2 +110.29 vs thr +10.31, spec +0.09 → `M-GATE0-FAIL`; primary/depth sets empty (only L5 has vectors) → `VERDICT NO-PRIMARY-SET · identity passes · reportable=False`; exit 0. **Per-item `dG_SELF`: +1.123, +1.015, 0.000** (items `cruxeval-x-python/102` T=614, `humaneval-x-python/Python/100` T=320, `humaneval-x-javascript/JavaScript/1` T=1070).
- **What worked / hypothesis verdict:** mechanics **pass** — the chat-templated prompts reproduce the banked ids, position-count assertions hold at every write, the joint path runs, exit codes behave, and 60 items × 34 layers × 7 arms extrapolates to ≈ 2 h (well inside the 7 h budget). The numbers are what a dead AR should give: `c3 ≈ rt ≈ edit ≈ foreign` (the AR emits the same near-mean vector for every read, so `edit − foreign` = +0.09), and that near-mean vector at the decoy spans still recovers 40 % of the L5 swap — decoy *removal*, W's ≈ 49 % component, reproduced on a second host at a different depth. Random at L5 destroys −75 nats. None of this is a result (`--ignore-liveness`, 3 items, dead pair). **SELF is not exact:** two items drift by a full nat, one is bit-exact. The banked rule is `|mean dG_SELF| ≤ 1.0` (`nla_tiers.py:118`, `abs(self_m)`; the gate implements the same) — [`2026-09-09_multilayer-gate-harness.md`](2026-09-09_multilayer-gate-harness.md) wrote it loosely as "mean |dG|"; the code and the banked semantics are `|mean|`. It passed here (0.712) but with a systematic +1 at a low layer, 60 items would sit at the tolerance and could withhold the whole run.
- **Observations:** the drift is a capture-shape artefact, not noise. The vectors stage captured `h` from the **prompt-only** forward (`logits_to_keep=1`) while the score stage writes it into the **prompt + reply** forward; causal attention makes the prompt-position states mathematically identical, but bf16 GEMM/attention kernels tile differently for different T, so the stored state differs from the live one by ~1e-3 relative and 28 downstream layers amplify that into ~1 nat. The item that was exactly 0.000 is the one whose two shapes happened to hit the same kernel path — a bf16 write of a bit-identical vector rounds back to itself, so an exact zero is the signature of a matching capture. On the 12B the write sat at L32 with 15 layers above it, and SELF was −0.018. **Fix (pre-data, no rule touched):** the vectors stage now captures from the exact sequences the score stage scores — `h1b` from `l1b_prompt + l0_reply`, `h0` from `l0_prompt + l0_reply` (new `acts_scored`, plus an assertion that the rendered L0 prompt equals `l0_prompt_ids`). Script sha `2c7a03312ac2140f…` → **`1f7a9d3191a9ce328d5789abd3e451090c43759f3b72b99cb673d2113079c419`**; 7 tests pass. The queued vector array 384656 calls the file on disk and records its sha at run time, so no resubmission is needed. Re-smoke submitted as job **384664**: expect `S_self_L5` ≈ 0 on all three items; if it is not, the residual is a real harness fault and gets its own entry before the array runs.
- **New questions / new hypotheses:** none for the ledger; one to keep in view — the 4B's `G_prompt_swap` (+174 on 3 items) is larger than the 12B's (+121 over 60), so the gate ratios are on a different absolute scale; Gate 0 is a ratio by design.
- **Next Steps:** read 384664's SELF line; then wait for the chain (384635 explain → 384636 join → 384655 layers → 384656 vectors → 384657 score).
