### Target Date: 2026-09-14 (AMENDMENT to H-R2 — the restoration denominator moves into THEIR runtime; a runtime discrepancy recorded before any steered number is read)

- **What was seen.** The first bake-off arm finished: `unsteered` (job 399522, their `SteeredCausalLM.run_llama`, steering off) on the frozen 50 snippets / 434 cases = **Pass@1 0.5323**, parsed 0.8817, Pass@3 0.8986. H-R1's `renamed_unsteered` on the **same 50 snippets and packs** with our runner (`ase_tf_run.py`, job in `gate_codellama7b/`) = **0.6866**, parsed 0.9071. Same model (CodeLlama-7b-Instruct), same T = 0.7 / top_p 1.0, same 3 runs, same parser (`ce.parse_predicted_labels`), same Pass@k definition (any of the first k runs) — a **−0.154** gap between two runtimes of the *unsteered* condition.
- **Cause, verified in code.** `ase_tf_run.py:115` wraps the prompt in `tok.apply_chat_template(...)` (CodeLlama-Instruct's `[INST]` format). Their `models.py:319 _build_prompt` returns the bare `f"{instruction}\n\n```java\n{code}\n```{answer_prefix}"` and `models.py:675` tokenises it raw — **no `apply_chat_template` anywhere in their `models.py`**. The Instruct model given an un-wrapped prompt rambles: reply length median **2 494** chars (p90 3 480, max 4 490 — the 512-token cap) vs **122** chars (max 286) under the chat template; parse drops and label inversions appear (Java_028 run 0: all six labels exactly flipped; runs 1–2 all correct). The bake-off arms are faithful to **their** code; H-R1's runner was not (its docstring already says "THE PROTOCOL IS THEIRS, THE RUNNER IS OURS", and the chat template was the deviation).
- **A second, smaller error of mine, caught the same hour:** `gate/original.jsonl` is the **Llama-3.1-8B** gate; CodeLlama's H-R1 rows are `gate_codellama7b/{original,renamed_unsteered}.jsonl`. The scratchpad and the scoring script's example command pointed at the wrong model. Corrected below; no number had been computed against it yet.
- **What this does to the frozen verdicts.**
  - **H-R2a and H-R2b are unaffected**: every arm is paired per snippet against the bake-off's own `unsteered` in the same runtime.
  - **H-R2c as pre-registered is invalid**: its denominator `original − unsteered` would subtract a raw-prompt number from a chat-templated one and measure prompt format, not renaming damage. **Amended, before any steered arm is read:** the denominator becomes the **original (un-renamed) subset packs through their runtime, unsteered** — `bakeoff_codellama7b/original_unsteered.jsonl`, job **399863** (`nla/scripts/nla_r2_orig_sbatch.sh`, queued behind lane A). The H-R1 chat-templated numbers (0.7995 / 0.6866) are reported beside it as the cross-runtime reference and enter no verdict. Restoration-ratio thresholds unchanged.
- **New hypothesis, free from 399863 — H-R4 (ASE): the paper's damage magnitude depends on prompt format.** Prediction: raw-runtime damage `original_unsteered − unsteered` on these 50 is **larger** than the chat-templated +0.113, because the un-wrapped Instruct model's rambling is where parse failures and inversions come from, and renaming makes replies longer (the length→parse route of 2026-09-13). If confirmed, part of the paper's −36 points on Qwen2.5-7B-Instruct is a *prompt-format* effect that their steering could be repairing. No verdict rule is set for H-R4 (descriptive); it would pre-register a chat-templated `codesteer` arm as a follow-up, not be read from this run.
- **Setup:** `nla/src/ase_bakeoff_stats.py` (sha256 `4378064cb674d7a5…`) written today and exercised on synthetic arms; it takes `--original` and will be pointed at `original_unsteered.jsonl`. Arms in flight: lane A 399522 ✓ → 399684 `uniform_prior` → 399685 `swap_oracle` → **399863 `original_unsteered`**; lane B 399523 `codesteer` → 399686 `prompt` → 399687 `foreign`; lane C 399524 `rand_prior` → 399688 `erasure` → 399689 `combined`. CPU tests 399493: 21 passed (9 skips = Gemma-weight tests). Smoke 399430: `codesteer` 1 352 steer_calls/run, `unsteered` 0, both rc = 0.
- **Next Steps:** read nothing until all ten files exist; then `python nla/src/ase_bakeoff_stats.py --dir /scratch/juno/jvl210002/ase2026/bakeoff_codellama7b --original /scratch/juno/jvl210002/ase2026/bakeoff_codellama7b/original_unsteered.jsonl --out …/bakeoff_stats.json`; results entry with H-R2a/b/c and the H-R4 descriptive line.
