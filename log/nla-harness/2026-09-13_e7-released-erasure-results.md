### Target Date: 2026-09-13 (H-E7 `ERASURE-NOISE` at 12B L32 — the erasure result does NOT scale, and the released pair's edit is specific)

- **Hypotheses / what we're testing:** exactly the five frozen in [`2026-09-13_e7-released-erasure-prereg.md`](2026-09-13_e7-released-erasure-prereg.md), unchanged. **H-E7c** (the gate) `erase_1.0 − erase_flip ≥ +3.0` with CI > 0 → `ERASURE-DIRECTIONAL`, else `ERASURE-NOISE` and E7a/E7b/E7d are **void**. **H-E7a** released `edit` vs `erase_1.0`. **H-E7b** the same against `edit_ours` (our SFT 12B pair's bytes, written in this job). **H-E7d** `erase_1.0 − 0.55·swap`, share against H-W23's 0.493. **H-C11** released `edit − foreign ≥ +3.0` → `SPEC-LIVE` else `SPEC-FLAT`. Predictions recorded before the run: DIRECTIONAL · MATCHES-EDIT · BEATS-EDIT-OURS (weak) · WITHIN-SHARE with share in [0.40, 0.58] · SPEC-LIVE.
- **Setup:** job **393219**, g-07-03, one H200 nvl, COMPLETED 01:03:28 rc=0, 2026-09-13T13:42→14:45Z. `nla/scripts/nla_e7_released_sbatch.sh` (sha256 `43cfda53b5a7116c7bf20afd3a81ace074a5a6f7f4856836fa812585d7d3ccc1`), `nla/src/nla_erasure.py` (`5d0163e57d258eff062ea13168abc5f68121d3c76f5bd72ef36961d4bc05ff40`), tests `6297f2ed832cee4e049da5f84134ca49f900fa67dd6c461932614557742a835a`. Config `nla/configs/nla_ml_gate_12b.yaml`, traces `data/nla/p0/trace_llr/gemma12b/traces.jsonl` (60 items), seed 20260724, N_BOOT 10000, cluster bootstrap over items. Host = the banked Gemma-3-12B-it text checkpoint `/scratch/juno/jvl210002/nla_ml_gemma12b/host_text`. **Dictionary identity:** the RELEASED `kitft/nla-gemma3-12b-L32-av` (snapshot `7aec2259…`) + `-ar` (`3d6901d8…`), reached through a root whose `L32/{av,ar}` symlink the HF snapshots — `/scratch/juno/jvl210002/nla_ml_gemma12b_released`; compare root = our SFT pair `/scratch/juno/jvl210002/nla_ml_gemma12b`. Three stages in one job: `nla_ml_gate.py --stage vectors --layer 32` (50.1 min, 471 spans / 60 items), `--stage score --ignore-liveness` (the released pair ships no `eval.json`/`check.json`, so liveness is ignored **by construction** and that gate's `reportable` is False — H-C11 is read from the rows, not from `gate_stats.json`), then `nla_erasure.py --layer 32 --compare-root <ours>` (1.6 min, 780 forwards). A 3-item smoke ran the same three stages into a separate root first. Outputs: `<released_root>/gate/{gate_rows.jsonl,gate_stats.json}` and `<released_root>/gate/erasure/{erasure_rows.jsonl,erasure_stats.json}`.
- **Results:**
  - **Gates, all pass.** G1 anchoring identity at full strength (`allow_subset: False`): **471/471 spans paired, 0 position mismatches, h0 and h1b byte-identical between the two roots** (`max_abs 0.0`, cos 1.0 − 2e-16). G2 rows identity: `edit`, `foreign`, `swap`, **`edit_ours`, `foreign_ours`** and `self` all reproduce at **exactly 0.0**; `erase_own_chaos_nats` 1.107 per item (the bf16 diagnostic, not gated). Vector gate `max_abs_fp32` 2.44e-4 ≤ 1e-3, bf16 differing fraction 6.58e-5 ≤ 5e-4 (96/471 spans affected).
  - **Arms at L32, β = 1, 60 items / 471 spans (nats of `G_sum`):** `swap` **+41.47** · `erase_own` +41.53 · `edit` (released) **+20.81** · `erase_2.0` +16.49 · `edit_ours` **+16.17** · `erase_rand` **+13.94** · `foreign_ours` +11.56 · `foreign` (released) +10.88 · `erase_flip` **+9.05** · `erase_1.0` **+8.60** · `erase_0.5` +5.53 · `self` **0.000**.
  - **H-E7c:** `erase_1.0 − erase_flip` = **−0.46 [−1.71, +0.80]** against a +3.0 bar → **`ERASURE-NOISE`**. H-E3 (the 4B gate, reported for continuity): `erase_1.0 − erase_rand` = **−5.34 [−6.84, −3.86]** — the *random* direction is **better** than the shared one.
  - **H-E7a (VOID):** `erase_1.0 − edit` = −12.22 [−15.35, −9.18], width 6.17 → the verdict string is `EDIT-CARRIES-CONTENT`, voided by the failed gate.
  - **H-E7b (VOID):** `erase_1.0 − edit_ours` = −7.57 [−9.65, −5.59] → `EDIT-CARRIES-CONTENT-OURS`, voided.
  - **H-E7d (VOID):** `erase_1.0 − 0.55·swap` = −14.21 [−16.78, −11.64], **share 0.207** against H-W23's 0.493.
  - **α is monotone, not saturating:** +5.53 → +8.60 → **+16.49** across α = 0.5/1.0/2.0. At 4B L7 the same ladder read +28.51 → +35.75 → +36.96 (saturating at ‖Δ‖).
  - `cos(Δ_s, Ê)` mean **0.575** (the shared direction is real in vector space), but gain does not track it: ρ = **+0.169, p = 0.196** (4B L7: ρ = +0.294, p = 0.023).
  - **H-C11:** released `edit − foreign` = **+9.93 [+7.36, +12.68]** → **`SPEC-LIVE`**. Our pair's SPEC on the same rows: **+4.61 [+3.19, +6.18]**. Paired `edit − edit_ours` = **+4.64 [+2.56, +6.88]**.
  - **Descriptive, from the released pair's own gate stage (`--ignore-liveness`, so not a gate verdict):** fidelity `c3/swap` = **0.991 [0.976, 1.008]** (`c3` +41.12, `swap` +41.47) against our SFT pair's **0.739 [0.695, 0.781]** on the identical host/layer/items/spans; `rt` +8.95; `random` −359.44; `self` 0.000; **editable spans 205** vs 139 for our pair; `span_positions` 1459.
- **What worked / hypothesis verdict:**
  - **H-E7c ✗ REFUTED (`ERASURE-NOISE`) — my prediction was wrong, and it is the consequential one.** The leave-one-out mean-difference direction delivers **nothing** over a sign-flipped version of itself at 12B L32 (−0.46 [−1.71, +0.80]), and is **beaten** by a norm-matched random direction (−5.34 [−6.84, −3.86]). Everything the arm does here is step *size*: α scales monotonically with no saturation. **The 4B L7 erasure result does not scale.**
  - **H-E7a / H-E7b / H-E7d — VOID by the pre-registered rule.** Their verdict strings (`EDIT-CARRIES-CONTENT`, `EDIT-CARRIES-CONTENT-OURS`, `ERASURE-WITHIN-SHARE`/share 0.207) are recorded but **not claimed**; the gate that licenses reading them failed. Both trained edits nevertheless beat `erase_1.0` by CI-excluding margins (−12.22 and −7.57), which is the direction `EDIT-CARRIES-CONTENT` names — but it is not adjudicated here, and a **directional control that works at this layer** is owed before it can be.
  - **H-C11 ✓ SUPPORTED (`SPEC-LIVE`), prediction correct:** +9.93 [+7.36, +12.68]. **This retires H-C11's original framing.** The "flat edit" (+19.47 vs random +19.60) that motivated the hypothesis was an artifact of the **old anchoring**; on the repaired 471-span anchoring the released pair's edit is specific at 2.15× our pair's specificity.
  - Engineering: the second-root mechanism works exactly as designed — G1 byte-identical across roots, G2 exact 0.0 on the compare arms. One job now yields both pairs' edit arms on one anchoring with no cross-run comparison to defend.
- **Observations:**
  - **The two layers disagree about what the write IS.** At 4B L7: beats random by +14.32, saturates at ‖Δ‖, share 0.489 ≈ H-W23's 0.493, gain tracks `cos(Δ_s, Ê)`. At 12B L32: loses to random by 5.34, monotone in α, share 0.207, no cos correlation. A single mechanism cannot produce both. The most economical reading is that the 4B L7 result was a statement about an **early layer**, where the decoy's contribution is still close to a token-identity direction that a mean difference can find — and L32/48 is three quarters of the way up a 12B model, where `cos(h0,h1b)` is already 0.960 and the residual stream is anisotropic enough (H-W39: 75 % of the way to a foreign activation moves the cosine only to 0.979) that any step of the right size buys nats. **Depth, not scale, is the candidate confound — and this run cannot separate them**, because it changed both at once. The cheap decisive test is the erasure vector at 12B **L7** (H-E8 below).
  - **`erase_rand` > `erase_flip` > `erase_1.0` is itself informative.** A Gaussian direction beats both structured ones. Whatever the +8.60 is, it is not removal along a learned decoy axis; and because `random` in the *gate* sense (full replacement by a norm-matched vector) scores −359 at this layer, the difference is entirely that `erase_*` is a **bounded step** from `h1b` rather than a replacement. The arm family measures step size at this layer.
  - **The memo's central claim needs qualifying, not withdrawing.** "The trained layers buy an operation that costs one mean difference" was measured at 4B L7 and is **false at 12B L32**, where the released pair's edit delivers +20.81 and our own +16.17 against a mean difference's +8.60. What survives unqualified is the *accuracy* bound (`ERASURE-FLOOR`, −0.035 [−0.100, +0.027]) — that was measured in token space on the host itself and owes nothing to the erasure vector. The published artifact must be updated before it is shown to anyone else.
  - **A genuine training result, free:** the released pair (≈10× data, RL) beats our SFT pair on the same items by +4.64 [+2.56, +6.88] on `edit` and 2.15× on SPEC (+9.93 vs +4.61), with 205 vs 139 editable spans and 0.991 vs 0.739 transport. This is the cleanest evidence in the programme that the recipe gap is real and large, and it strengthens the H-C1 `BUDGET-EXPLAINS` verdict by putting both pairs on one anchoring in one job (previously two runs).
  - Silent-failure checks: `self` exactly 0.000 (471 spans × 60 items); G1 byte-identity rules out any anchoring drift between roots; the bf16 chaos floor (1.107 nats per item, 0.06 at the mean) is ~2.4× the 4B figure and is now the binding reason no arithmetic-built arm can be gated at 0.05 — H-E4 is more urgent than it looked.
- **New questions / new hypotheses:**
  - **H-E8 (decisive, ~0.4 GPU-h, no training):** run the erasure arms at **12B L7** using the already-banked host. If the vector is directional there, the 4B result was about **depth** and the erasure story survives with a layer qualifier; if it is noise there too, the 4B L7 result was about the **4B model** and the story is one host wide. Vectors at L7 need an AV/AR at L7, which we do not have at 12B — so the honest cheap version is the **mirror**: the 4B erasure arms at **L32/34** (banked vectors exist for all 34 4B layers), asking whether `ERASURE-NOISE` appears at depth **within one model**. That needs no new training at all.
  - **H-E9:** does a *supervised* direction rescue it at L32 — e.g. the top principal component of `h0 − h1b` over the other items' spans, or a ridge fit predicting `h0` from `h1b` — or is no linear direction competitive with the AR's state at this depth? Distinguishes "the mean is the wrong estimator" from "there is no linear erasure direction here".
  - **H-E10:** the α ladder is monotone through α = 2.0 at L32. Where does it turn over, and does the turnover coincide with `swap`'s +41.47? If an *untrained* bounded step keeps climbing toward the ceiling, the interesting quantity stops being direction and becomes **how far from `h1b` the host tolerates being moved** — which is a statement about the host, testable with no NLA at all.
  - **H-C11 follow-on:** SPEC 2.15× for ≈10× data plus RL. With H-C8's dose curve (`SPEC` +2.49 → +5.49 → +8.72 across 25/50/100 %) this is a third point on the same axis and the first one off our own recipe — worth folding into the H-C7 decision rather than leaving as a standalone comparison.
- **Next Steps:** (1) update the published positioning memo — qualify the erasure claim to 4B L7, add the H-C11/training-gap row, keep the `ERASURE-FLOOR` accuracy bound unchanged; (2) run the free within-model depth mirror (4B erasure arms at L32) before drawing any depth-vs-scale conclusion; (3) then the attention-reallocation arm, which this result makes *more* attractive, not less: the trained edit is doing something a mean difference cannot at the layer that matters, so "what does it do to attention" is now a live question rather than a tidying exercise.
