### Target Date: 2026-08-05 (Overnight run: false-positive abort → diagnosis → fix → relaunch)
- **Hypotheses / what we're testing:** None — operations. The 2026-08-04 overnight capture ([`2026-08-04_overnight-launch.md`](2026-08-04_overnight-launch.md)) did not survive: post-mortem + fix + relaunch.
- **Setup:** Out-dir `data/nla/overnight/2026-08-04/`. Diagnosis: parsed `captures.jsonl` reads for CJK content with per-read CJK character *fraction*. Fixes to [`../../nla/src/overnight_capture.py`](../../nla/src/overnight_capture.py) (3 edits); smoke `--limit 3` in isolated `overnight/smoke-relaunch/`; relaunch `tmux new-session -d -s nla-overnight "OUT_DIR=.../2026-08-04 bash scripts/overnight.sh"` (resume into the same dir).
- **Results:**
  - **What happened overnight:** the run aborted **19 minutes in** (16:05→16:24, 19/380 tasks, 231 reads, 0 error rows) — the CJK injection-failure monitor tripped at 6/100. The shutdown itself worked exactly as designed: PARTIAL summary written, server killed by the driver trap, GPUs released.
  - **Diagnosis — FALSE POSITIVE:** all 6 flagged reads are ordinary English reads with CJK *fraction ≤ 1%* — they quote a stray Chinese phrase. One is genuinely informative: a read reporting the model's internal conclusion phrased in Chinese (*"因此最终结果是：f(10) = 5…"* — Qwen partially reasoning in Chinese, faithfully read out). A real injection failure is a MOSTLY-CJK free-association read; none matched. The detector counted any-CJK-anywhere.
  - **Fixes:** (1) CJK monitor now flags a read only when its CJK fraction > 0.30 (threshold on the trailing-100 window unchanged at 5%); (2) resume no longer treats **error rows** as done (they retry on restart); (3) malformed DRM summary-table header repaired.
  - **Smoke:** `--limit 3` clean (exit 0, well-formed summary, server killed by trap).
  - **Relaunch (06:09):** resume verified — `361 tasks total to run, 19 already done`, heartbeat `5/361 done (24 cum) · 66s/task · ETA 6.5h`, GPUs 0 (runner) + 1 (server) active. Wall guard 11 h.
  - **Early data from the 19 captured tasks** (single runs — data points, not conclusions): Dataset-A accuracy gradient already visible — L0 0.50, L1 0.33, L1b 0.50, L2 0.33, **L3 0.00**; DRM_AR at L1b trap positions mean −0.015 (n=24 reads).
- **What worked / hypothesis verdict:** N/A. The abort-safely machinery all worked; the *detector definition* was wrong. Charter lesson reinforced: an unattended monitor needs its trigger defined on the failure's *signature* (mostly-CJK free association), not a proxy that overlaps with legitimate behavior (any CJK — which Qwen legitimately produces).
- **Observations:** The bilingual-conclusion read is a nice incidental finding for the faithfulness program: the NLA reports internal language switching — deck's "language switching" NLA finding reproduced in the wild on a code task.
- **New questions / new hypotheses:** None new; the run's morning summary will feed the tier-accuracy and DRM questions.
- **Next Steps:** Morning check per plan (runner.log COMPLETE, ~380 rows, errors <15, per-tier accuracy table, DRM table); then the results log entry.
