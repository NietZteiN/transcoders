### Target Date: 2026-09-09 (Phase B gate harness — `nla_ml_gate.py` frozen before any trained pair exists)
- **Hypotheses / what we're testing:** organizational / harness day. No new hypothesis; this entry freezes the **scorer** for the pre-registered Phase B rules (Gate 0, H-M1, H-M2, specificity, SELF identity, H-M3 — [`2026-09-09_multilayer-train-prereg.md`](2026-09-09_multilayer-train-prereg.md), rules verbatim from [`../../docs/nla_multilayer_scoping.md`](../../docs/nla_multilayer_scoping.md) §4) so the code that will emit the verdict exists, is hashed, and is unit-tested **before** a single AV/AR pair has finished training. Two labels are named here because they are outcomes the rules can produce but the prereg did not spell out: **`M-GATE0-FAIL`** (Gate 0 fails — the joint clean-state write does not even reach 25 % of a prompt swap, so H-M1/H-M2 are not evaluated) and **`M-NO-GAIN`** (H-M1 fails while H-M2 passes — the joint edit beats its own random benchmark but not the best single-layer edit). Both are fixed now, pre-data.
- **Setup:**
  - `nla/src/nla_ml_gate.py` sha256 `2c7a03312ac2140ff1bd71fee10f425f94e106bbe72c13a3e0f564f51a851762`; config `nla/configs/nla_ml_gate.yaml` sha256 `c7c1d9e3ddc1ddde7ffee4dedf3772641637fdcef650a4895fbf4ea3e0e4baad` (`seed 20260724`, `n_boot 10000`, `max_spans_per_item 8`, `repair true`, `max_new_read 180`, liveness thresholds `av_holdout_gap_min 0.0 / ar_fve_min 0.20 / max_no_tag_frac 0.10 / max_cjk_frac 0.10 / cycle_real_gt_other true`, sets `primary [8,22,24]`, `depth [8,17,26]`, `all`, rules `gate0_ratio_min 0.25 / hm2_frac_of_c3 0.30 / self_tol_nats 1.0 / hm3_ratio_min 0.80 / hm3_confirm_frac 0.80`). Tests `nla/tests/test_nla_ml_gate.py` sha256 `01466e05d1ffdec9…` — 7 CPU tests pass (synthetic rows over {8,22,24}: EDIT-LIVE + H-M3 CONFIRM; GENERIC + REFUTE; GAIN-BUT-GENERIC; GATE0-FAIL; SELF identity fail / exact-zero pass; an unwritten arm raises `ArmNotWritten`; `liveness()` read from real trainer output shapes).
  - Two stages. **`--stage vectors --layer K`** (array job, one layer per task): host `data/nla/ml/gemma4b/host_text` (bf16, `gemma_text.load_gemma_text`) + `LocalAV(L{K}/av)` + `NLACritic(L{K}/ar)`; the repaired anchoring of [`2026-09-06`](2026-09-06_meaning-clean-prereg.md) (`recover`, `term_spans`, `span_token_positions`, ≤ 8 spans/item) on the **4B's own traces** (`data/nla/p0/trace_llr/gemma4b/traces.jsonl`, job 384619); asserts the chat-templated L1b prompt reproduces `l1b_prompt_ids` token-for-token; per span banks `h0` (L0 state at the matched true-term occurrence), `h1b` (L1b state at `max(pos)`), `c3 = AR(AV(h0))`, `rt = AR(AV(h1b))`, `edit = AR(substitute_terms(AV(h1b), {decoy: true}))`, `foreign` (decoy → another item's true term, `others[crc32(f"{sid}#{si}") % len(others)]`), and `self_all = h1b` at every span position → `gate/vectors/L{K}.npz` + `L{K}_spans.jsonl` + `L{K}_manifest.json` (cosines, AV/AR provenance, config + script shas). Non-editable spans (decoy absent from the read) get the unedited reconstruction so `edit`/`foreign`/`rt` write the same positions — the W protocol unchanged.
  - **`--stage score`**: liveness from `L{K}/{av,ar}/eval.json` + `check.json` (missing file ⇒ dead); every requested set is restricted to its live subset and the restriction is printed; anchoring is asserted identical across layers; one `PositionReplacer` per live layer, joint writes = all replacers armed at once; `logp(prompt_ids, reply_ids)` teacher-forced with `use_cache=False, logits_to_keep=len(rids)+1`, replacer position counts asserted after every call. Per item: `logp_U`, `logp(l0_prompt, l0_reply)` → `G_prompt_swap`; 7 single arms per live layer (`S_{c3,swap,edit,foreign,rt,random,self}_L{K}`) and 7 per set (`M_{kind}@{set}`, `SELF@{set}`). Random = unit vector from `default_rng(seed + crc32(f"{sid}#{si}#L{K}#rnd"))` — one fixed draw per (item, span, layer), so the single-layer and joint random arms use the *same* vectors. `record_positions` on every row → `gate/gate_rows.jsonl`, `score_manifest.json`, `gate_stats.json`.
  - Scorer: cluster bootstrap over items with **shared draws** across arms (`N_BOOT 10 000`, seed 20260724). Gate 0 via a bootstrap of the ratio `mean(M_c3)/mean(G_prompt_swap)`; H-M2 (`M_edit − M_random`) and specificity (`M_edit − M_foreign`) via `arm_guard.paired` (refuses position-count mismatch); **H-M1 via `arm_series`, not `paired`** — the joint write touches 3× the positions of a single-layer write *by design*, so a paired position-count check would refuse the very comparison the hypothesis asks; the zero-position guard still applies. `best_single_edit_layer = argmax_K mean(S_edit_L{K})` — over the **live** layers. SELF identity: mean |dG| ≤ 1.0 nats for every set's `SELF@{set}` **and** every `S_self_L{K}`; any failure ⇒ `identity_passes=false`, exit 3, verdict withheld. `reportable = identity_passes and not --ignore-liveness` — `--ignore-liveness` exists for the smoke only and is written into the manifest so a smoke score can never be quoted as a result.
  - Jobs submitted this session (all `gpu:nvidia_h200_nvl`): gate smoke **384634** (`nla/scripts/nla_ml_gate_smoke.sh` sha256 `c24408557092f974…`: pytest → `vectors --layer 5 --root data/nla/ml/gemma4b_smoke --smoke` on the 40-doc smoke pair → `score --smoke --ignore-liveness`); explain array **384635** (`afterok:384622`); join **384636** (`afterok:384635_*`). Not yet submitted, by the go/no-go rule: `nla_ml_layer.sh` (waits for 384619's final accuracy), then `nla_ml_vectors.sh` (sha256 `19bb3d7f0453d83d…`, array 0-33, `afterok:` layer array) and `nla_ml_score.sh` (sha256 `e76ce2074b5b6a2c…`, `afterok:` vectors array; exit 3 = SELF failed).
- **Results:** none — no trained pair exists yet. Progress at writing: 384619 at 22/60 items (18.9 min); 384622 past its 40-doc smoke (all stages passed: AV L5 holdout gap permuted−real **+0.053**; AR L5 fve **−1.007** vs shuffled −1.020 vs train-mean −0.103, cos 0.959 vs shuffled 0.9587; check L5 no-tag 0, CJK 0.0, cos_cycle 0.969 — at 40 docs these are smoke numbers, not liveness verdicts) and into the full extract at 3,216/20,000 docs, fp32 memmaps, 18 GB resident.
- **What worked / hypothesis verdict:** n/a (harness day). Four defects caught on self-review before the hash was taken: a convoluted `G_prompt_swap` extraction, `pos_counts[arm] = dict.fromkeys(range(n))` where a range was meant, a `row.get('SELF@primary' and 'dG_SELF@primary')` that silently evaluated to the second string, and a provenance `experiment` key inherited from `nla_train` — all fixed in the hashed version.
- **Observations:** one **pre-data amendment to the prereg**, recorded here so it cannot be mistaken for a post-hoc change: the prereg says `max_ℓ S_edit_ℓ` is taken "over all 34 trained layers"; the scorer takes it over the **live** layers, because a PAIR-DEAD layer has no valid single-layer edit (its AR fails reconstruction) and the score stage does not load dead pairs at all. This can only *lower* the max and therefore makes H-M1 marginally easier, not harder — if the live set turns out much smaller than 34 the H-M1 verdict is reported with the live count beside it. The smoke pair's AR fve on L5 (−1.007 vs shuffled −1.020) shows why the liveness rule (b) is needed on the full run: at 40 docs the AR reconstructs *nothing* beyond the mean, and cosine 0.959 vs 0.9587 is entirely anisotropy.
- **New questions / new hypotheses:** none beyond the prereg's H-M1–H-M3.
- **Next Steps:** (1) 384634 must exit 0 (SELF identity + position-count assertions on real 4B states; fix and resubmit if not). (2) When 384619 finishes, apply the band `acc_l0 ∈ 0.633 ± 0.15` from `traces_summary.json`; in band → `sbatch --dependency=afterok:384636 nla/scripts/nla_ml_layer.sh`, then `nla_ml_vectors.sh` and `nla_ml_score.sh` chained with `afterok`. (3) Results entry when `gate_stats.json` lands, with the liveness table (which layers are PAIR-DEAD and why) *before* any verdict.
