### Target Date: 2026-09-13 (H-S14 pre-registration — screen a 4.3× larger corpus so the accuracy question can actually be answered)

- **Why, stated bluntly:** I have told the user four times today that the corpus is the binding constraint on the accuracy arm, and then kept running interventions on the same 60 items. The user's correction was right. The 60-item pool caps **any** method at **+0.033** (L0 34/60 vs L1b 32/60 — two items), and the only place a real effect lives is the 7 flippable items where `c3_band` fixed 3 and broke 0 at McNemar p = 0.250 ([`2026-09-13_budget-confound-results.md`](2026-09-13_budget-confound-results.md)). **No intervention can be shown to match CodeSteer, or to fail to, on this pool — the experiment is unpowered by construction, not by choice of lever.**
- **And the larger corpus was already here.** `data/stimuli/alignment/humaneval_x_{python,js}_L1b_mapping.parquet` hold **164 problems per language**, each with all five tiers pre-generated (`code_L0`, `obf_code_L1`, `code_L1b`, `obf_code_L2`, `obf_code_L3`), the adversarial rename maps, and execution-derived I/O pairs. `build_dataset_c.py` converts them: **257 usable snippets** after dropping 69 without parseable I/O pairs and quarantining 2 that fail the rename gate (`JavaScript/137` maps an identifier onto the JS keyword `let`; `JavaScript/95` carries `hi` on both sides). `data/stimuli/dataset_c/dataset_c.jsonl`, 1 285 rows. CLAUDE.md's "Dataset B = 250 snippets" is 250 *rows* = 50 snippets, which is how I mis-scoped this.
- **Validation of the build (passes, and it is the right gate).** The first gate compared the ~29 overlapping HumanEval-X tasks against the banked stimulus and **failed 39 of 58** — informatively: adversarial renaming is *sampled*, so `Python/0`'s banked L1b renames to `write_top/application/dayofweek` where the parquet renames to `super_parent/failed_to_create_image_path_list`, and banked `Python/31`'s L0 is `def myFunct(n)` against the parquet's `def is_prime(n)`. The banked 60 are **not reproducible** from these parquets. The gate that does validate the conversion is internal: applying `rename_map` to `code_L0` (word-boundary, longest key first) must reproduce `code_L1b` exactly with no original identifier surviving into L1b and no decoy name occurring in L0 — **164/164 per language, zero leaks.** Overlap is *reported* (29 tasks, in `meta.task_id` and the manifest) and never pooled as independent, since those items share an underlying program with a banked-60 item.
- **Hypotheses:**
  - **H-S14a (the screen):** on 257 new snippets, what are `acc_l0`, `acc_l1b`, and the **flippable count** (L0 correct ∧ L1b wrong) at the **corrected 2 600-token budget**? Screening at 1 100 would misclassify items as L0-incorrect for running out of tokens — H-A8 measured that at ~20 % of generations — and every such item is silently removed from the flippable pool, so the budget is not a free parameter here.
  - **Pre-declared yield and power.** `docs/nla_flippable_corpus_scoping.md` put the screening yield at ~11 %; the banked 60 gave 7/60 = 11.7 %. **Prediction: 25–35 flippable from 257.** At 28 flippable, and taking the observed rescue rate of 3/7 forward, a `c3_band`-style arm would fix ~12 and break ~0, giving McNemar p ≈ 2·2⁻¹² ≈ **5 × 10⁻⁴** — decisively significant where the 7-item version could not clear 0.09 even on a perfect rescue. **That is the whole point of the run.**
  - **H-S14b (pre-registered now, run after the screen):** on the enlarged flippable set, `c3_band − noop` and the attention-reallocation arm (`set_many` knockout of the split-half-stable top heads `L23H3, L33H2, L13H6, L30H2, L23H1`, against a materialised-mask reference and a layer-matched random-head control), all at 2 600 tokens. **Frozen bar: `RESCUE-REAL` if `c3_band − noop` on the flippable subset has McNemar p < 0.05 and the paired CI excludes 0; `RESCUE-ABSENT` otherwise.** Reported alongside: harm on the non-flippable items, because H-A8 showed the pooled effect is the sum of a rescue and a harm.
  - **Honest limit, recorded before the run:** matching a number CodeSteer reports on *its* benchmark is still not what this delivers. What it delivers is a powered head-to-head between NLA editing and attention reallocation on the same stimuli, and a rescue estimate with a CI narrow enough to be worth quoting.
- **Setup:** `host_traces.py --model gemma4b --datasets dataset_c --max-new-gen 2600`, seed 20260724, greedy, `JsonlSink` resumable so a wall-clock stop costs nothing. Both new flags default to the banked values, so every existing trace file reproduces. Output `data/nla/p0/trace_llr/gemma4b_c/traces.jsonl` + `traces_summary.json`. Cost: 257 × 2 generations at the measured ~17.5 s each ≈ **2.5 GPU-h**, one GPU, `--partition=h200,h100`.
- **Results / verdict / observations:** to be filled by the results entry. Nothing here is a result.
- **Next Steps:** the screen; then H-S14b on the enlarged flippable set; then the memo's bound and Figure 1 are re-stated on a corpus that can support them.
