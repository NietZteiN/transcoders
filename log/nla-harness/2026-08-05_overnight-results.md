### Target Date: 2026-08-05 (Overnight capture COMPLETE — results)
- **Hypotheses / what we're testing:** None pre-registered (capture run, not an experiment) — but the collection was sized to make tier-level patterns visible. Companion to [`2026-08-05_overnight-abort-fix-relaunch.md`](2026-08-05_overnight-abort-fix-relaunch.md).
- **Setup:** Relaunched run finished **12:38**, exit COMPLETE, wall ~6.5 h. **380/380 rows, 0 error rows, 0 CJK aborts**; 4,812 reads this pass (5,043 total with the first batch); server killed by driver trap, all GPUs released. Artifacts: `data/nla/overnight/2026-08-04/{captures.jsonl, summary.md, run_manifest.json}`. Composition: 300 graded output-prediction rows (A: 10/tier, B: 50/tier × 5 tiers), 50 prompt-read rows (ungradeable A-python), 30 synthetic slice tasks. Single greedy runs — every cell is a data point, not a conclusion.
- **Results (from `summary.md`):**
  - **Accuracy by tier** (A n=10, B n=50 per tier): A: L0 .70 · L1 .50 · **L1b .80** · L2 .60 · L3 .50. B: L0 .64 · L1 .56 · L1b .60 · L2 .64 · L3 .54. Pooled ≈ L0 .65 / L1 .55 / L1b .63 / L2 .63 / **L3 .53**.
  - **The pattern is NON-monotone and echoes the papers:** L1b ≥ L0 on Dataset A (.80 vs .70) — Paper 1's "some renaming matches or beats unobfuscated" and Papers 2–3's "renaming's pooled effect cancels by item" reproduced out-of-the-box on this non-panel model; **L3 (stacked) is the floor** in both datasets, consistent with the OR-0.57-only-control-flow story plus stacking.
  - **Neutral renaming (L1) ≤ adversarial (L1b) in both datasets** — surprising; worth an item-level look before believing it (single greedy runs).
  - **Slice prediction: 0.33** (10/30, convention stated in-prompt) — the slicing weakness from the 3-task gallery replicates at scale.
  - **DRM_AR at trap identifiers:** L1b **−0.0146** (n=333 reads), L3 **−0.0349** (n=172) — true-leaning on average with the crude template glosses; L3 *more* true-leaning than L1b. Absolute signs remain untrustworthy (gloss anchors); the usable contrasts need the per-item analysis against matched L0 baselines (not in the run summary).
- **What worked / hypothesis verdict:** N/A (capture). Operationally: the robustness rig went 361 tasks / ~6.5 h unattended with zero errors and zero false aborts after the CJK-detector fix. The dataset now exists for exactly what N1 needs: 300 graded runs with CoT + identifier + answer-line reads and ground truth.
- **Observations:** (1) The early 19-task teaser (L3 = 0.00) vs the final (L3 = 0.50 on A) is a textbook small-n lesson — the charter's "a single run is a data point" rule, self-demonstrated. (2) B-L0 accuracy (.64) < A-L0 (.70) — B's cruxeval/leetcode mix is harder baseline material. (3) 50 A-python rows remain read-only packages (no calls/ground truth by construction).
- **New questions / new hypotheses:** (1) Is L1 ≤ L1b real or greedy-run noise? (K-sample runs would answer — that's N1's S1 anyway.) (2) Do CoT/answer-line reads *differ* between correct and wrong runs (the N1 question — this dataset can now give a first uncontrolled look). (3) Does per-item DRM_AR predict the wrong L1b answers?
- **Next Steps:** (1) A first exploratory pass over `captures.jsonl` — read-vs-correctness contrasts and per-item DRM (cheap, no GPU); (2) un-park N1 for the controlled version; (3) fold `ninja` into `nla/environment.yml` (still ad hoc).
