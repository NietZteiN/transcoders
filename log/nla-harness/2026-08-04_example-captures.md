### Target Date: 2026-08-04 (Example captures: output + slice prediction, out-of-box answers + NLA reads)
- **Hypotheses / what we're testing:** Exploratory capture (no formal hypothesis): record what the out-of-the-box model (Qwen2.5-7B-Instruct, greedy) answers on simple code tasks — **output prediction** (Dataset-A L0 snippets, ground truth = `expected_output`) and **slice prediction** (3 hand-built line-numbered programs, dynamic backward slice by hand) — alongside NLA L20 reads at key code tokens and across the CoT.
- **Setup:** Env `nla-mi`; AV server GPU 1 (`--mem-fraction-static 0.6`), extractor+AR GPU 0; seed 20260724; greedy, max_new 900 (first pass at 384 truncated every long reply — lesson recorded). New script [`../../nla/src/capture_examples.py`](../../nla/src/capture_examples.py). 5 tasks (2 output-prediction picked from A-L0 with parseable calls; 3 slicing), 42 reads. Server stopped after the run.
- **Results:** artifacts: `data/nla/examples/captures.json` + human-readable [`../../data/nla/examples/EXAMPLES.md`](../../data/nla/examples/EXAMPLES.md).
  - **JavaScript/58 (output prediction, `common([1,2,3,4],[2,3,4,5])` HumanEval-X): ✅ correct** (`[2, 3, 4]`; first grading pass called it wrong — the model wraps answers in backticks, grader fixed). Reads at `ret`/`.add` tokens correctly name Set/array-intersection operations.
  - **JavaScript/63 (`fibfib(14)`): ❌ no answer** — greedy CoT exhausts 900 tokens mid-trace of the depth-14 tribonacci (the Paper-3 bounded-System-2 phenomenon appearing out of the box). Reads: Fibonacci/recursion at every `fib` token.
  - **slice_1 (straight-line): ✅ correct** `[1,3,5]`. The near-answer CoT read verbalizes *"now presenting a summary line with 'The relevant lines are: 3, 4, 5'"* — the L20 state near the answer encodes an answer-shaped line list (slightly off from the final [1,3,5], echoing the known specifics-confabulation).
  - **slice_2 (loop): ❌** `[2,5,7]` vs `[1,2,4,5]`; **slice_3 (branch): ❌** `[4,5,8,9]` vs `[1,2,4,5,8]`. **Consistent error pattern: the model includes the `print` line and drops initializer lines** (n=5, x/y defs). NB the print-line inclusion is arguably a slicing-convention disagreement; the dropped initializers are genuine errors. Reads at `total`/`count`/`n` tokens name the right entities (loop accumulation, sum/count); a cot@4 read confabulates "C code".
  - Read health: round-trip cos 0.73–0.96, CJK 0/42.
- **What worked / hypothesis verdict:** N/A (exploratory capture). The gallery does what it's for: paired (task, model answer, ground truth, position-wise internal reads) examples now exist for both task kinds.
- **Observations:** (1) Near-answer CoT reads carrying answer-adjacent content ("relevant lines are…", "the correct answer format is 'steps 1 to 8'") is exactly the raw material N1's agreement metric would exploit. (2) Truncation is a first-class failure mode — the N1 design's `truncated` covariate is justified; fibfib(14) at greedy needs >900 tokens. (3) Slice-prediction ground truth needs a stated convention (print line in/out) before it's usable quantitatively. (4) Grading regex must tolerate backtick-wrapped answers (fixed).
- **New questions / new hypotheses:** Do near-answer reads *disagree* with the final answer specifically on wrong runs? (A 2-task observation suggests reads are answer-shaped; N1 tests the discriminative version.) Is the initializer-dropping slice error visible in the reads at those initializer tokens (never read in this capture — add them)?
- **Next Steps:** (1) Optionally widen the gallery (more tasks incl. Python output-prediction rows — the A python rows lack parseable call inputs, needs a small input-format shim); (2) fold the slice-convention fix + initializer-token reads into any repeat; (3) un-park N1 when ready.
