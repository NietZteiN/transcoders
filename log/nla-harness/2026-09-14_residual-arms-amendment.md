### Target Date: 2026-09-14 (AMENDMENT to H-R2 — the residual arms' parameters, frozen before any steering number exists)

- **Why.** [`2026-09-14_bakeoff-amendment-arms.md`](2026-09-14_bakeoff-amendment-arms.md) added `swap_oracle` / `foreign` / `erasure` / `combined` but left their write parameters unstated because the Java alignment did not yet exist. It now does (`nla/src/ase_align_java.py`, 6/6) and the arms are implemented in `nla/src/ase_steer_run.py` (sha256 `d6207d15df0c6cbf…`), so the parameters are fixed here. **No steering arm of any kind has produced a number at the time of writing** (smoke 399430 still queued), so nothing below is chosen against data.
- **Frozen:**
  - **Host / layer / coefficient:** CodeLlama-7b-Instruct (32 layers), write at the output of **decoder layer K = 7**, **β = 1.0** (full replacement, one layer). K = 7 is carried over from the Gemma-3-4B family's primary layer by index (≈ 22 % depth vs 21 % there), not tuned here; there is no Llama-7B layer sweep and none will be run before the arms. The β = 0.35 band over L2–13 stays **held** as in the arms amendment.
  - **Positions:** every decoy token of every renamed identifier occurrence in the *renamed* prompt (code **and** case expressions), from `align()` plus the special-token offset **measured per prompt** (`_encode` asserts the tokenised prompt is a pure prefix-extension of the bare tokenisation). Snippets failing any alignment gate are **excluded** and listed in `<arm>.jsonl.residual_meta.json`, never written best-effort.
  - **Vectors:** `swap_oracle` = the original prompt's layer-K state — per token when the original and decoy spans tokenise to equal length, else the original span's **mean** at every decoy token. `foreign` = the mean original-span state of a span drawn (seed 20260724) from a **different** aligned snippet. `erasure` = `h1b_mean_s + dn^(−i) · unit(E^(−i))`, with `Delta = h0_mean − h1b_mean` over every span of every **other** aligned snippet in the run — the `erase_loonorm` construction of H-E14, direction **and** magnitude leave-one-item-out, so nothing from the written item's original enters its vector. `combined` = their level-2 `slice_hybrid` steering (decode-only, unchanged) **plus** the `erasure` write (prefill).
  - **Identity gate, per snippet, before any arm (exit 3 on failure):** writing the renamed prompt's **own exact per-token** state at its decoy positions must leave the next-token logits at the last prompt position within **0.05** of the unhooked forward, and `n_positions_written` must equal the number of aligned decoy tokens. Then, per generation, `n_positions_written == len(targets)` or exit 3.
  - **Readout / verdicts:** unchanged — Pass@k case-weighted, 3 sampled runs at T = 0.7, paired per snippet against `unsteered`, cluster bootstrap N_BOOT 10 000 seed 20260724; H-R2a/b/c as pre-registered. Pre-declared readings for the new arms: `swap_oracle ≤ unsteered` ⇒ span-position writes have **no** accuracy headroom on this protocol and `erasure`'s result is bounded by it whatever it is; `foreign ≈ swap_oracle` ⇒ any gain is perturbation, not content.
- **Prediction, stated now:** `swap_oracle` within ±0.05 of `unsteered` (the Gemma tier ladder found `ERASURE-FLOOR`: erasing the decoy is not what repairs accuracy), `erasure` and `foreign` indistinguishable from it, `codesteer` the only arm to separate if any does.
- **Verification before GPU:** `nla/tests/test_ase_residual.py` (CPU job 399493) exercises alignment + offset + capture + identity gate + target arithmetic on a random-init 2-layer Llama with the real CodeLlama tokenizer and the two smoke packs; the negative test shifts every position by one and must **fail** the gate.
- **Next Steps:** smoke 399430 → five non-residual arms; then `swap_oracle`, `foreign`, `erasure`, `combined` via `nla_r2_arm_sbatch.sh <arm>` (`--packs-orig` wired in), ~0.7 GPU-h each.
