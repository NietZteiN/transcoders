### Target Date: 2026-08-06 (N5 — dense capture COMPLETE: 91 pairs, 4,653 reads, 0 errors)
- **Hypotheses / what we're testing:** None — N5 is an instrument run. It produces the dense read corpus that HT12/HT13/HT14 (pre-registered earlier today) will be tested on. Gate: ≥200/234 case-sides complete, CJK never fires, and **median rt_cos within 0.01 of the banked corpus** (AV-drift check).
- **Setup:** `nla/scripts/dense.sh` in tmux `nla-dense`, GPUs 2 (runner) + 3 (AV server), **default sglang config, sequential AV, temp 0** — the regime verified to reproduce the banked corpus 60/60 (user decision, same day). No generation: banked replies teacher-forced through `extract_chat`. 91 wrong/control pairs from `match_controls.py` (median length ratio **1.043**, all within the hard 1.5× bound; 26 of 117 wrong cases left unpaired rather than badly matched). Positions from `dense_positions.py`: uniform sweep stride 20 + the fixed **0.70–0.90 strip** at stride 8 on every case + ±20-token bursts at stride 2 for the **3** known-answer-verified localizations only. Seed 20260724.
- **Results:**
  - **COMPLETE: 91/91 pairs, 4,653 reads (4,421 new + 232 reused), 0 error rows, 0 CJK alarms**, ~176 s/pair, ~4.4 h wall.
  - Reads by role: sweep 3,071 · strip 1,554 · burst 28. Per case: median **24** reads (range 10–58) — **~4× the previous CoT density** (6 per trace).
  - **Drift gate: literally FAILED, substantively PASSED.** Median rt_cos 0.8580 vs the banked 0.8780 — outside the ±0.01 bar. But the bar was a proxy, and the direct tests clear it:
    1. the **232 reused reads match the banked values exactly (0 mismatches)**;
    2. **position-matched** comparison (banked CoT vs dense new, by position quintile) agrees within ±0.017 on quintiles 20-40 / 40-60 / 60-80 / 80-100.
    The 0.02 shift is **composition, not drift**: the banked median is lifted by identifier reads (median 0.883) which the dense run does not take, and the dense run is deliberately weighted toward the 0.70–0.90 strip — the known low-faithfulness trough (strip median **0.8387** vs sweep 0.8689). Recorded as a proxy-gate failure with a passing direct test rather than as a pass.
  - **Early signal relevant to HT12 (NOT the test — N6 is):** in this length-matched, position-matched sample the correct-vs-wrong faithfulness gap is **Δ = +0.0021** (mean 0.8540 vs 0.8519), against **+0.010 to +0.019 per tier** in the banked corpus. That is a **5–9× smaller** effect once controls are length-matched and the same relative positions are read. It is exactly the direction the pre-registration's refute clause anticipated (|β| < 0.005 = "too small to be a usable signal"), and it suggests the original post-hoc gap was substantially length/composition driven. **N6 decides this properly**, with the mixed model, case-level clustering and read-length covariate; this raw Δ is not that test.
- **What worked / hypothesis verdict:** N/A (instrument). The rig ran unattended with zero errors, and the resume/checkpoint machinery was proven in the smoke (rerun → "nothing to do").
- **Observations:** (1) The composition confound in the drift gate is a lesson for future gates: a summary statistic compared across two *differently-sampled* corpora is not a drift test — the reused-read identity check and position-matching are. (2) Only 28 burst reads exist, because only 3 localizations survived validation; the design change made on 2026-08-06 (strip-on-every-case) is what saved the error-region coverage — the strip contributed 1,554 reads. (3) 26 wrong cases have no control: they exhausted the length-compatible correct pool, mostly at L3 (fewest correct cases, longest traces). Analyses must report this as missingness, not silently drop it.
- **New questions / new hypotheses:** Does the correct-vs-wrong gap survive at all once N6's covariates are applied, or does the dense matched sample kill it? If it dies, the honest headline is that the 2026-08-06 faithfulness↔correctness finding was a length/composition artifact — which is a real (negative) result and worth stating plainly.
- **Next Steps:** N6 (mixed model + case-level permutation + cluster bootstrap, per the frozen rules), then N7 (judge alignment, with the shuffled/distant nulls and the AR baseline), then N8 (answer onset), then the artifact features.
