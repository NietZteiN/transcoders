### Target Date: 2026-07-24 (Charter + research-ops scaffold)
- **Hypotheses / what we're testing:** None — organizational. Goal: stand up the `transcoders/` sub-project charter and research-ops scaffold, modeled on the sibling `translation/` project, now that the goal artifacts are in hand.
- **Setup:** Working dir `transcoders/` on `csr-94608.utdallas.edu`. No GPU used. Inputs: two goal artifacts the user added — the SAE/transcoder experiment menu (compass markdown) and the `Obfuscation_Mechanisms_Combined` proposal deck (`.pptx` + `.pdf`). Actions:
  - Organized artifacts into [`../../docs/`](../../docs/): compass markdown → `docs/experiment_menu.md`; deck → `docs/Obfuscation_Mechanisms_Combined.{pptx,pdf}`.
  - Wrote [`../../CLAUDE.md`](../../CLAUDE.md) — adapted from `../translation/CLAUDE.md`: reused §1 compute (same host, no SLURM, 4× A6000) and §2 storage / destructive-command rules; wrote a new §3 (research goal); added SAE-contestation discipline + MI silent-failure checks to §4; adopted the full `log/` protocol in §6.
  - Built this `log/` folder (`TEMPLATE.md`, `README.md`, `setup/README.md`) and migrated the flat `LOG.md` into [`2026-07-24_register-foundational-papers.md`](2026-07-24_register-foundational-papers.md); removed the flat `LOG.md`.
  - Seeded [`../../CLAUDE_SCRATCHPAD.md`](../../CLAUDE_SCRATCHPAD.md).
- **Results:** Scaffold in place. Project defined as **Instrument 3 (SAE + transcoder feature & circuit analysis)** of the mechanistic follow-up to Papers 1–3, on Schulte's Block-Model frame. Committed experiments: **E1** (semantic-capture feature diff, L0 vs L1b), **E2** (causal feature steering), **E3** (transcoder attribution graphs on L0/L2/L3), **E7** (cross-instrument triangulation); **E4** as a cheap panel-agnostic add.
- **What worked / hypothesis verdict:** N/A (organizational). Charter links verified to resolve; no dead links to translation-only helper files.
- **Observations:** Feasibility is **dictionary-bound** — forward-pass experiments (E1/E2/E7/E4) fit one A6000, but models without public SAEs/CLTs (reasoning-Qwen, coder models) need from-scratch training on the 8×H100 allocation. SAEs are contested (Kantamneni et al.; AxBench), so the charter mandates dense-probe/prompting baselines + causal validation on every feature/steering claim.
- **New questions / new hypotheses:** Which committed experiment to stand up first? E1/E2 on **Llama-3.1-8B** (Llama Scope) is the lowest-friction entry (pretrained dictionaries, forward-pass only). E7 should run regardless as the triangulation backbone.
- **Next Steps:** Create `src/`, `configs/`, `data/`; pin a conda env under `/data/jvl210002/conda_envs/` (`sae_lens` / `circuit-tracer` / `transformer_lens`); smoke-test the E1 feature-extraction path on a supported small model (Gemma-2-2B / Llama-3.2-1B) before the Llama-3.1-8B run.
